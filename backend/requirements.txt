# RecViz Backend
fastapi==0.128.6
uvicorn==0.40.0
httpx==0.28.1
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# Superset runs in Docker (its own container, its own Python).
# RecViz backend talks to Superset via HTTP (httpx), NOT as a Python dependency.
# Pinned version reference: apache-superset==6.0.0 (see superset/Dockerfile)

# Database / Cache
psycopg2-binary==2.9.11
redis==4.6.0
requests==2.32.5

# Async SQLAlchemy + Migrations
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
alembic==1.18.4
