---
phase: 08-alembic-audit-dead-code-sweep-memory-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/types.py
  - backend/app/db/models/kpi.py
  - backend/app/db/models/connection.py
  - backend/app/db/models/chart.py
  - backend/app/db/models/dashboard.py
  - backend/app/db/models/data_source.py
  - backend/app/db/models/dataset.py
  - backend/requirements.txt
  - backend/app/services/config_migrator.py
  - backend/tests/test_connection_model.py
  - backend/tests/test_portable_json.py
  - backend/tests/test_query_utils.py
  - backend/tests/test_schema_introspection.py
  - backend/tests/test_test_connection_by_id.py
  - backend/tests/test_uri_builder.py
  - frontend/src/components/explorer/schema-browser.test.tsx
  - CLAUDE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/USAGE-TRACKER.md
autonomous: true
requirements:
  - FINAL-01
  - FINAL-02
  - FINAL-03
  - FINAL-04
  - FINAL-05
  - FINAL-06

must_haves:
  truths:
    - "PortableJSON alias no longer exists in types.py and all 6 ORM model files import OracleJSON directly"
    - "Alembic migration creates exactly 6 recviz_* tables with no extraneous objects"
    - "v$parameter COMPATIBLE value is documented"
    - "Dead code files from USAGE-TRACKER are deleted and no broken imports remain"
    - "requirements.txt contains only actually-imported dependencies"
    - "CLAUDE.md local dev section accurately reflects Docker Oracle setup"
  artifacts:
    - path: "backend/app/db/types.py"
      provides: "OracleJSON type without PortableJSON alias"
      contains: "class OracleJSON"
    - path: "CLAUDE.md"
      provides: "Updated local dev infrastructure section"
      contains: "gvenzl/oracle-free"
  key_links:
    - from: "backend/app/db/models/*.py"
      to: "backend/app/db/types.py"
      via: "OracleJSON import"
      pattern: "from app.db.types import OracleJSON"
---

<objective>
Backend cleanup: PortableJSON removal, Alembic audit, dead code sweep, requirements prune, CLAUDE.md drift fix.

Purpose: Final milestone consolidation -- remove the one-milestone-grace PortableJSON alias, audit Alembic against live Oracle, sweep dead code identified across phases 1-7, prune unused Python dependencies, and fix CLAUDE.md drift discovered during the milestone.

Output: Clean backend with no dead code, no stale aliases, accurate documentation.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/USAGE-TRACKER.md
@.planning/phases/08-alembic-audit-dead-code-sweep-memory-cleanup/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/db/types.py (CURRENT -- line 50 must be removed):
```python
# Grace alias -- retained for one milestone; Phase 8 removes this and updates all imports.
PortableJSON = OracleJSON
```

6 ORM model files that import PortableJSON (all must change to OracleJSON):
- backend/app/db/models/kpi.py:4        from app.db.types import PortableJSON
- backend/app/db/models/connection.py:11 from app.db.types import PortableJSON
- backend/app/db/models/chart.py:4      from app.db.types import PortableJSON
- backend/app/db/models/dashboard.py:4  from app.db.types import PortableJSON
- backend/app/db/models/data_source.py:4 from app.db.types import PortableJSON
- backend/app/db/models/dataset.py:4    from app.db.types import PortableJSON

Each file also uses `PortableJSON()` in mapped_column calls -- those must change to `OracleJSON()`.

Dead code candidates from USAGE-TRACKER (Phase 8 resolution):
- backend/app/services/config_migrator.py -- not imported anywhere in backend/app/
- backend/tests/test_connection_model.py -- async-mocked, stale
- backend/tests/test_portable_json.py -- tests alias being removed
- backend/tests/test_query_utils.py -- async-mocked, stale
- backend/tests/test_schema_introspection.py -- async-mocked, stale
- backend/tests/test_test_connection_by_id.py -- async-mocked, stale
- backend/tests/test_uri_builder.py -- async-mocked, stale
- frontend/src/components/explorer/schema-browser.test.tsx -- test file, deferred

CLAUDE.md drift: Local Dev section says "Oracle Cloud Always Free Autonomous Database 19c"
and "No Docker" but actual setup uses Docker gvenzl/oracle-free per project_local_dev_setup
and project_oracle_local_setup memory entries. Rule #4 needs nuance (Docker is for Oracle DB
server only, not the app).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic audit + requirements prune + v$parameter check</name>
  <read_first>
    - backend/app/migrations/versions/001_initial_oracle_schema.py
    - backend/app/migrations/alembic.ini
    - backend/app/migrations/env.py
    - backend/requirements.txt
  </read_first>
  <files>backend/requirements.txt</files>
  <action>
**Alembic audit (per D-01):**
1. Run `cd /Users/aarun/Workspace/Projects/RecViz/backend && PYTHONPATH=. alembic -c app/migrations/alembic.ini upgrade head` against live Oracle. Capture output.
2. After migration completes, verify exactly 6 `recviz_*` tables exist by running:
   ```python
   import oracledb
   conn = oracledb.connect(user='recviz', password='recviz_dev', dsn='localhost:1521/FREEPDB1')
   cursor = conn.cursor()
   cursor.execute("SELECT table_name FROM user_tables WHERE table_name LIKE 'RECVIZ_%' ORDER BY table_name")
   tables = [r[0] for r in cursor.fetchall()]
   ```
   Expected: RECVIZ_ALEMBIC_VERSION, RECVIZ_CHARTS, RECVIZ_CONNECTIONS, RECVIZ_DASHBOARDS, RECVIZ_DATASETS, RECVIZ_DATA_SOURCES, RECVIZ_KPIS (7 tables including alembic version table, 6 app tables).
3. Check for extraneous objects: `SELECT object_name, object_type FROM user_objects WHERE object_name LIKE 'RECVIZ_%' AND object_type NOT IN ('TABLE', 'INDEX')` -- should only show CHECK constraints (the IS JSON ones).

**v$parameter COMPATIBLE check (per D-02):**
Run: `SELECT value FROM v$parameter WHERE name = 'compatible'` via the Oracle connection. Document the returned value. On Oracle Cloud/Docker gvenzl/oracle-free this will be something like 23.x.x (Docker is Oracle 23ai Free). Note: Citi prod is 19c so COMPATIBLE there is expected 19.0.0. Both support 128-byte identifiers. Document this finding in the USAGE-TRACKER Phase 8 section.

**Requirements prune (per D-05):**
For each dependency in `backend/requirements.txt`, grep `backend/app/` for actual import usage:
- `fastapi` -- imported in api/ routes, main.py. KEEP.
- `uvicorn` -- imported in main.py. KEEP.
- `pydantic` -- imported in models/. KEEP.
- `pydantic-settings` -- imported in config.py. KEEP.
- `python-dotenv` -- used by pydantic-settings. KEEP.
- `sqlalchemy` -- imported everywhere in db/. KEEP.
- `alembic` -- used by migrations/. KEEP.
- `oracledb` -- imported in engine.py, main.py. KEEP.
- `cryptography` -- imported in services/encryption.py. KEEP.

All 9 dependencies are actually used. No pruning needed but verify with grep. If any surprise unused deps are found, remove them.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/backend && python -c "
import oracledb
oracledb.init_oracle_client(lib_dir='/Users/aarun/oracle/instantclient')
conn = oracledb.connect(user='recviz', password='recviz_dev', dsn='localhost:1521/FREEPDB1')
cur = conn.cursor()
cur.execute(\"SELECT table_name FROM user_tables WHERE table_name LIKE 'RECVIZ_%' ORDER BY table_name\")
tables = [r[0] for r in cur.fetchall()]
print('Tables:', tables)
assert len([t for t in tables if t != 'RECVIZ_ALEMBIC_VERSION']) == 6, f'Expected 6 app tables, got {len(tables)-1}'
cur.execute(\"SELECT value FROM v\\$parameter WHERE name = 'compatible'\")
compat = cur.fetchone()
print('COMPATIBLE:', compat[0] if compat else 'NOT FOUND')
conn.close()
print('Alembic audit PASSED')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Alembic migration applies cleanly, 6 recviz_* app tables exist
    - v$parameter COMPATIBLE value documented in USAGE-TRACKER
    - requirements.txt verified -- all deps actually imported (no changes needed unless surprise found)
  </acceptance_criteria>
  <done>Alembic migration verified clean against live Oracle, COMPATIBLE value documented, requirements.txt confirmed lean</done>
</task>

<task type="auto">
  <name>Task 2: PortableJSON removal + dead code sweep</name>
  <read_first>
    - backend/app/db/types.py
    - backend/app/db/models/kpi.py
    - backend/app/db/models/connection.py
    - backend/app/db/models/chart.py
    - backend/app/db/models/dashboard.py
    - backend/app/db/models/data_source.py
    - backend/app/db/models/dataset.py
    - backend/app/services/config_migrator.py
    - .planning/USAGE-TRACKER.md
  </read_first>
  <files>
    backend/app/db/types.py,
    backend/app/db/models/kpi.py,
    backend/app/db/models/connection.py,
    backend/app/db/models/chart.py,
    backend/app/db/models/dashboard.py,
    backend/app/db/models/data_source.py,
    backend/app/db/models/dataset.py,
    backend/app/services/config_migrator.py,
    backend/tests/test_connection_model.py,
    backend/tests/test_portable_json.py,
    backend/tests/test_query_utils.py,
    backend/tests/test_schema_introspection.py,
    backend/tests/test_test_connection_by_id.py,
    backend/tests/test_uri_builder.py,
    frontend/src/components/explorer/schema-browser.test.tsx,
    .planning/USAGE-TRACKER.md
  </files>
  <action>
**PortableJSON removal (per D-06):**
1. Edit `backend/app/db/types.py`: Remove lines 49-50 (the comment and `PortableJSON = OracleJSON` alias). Nothing else changes in this file.
2. Edit each of the 6 ORM model files to replace `from app.db.types import PortableJSON` with `from app.db.types import OracleJSON`, and replace all `PortableJSON()` calls with `OracleJSON()`:
   - `backend/app/db/models/kpi.py`: line 4 import, line 23 mapped_column
   - `backend/app/db/models/connection.py`: line 11 import, line 31 mapped_column
   - `backend/app/db/models/chart.py`: line 4 import, line 22 mapped_column
   - `backend/app/db/models/dashboard.py`: line 4 import, line 17 mapped_column
   - `backend/app/db/models/data_source.py`: line 4 import, line 16 mapped_column
   - `backend/app/db/models/dataset.py`: line 4 import, line 22 mapped_column
3. After all edits, run `cd /Users/aarun/Workspace/Projects/RecViz/backend && python -c "from app.db.types import OracleJSON; from app.db.models.chart import RecvizChart; from app.db.models.connection import RecvizConnection; from app.db.models.dashboard import RecvizDashboard; from app.db.models.dataset import RecvizDataset; from app.db.models.data_source import RecvizDataSource; from app.db.models.kpi import RecvizKpi; print('All models import OK')"` to verify no import errors.

**Dead code sweep (per D-03, D-04):**
Delete the following confirmed dead code files:
- `backend/app/services/config_migrator.py` -- not imported anywhere in `backend/app/` (confirmed via grep). Contains config schema migration framework with zero registered migrations.
- `backend/tests/test_connection_model.py` -- references `PortableJSON` (being removed), async-mocked against pre-sync patterns
- `backend/tests/test_portable_json.py` -- tests the alias being removed
- `backend/tests/test_query_utils.py` -- async-mocked, stale
- `backend/tests/test_schema_introspection.py` -- async-mocked, stale
- `backend/tests/test_test_connection_by_id.py` -- async-mocked, stale
- `backend/tests/test_uri_builder.py` -- async-mocked, stale
- `frontend/src/components/explorer/schema-browser.test.tsx` -- test file, tests deferred this milestone

After deletion, verify no broken imports:
- `cd /Users/aarun/Workspace/Projects/RecViz/backend && python -c "from app.main import app; print('Backend imports OK')"` -- ensures no transitive import of deleted files
- `cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit 2>&1 | head -20` -- ensures no TypeScript compilation errors from deleted test file
- `grep -r "config_migrator" /Users/aarun/Workspace/Projects/RecViz/backend/app/ 2>/dev/null` -- must return empty (no remaining references)
- `grep -r "PortableJSON" /Users/aarun/Workspace/Projects/RecViz/backend/app/ 2>/dev/null` -- must return empty

**Update USAGE-TRACKER.md Phase 8 section** with:
- Files removed (config_migrator.py, 6 test files, 1 frontend test file)
- Files modified (types.py, 6 ORM models)
- Resolution of all Phase 1 dead code candidates
- v$parameter COMPATIBLE finding from Task 1
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/backend && python -c "
from app.db.types import OracleJSON
# Verify PortableJSON alias is gone
try:
    from app.db.types import PortableJSON
    print('FAIL: PortableJSON still importable')
    exit(1)
except ImportError:
    print('OK: PortableJSON alias removed')

# Verify all models import cleanly with OracleJSON
from app.db.models.chart import RecvizChart
from app.db.models.connection import RecvizConnection
from app.db.models.dashboard import RecvizDashboard
from app.db.models.dataset import RecvizDataset
from app.db.models.data_source import RecvizDataSource
from app.db.models.kpi import RecvizKpi
print('OK: All 6 ORM models import with OracleJSON')

# Verify config_migrator is gone
import importlib
try:
    importlib.import_module('app.services.config_migrator')
    print('FAIL: config_migrator still exists')
    exit(1)
except ModuleNotFoundError:
    print('OK: config_migrator deleted')

# Verify app boots
from app.main import app
print('OK: Backend app imports successfully')
print('Dead code sweep PASSED')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `from app.db.types import PortableJSON` raises ImportError
    - All 6 ORM models use `from app.db.types import OracleJSON` and `OracleJSON()` in mapped_column
    - config_migrator.py deleted, 6 backend test files deleted, 1 frontend test file deleted
    - grep for "PortableJSON" in backend/app/ returns zero hits
    - grep for "config_migrator" in backend/app/ returns zero hits
    - Backend app imports successfully (no broken transitive imports)
    - Frontend `tsc --noEmit` passes (no broken imports from deleted test)
    - USAGE-TRACKER.md Phase 8 section updated with all changes
  </acceptance_criteria>
  <done>PortableJSON alias removed from types.py, 6 ORM models updated to OracleJSON, 8 dead code files deleted, no broken imports remain, USAGE-TRACKER updated</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md drift fix + codebase docs update</name>
  <read_first>
    - CLAUDE.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <files>CLAUDE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md</files>
  <action>
**CLAUDE.md update (per D-07):**
The Local Dev section has drifted from reality. Fix the following:

1. **Rule #4 nuance:** Change "No Docker. Not in dev, not in prod. Everything runs natively." to "No Docker for the application. Not in dev, not in prod. Python, FastAPI, and the frontend run natively. The Oracle database server runs in Docker locally (gvenzl/oracle-free) for convenience."

2. **Local Dev section:** Replace:
   ```
   - **Oracle Cloud Always Free Autonomous Database 19c** — stands in for Citi's Oracle.
   ```
   With:
   ```
   - **Docker Oracle** (`gvenzl/oracle-free:latest`) — local dev database. Oracle 23ai Free in container, code targets 19c compatibility. Start: `docker run -d --name oracle-26ai -p 1521:1521 -e ORACLE_PASSWORD=RecViz2026 -e APP_USER=recviz -e APP_USER_PASSWORD=recviz_dev gvenzl/oracle-free:latest`
   ```

3. **Remove "No Docker" from Local Dev bullet:** The "No Docker. No Postgres. No Redis. No Celery." line should become "No Postgres. No Redis. No Celery." (Docker is used for Oracle DB).

4. **Add seed command to Local Dev:** After the frontend bullet, add:
   ```
   - **Seed data**: `cd backend && PYTHONPATH=. python ../scripts/seed-oracle.py`
   - **Migrations**: `cd backend && PYTHONPATH=. alembic -c app/migrations/alembic.ini upgrade head`
   ```

**Codebase docs update:**
1. `STRUCTURE.md`: Find all references to `PortableJSON` and replace with `OracleJSON`. The file at line 105 says `types.py  # PortableJSON (JSONB / JSON)` -- update to `types.py  # OracleJSON (BLOB IS JSON)`. Line 229 and 391 also reference PortableJSON.

2. `ARCHITECTURE.md`: Find all references to `PortableJSON` and update to `OracleJSON`. Line 74 says `PortableJSON custom type (JSONB on PostgreSQL, JSON on Oracle)` -- update to `OracleJSON custom type (BLOB IS JSON on Oracle 19c)`. Line 149 section header also says PortableJSON.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz && grep -c "PortableJSON" CLAUDE.md .planning/codebase/STRUCTURE.md .planning/codebase/ARCHITECTURE.md 2>/dev/null | grep -v ":0$" && echo "FAIL: PortableJSON references remain" && exit 1; grep -q "gvenzl/oracle-free" CLAUDE.md && echo "OK: Docker Oracle documented" || (echo "FAIL: Docker Oracle not in CLAUDE.md" && exit 1); grep -q "OracleJSON" .planning/codebase/STRUCTURE.md && echo "OK: STRUCTURE.md updated" || (echo "FAIL: STRUCTURE.md not updated" && exit 1); echo "CLAUDE.md update PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Local Dev section mentions gvenzl/oracle-free Docker setup
    - CLAUDE.md rule #4 clarifies Docker is for Oracle DB only, not the app
    - CLAUDE.md Local Dev no longer says "No Docker"
    - CLAUDE.md includes seed and migration commands
    - Zero references to "PortableJSON" in CLAUDE.md, STRUCTURE.md, ARCHITECTURE.md
    - All references updated to OracleJSON with accurate descriptions
  </acceptance_criteria>
  <done>CLAUDE.md accurately reflects Docker Oracle local dev, rule #4 clarified, codebase docs updated from PortableJSON to OracleJSON</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This phase is cleanup/audit only -- no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | Alembic migration | accept | Read-only audit, no schema changes made |
| T-08-02 | I (Info Disclosure) | CLAUDE.md | accept | Contains no secrets, only setup instructions |
</threat_model>

<verification>
1. `from app.db.types import PortableJSON` raises ImportError
2. All 6 ORM models use OracleJSON directly
3. Backend boots successfully: `python -c "from app.main import app"`
4. Alembic migration verified against live Oracle (6 app tables)
5. grep for PortableJSON/config_migrator in backend/app/ returns zero hits
6. CLAUDE.md contains gvenzl/oracle-free and no "PortableJSON"
7. Codebase docs (STRUCTURE.md, ARCHITECTURE.md) say OracleJSON
</verification>

<success_criteria>
- PortableJSON alias completely removed from codebase (types.py, models, docs)
- 8 dead code files deleted (1 service, 6 backend tests, 1 frontend test)
- Alembic audit documented with COMPATIBLE value
- requirements.txt verified lean
- CLAUDE.md accurately reflects current local dev setup
- No broken imports in backend or frontend
</success_criteria>

<output>
After completion, create `.planning/phases/08-alembic-audit-dead-code-sweep-memory-cleanup/08-01-SUMMARY.md`
</output>
