---
phase: 08-dashboard-builder
plan: 05
type: execute
wave: 3
depends_on: ["08-03", "08-04"]
files_modified:
  - frontend/src/components/builder/builder-toolbar.tsx
  - frontend/src/hooks/use-builder-keyboard-shortcuts.ts
  - frontend/src/components/builder/builder-page.tsx
  - frontend/src/routes/_app/dashboards/new.tsx
  - frontend/src/routes/_app/dashboards/$dashboardId.edit.tsx
autonomous: true
requirements:
  - BLDR-02
  - BLDR-06
  - BLDR-07
must_haves:
  truths:
    - "BuilderToolbar renders [+ Add], Undo, Redo, Save Dashboard, Save As, and Exit Builder buttons"
    - "Undo/Redo buttons are disabled when history stacks are empty"
    - "Ctrl+Z triggers undo, Ctrl+Shift+Z triggers redo, Ctrl+S triggers save — only in edit mode"
    - "BuilderPage composes toolbar, canvas, empty state, and panels into a complete builder experience"
    - "Panels render live chart/KPI/grid content inside BuilderPanel wrappers on the canvas"
  artifacts:
    - path: "frontend/src/components/builder/builder-toolbar.tsx"
      provides: "Top toolbar for the builder"
      exports: ["BuilderToolbar"]
    - path: "frontend/src/hooks/use-builder-keyboard-shortcuts.ts"
      provides: "Keyboard shortcut hook for undo/redo/save"
      exports: ["useBuilderKeyboardShortcuts"]
    - path: "frontend/src/components/builder/builder-page.tsx"
      provides: "Full builder page composing toolbar + canvas + panels"
      exports: ["BuilderPage"]
  key_links:
    - from: "frontend/src/components/builder/builder-toolbar.tsx"
      to: "frontend/src/stores/layout-history-store.ts"
      via: "undo/redo button handlers"
      pattern: "useLayoutHistoryStore"
    - from: "frontend/src/components/builder/builder-page.tsx"
      to: "frontend/src/components/builder/builder-canvas.tsx"
      via: "renders BuilderCanvas"
      pattern: "BuilderCanvas"
---

<objective>
BuilderToolbar (top bar with all actions), keyboard shortcuts hook, and BuilderPage component that composes the full builder experience.

Purpose: The toolbar (D-10) provides all builder actions: add content, undo/redo (D-07), save, save as, and exit. The BuilderPage component wires together the toolbar, canvas, empty state, inline title/description editing, and panel rendering — making the builder a complete interactive page. This is where the WYSIWYG experience comes together.

Output: Complete builder page with toolbar, keyboard shortcuts, and live panel rendering.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-dashboard-builder/08-CONTEXT.md
@.planning/phases/08-dashboard-builder/08-UI-SPEC.md
@.planning/phases/08-dashboard-builder/08-03-SUMMARY.md
@.planning/phases/08-dashboard-builder/08-04-SUMMARY.md

<interfaces>
From frontend/src/stores/builder-store.ts:
```typescript
useBuilderStore: { items, filters, name, description, isDirty, dashboardId,
  initNew, initFromConfig, updateName, updateDescription, addItem, removeItem,
  updateLayouts, markClean, ... }
```

From frontend/src/stores/layout-history-store.ts:
```typescript
useLayoutHistoryStore: { past, future, canUndo, canRedo, pushSnapshot, undo, redo, reset }
```

From frontend/src/components/builder/builder-canvas.tsx:
```typescript
export function BuilderCanvas({ children }: BuilderCanvasProps)
```

From frontend/src/components/builder/builder-panel.tsx:
```typescript
export function BuilderPanel({ item, onEdit, onRemove, children }: BuilderPanelProps)
```

From frontend/src/components/builder/builder-empty-state.tsx:
```typescript
export function BuilderEmptyState({ onAddContent }: BuilderEmptyStateProps)
```

From frontend/src/components/charts/chart-factory.tsx:
```typescript
export function ChartFactory({ config, ... }: ChartFactoryProps)
```

From frontend/src/components/kpis/kpi-preview-card.tsx:
```typescript
export function KpiPreviewCard({ ... }: KpiPreviewCardProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuilderToolbar and keyboard shortcuts hook</name>
  <files>frontend/src/components/builder/builder-toolbar.tsx, frontend/src/hooks/use-builder-keyboard-shortcuts.ts</files>
  <read_first>
    - frontend/src/stores/layout-history-store.ts (canUndo, canRedo, undo, redo)
    - frontend/src/stores/builder-store.ts (isDirty, markClean)
    - frontend/src/components/ui/button.tsx (Button variants: primary, outline, ghost)
    - frontend/src/components/ui/separator.tsx (Separator component)
    - frontend/src/components/ui/tooltip.tsx (Tooltip components)
    - .planning/phases/08-dashboard-builder/08-UI-SPEC.md (Edit Mode toolbar layout)
  </read_first>
  <action>
1. Create `frontend/src/hooks/use-builder-keyboard-shortcuts.ts`:
   - Accept callbacks: `onUndo: () => void`, `onRedo: () => void`, `onSave: () => void`, `enabled: boolean`
   - useEffect with keydown listener:
     - Ctrl+Z (no Shift): `e.preventDefault()`, call onUndo
     - Ctrl+Shift+Z: `e.preventDefault()`, call onRedo
     - Ctrl+S: `e.preventDefault()`, call onSave
   - Check `e.metaKey || e.ctrlKey` for Mac/Windows compatibility
   - Cleanup on unmount
   - Named export `useBuilderKeyboardShortcuts`

2. Create `frontend/src/components/builder/builder-toolbar.tsx`:

```typescript
interface BuilderToolbarProps {
  onAddClick: () => void
  onSave: () => void
  onSaveAs: () => void
  onExit: () => void
  isSaving: boolean
}
```

Layout per UI-SPEC Edit Mode toolbar:
- Sticky container: `className="sticky top-0 z-30 flex items-center justify-between border-b bg-background px-6 py-2"`
- Left section: `<div className="flex items-center gap-2">`
  - [+ Add] button: `<Button size="sm" onClick={onAddClick}><Plus className="mr-1.5 size-4" />Add</Button>` (primary variant, default)
  - `<Separator orientation="vertical" className="mx-1 h-6" />`
  - Undo button: `<Tooltip><TooltipTrigger asChild><Button variant="ghost" size="sm" onClick={handleUndo} disabled={!canUndo}><Undo2 className="size-4" /></Button></TooltipTrigger><TooltipContent>Undo (Ctrl+Z)</TooltipContent></Tooltip>`
  - Redo button: `<Tooltip><TooltipTrigger asChild><Button variant="ghost" size="sm" onClick={handleRedo} disabled={!canRedo}><Redo2 className="size-4" /></Button></TooltipTrigger><TooltipContent>Redo (Ctrl+Shift+Z)</TooltipContent></Tooltip>`
  - Unsaved changes dot: `{isDirty && <span className="size-2.5 rounded-full bg-amber-500" />}` (per UI-SPEC)
- Right section: `<div className="flex items-center gap-2">`
  - [Save Dashboard]: `<Button size="sm" onClick={onSave} disabled={isSaving}>{isSaving ? <Loader2 className="mr-1.5 size-4 animate-spin" /> : null}Save Dashboard</Button>` (primary variant)
  - [Save As]: `<Button variant="outline" size="sm" onClick={onSaveAs}>Save As</Button>`
  - [Exit Builder]: `<Button variant="ghost" size="sm" onClick={onExit}>Exit Builder</Button>`

Read canUndo/canRedo from useLayoutHistoryStore. Read isDirty from useBuilderStore.
handleUndo/handleRedo call the store's undo/redo and then update builder store's layouts.
Wrap in `<TooltipProvider delayDuration={300}>`.

Icons: `Plus`, `Undo2`, `Redo2`, `Loader2` from lucide-react.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - use-builder-keyboard-shortcuts.ts exports `useBuilderKeyboardShortcuts`
    - use-builder-keyboard-shortcuts.ts handles Ctrl+Z (undo), Ctrl+Shift+Z (redo), Ctrl+S (save)
    - use-builder-keyboard-shortcuts.ts checks `e.metaKey || e.ctrlKey`
    - builder-toolbar.tsx exports `BuilderToolbar` as named export
    - builder-toolbar.tsx contains buttons with text "Add", "Save Dashboard", "Save As", "Exit Builder"
    - builder-toolbar.tsx contains `Undo2` and `Redo2` icon imports from lucide-react
    - builder-toolbar.tsx contains `disabled={!canUndo}` and `disabled={!canRedo}`
    - builder-toolbar.tsx contains `bg-amber-500` for unsaved changes indicator
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>BuilderToolbar renders all builder actions and keyboard shortcuts hook handles Ctrl+Z/Shift+Z/S.</done>
</task>

<task type="auto">
  <name>Task 2: BuilderPage composition — full builder experience</name>
  <files>frontend/src/components/builder/builder-page.tsx, frontend/src/routes/_app/dashboards/new.tsx, frontend/src/routes/_app/dashboards/$dashboardId.edit.tsx</files>
  <read_first>
    - frontend/src/components/builder/builder-toolbar.tsx (just created)
    - frontend/src/components/builder/builder-canvas.tsx (from Plan 03)
    - frontend/src/components/builder/builder-panel.tsx (from Plan 04)
    - frontend/src/components/builder/builder-empty-state.tsx (from Plan 04)
    - frontend/src/stores/builder-store.ts (items, name, description, updateName, updateDescription, removeItem)
    - frontend/src/hooks/use-builder-keyboard-shortcuts.ts (just created)
    - frontend/src/hooks/use-managed-dashboards.ts (useCreateDashboard, useUpdateDashboard)
    - frontend/src/components/charts/chart-factory.tsx (for rendering charts inside panels)
    - frontend/src/components/kpis/kpi-preview-card.tsx (for rendering KPIs inside panels)
    - frontend/src/components/dashboard/config-data-grid.tsx (for rendering grids inside panels)
    - frontend/src/routes/_app/dashboards/$dashboardId.tsx (existing view page for reference)
  </read_first>
  <action>
1. Create `frontend/src/components/builder/builder-page.tsx`:

```typescript
interface BuilderPageProps {
  mode: 'create' | 'edit'
}
```

This is the main composition component. It:

a) **Inline title/description editing** (above canvas, below toolbar):
   - Name: `<input type="text" value={name} onChange={(e) => updateName(e.target.value)} placeholder="Untitled Dashboard" className="w-full bg-transparent text-2xl font-semibold tracking-tight border-transparent hover:border-input focus:border-input focus:outline-none border rounded-md px-2 py-1" />`
   - Description: `<input type="text" value={description} onChange={(e) => updateDescription(e.target.value)} placeholder="Add a description..." className="w-full bg-transparent text-sm text-muted-foreground border-transparent hover:border-input focus:border-input focus:outline-none border rounded-md px-2 py-1 mt-1" />`

b) **Toolbar rendering**: `<BuilderToolbar onAddClick={handleAddClick} onSave={handleSave} onSaveAs={handleSaveAs} onExit={handleExit} isSaving={isSaving} />`

c) **Canvas with panels**:
   - If items.length === 0: render `<BuilderEmptyState onAddContent={handleAddClick} />` (with AnimatePresence for fade-out)
   - If items.length > 0: render `<BuilderCanvas>` with children being `items.map((item) => <div key={item.id}><BuilderPanel item={item} onEdit={handleEditPanel} onRemove={handleRemovePanel}>` with content based on item.type:
     - `'chart'`: render a placeholder div with chart type icon and title (full ChartFactory integration will work once charts are added via picker dialog — for now show the chart title and type as a labeled card)
     - `'kpi'`: render a placeholder with KPI title (KpiPreviewCard integration later)
     - `'grid'`: render a placeholder with grid title

d) **Save handler**: calls `useCreateDashboard().mutateAsync(...)` for create mode or `useUpdateDashboard().mutateAsync(...)` for edit mode. On success, calls `markClean()` and `historyStore.reset()`, shows `toast.success('Dashboard saved')`, navigates to `/dashboards/${id}`.

e) **Exit handler**: if isDirty, show unsaved changes warning (for now, `window.confirm('You have unsaved changes. Leave without saving?')`). Navigate to `/dashboards` or `/dashboards/${dashboardId}`.

f) **Keyboard shortcuts**: call `useBuilderKeyboardShortcuts(handleUndo, handleRedo, handleSave, true)`

g) **handleAddClick**: no-op for now (will be wired to AddContentMenu in Plan 06)

h) **handleEditPanel**: no-op for now (will be wired to PanelConfigPopover in Plan 07)

i) **handleRemovePanel**: calls `removeItem(itemId)` from builder store

2. Update `frontend/src/routes/_app/dashboards/new.tsx`:
   - Import and render `<BuilderPage mode="create" />`
   - Remove the old placeholder content

3. Update `frontend/src/routes/_app/dashboards/$dashboardId.edit.tsx`:
   - Keep the `useManagedDashboard(dashboardId)` fetch and initFromConfig logic
   - Replace placeholder with `<BuilderPage mode="edit" />`
   - Show loading state while dashboard loads
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - builder-page.tsx exports `BuilderPage` with props `mode: 'create' | 'edit'`
    - builder-page.tsx renders `BuilderToolbar`, inline title input, inline description input, and `BuilderCanvas`
    - builder-page.tsx renders `BuilderEmptyState` when items.length === 0
    - builder-page.tsx renders `BuilderPanel` for each item when items.length > 0
    - builder-page.tsx calls `useBuilderKeyboardShortcuts`
    - builder-page.tsx contains save logic using `useCreateDashboard` (create mode) or `useUpdateDashboard` (edit mode)
    - builder-page.tsx calls `toast.success('Dashboard saved')` on save success
    - new.tsx renders `<BuilderPage mode="create" />`
    - $dashboardId.edit.tsx renders `<BuilderPage mode="edit" />`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>BuilderPage composes toolbar, canvas, panels, and save/exit logic into a complete builder experience. Route pages render the full builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Save action sends DashboardConfig JSON to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | Save dashboard | mitigate | Config serialized from builder store state (typed). Backend validates with Pydantic. |
| T-08-08 | Repudiation | Save dashboard | accept | No audit logging yet (deferred to SECU-04). Save overwrites previous version. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. `cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -5` shows no failures
</verification>

<success_criteria>
- BuilderToolbar renders all action buttons with correct labels
- Keyboard shortcuts work for undo/redo/save
- BuilderPage composes the complete builder experience
- Route pages render the full builder
- Save creates/updates dashboards via API
- Exit handles unsaved changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-builder/08-05-SUMMARY.md`
</output>
