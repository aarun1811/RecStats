---
phase: 08-dashboard-builder
plan: 04
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - frontend/src/components/builder/builder-panel.tsx
  - frontend/src/components/builder/builder-empty-state.tsx
autonomous: true
requirements:
  - BLDR-02
  - BLDR-06
must_haves:
  truths:
    - "BuilderPanel wraps each grid item with a drag handle header (GripVertical icon), title, edit button (Pencil), and remove button (X)"
    - "Drag handle has class 'drag-handle' so react-grid-layout restricts drag initiation to the header"
    - "Edit button shows tooltip 'Edit Panel Settings' and remove button shows 'Remove from Dashboard'"
    - "Empty state shows centered message 'Start building your dashboard' with '+ Add Content' button"
  artifacts:
    - path: "frontend/src/components/builder/builder-panel.tsx"
      provides: "Grid item wrapper with drag handle and controls"
      exports: ["BuilderPanel"]
    - path: "frontend/src/components/builder/builder-empty-state.tsx"
      provides: "Empty canvas state"
      exports: ["BuilderEmptyState"]
  key_links:
    - from: "frontend/src/components/builder/builder-panel.tsx"
      to: "frontend/src/types/builder.ts"
      via: "import BuilderItem"
      pattern: "import.*BuilderItem"
---

<objective>
BuilderPanel wrapper component (each draggable/resizable grid item) and the empty canvas state component.

Purpose: BuilderPanel is the visual container for every chart, KPI, and grid on the canvas. It provides the drag handle (D-03), edit/remove buttons, and content area. The empty state (D-14) welcomes users to a new dashboard. Both are needed before the canvas can display any content.

Output: BuilderPanel with drag handle, edit/remove controls, hover ring, and content slot. BuilderEmptyState with CTA button.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-dashboard-builder/08-CONTEXT.md
@.planning/phases/08-dashboard-builder/08-UI-SPEC.md
@.planning/phases/08-dashboard-builder/08-02-SUMMARY.md

<interfaces>
From frontend/src/types/builder.ts (created in Plan 02):
```typescript
export type BuilderItemType = 'chart' | 'kpi' | 'grid'
export interface BuilderItem { id: string; type: BuilderItemType; layout: ChartLayout; chart?: BuilderChartRef; kpi?: BuilderKpiRef; grid?: BuilderGridRef; }
```

From frontend/src/components/ui/empty.tsx:
```typescript
export function Empty({ className, children, ...props }: React.HTMLAttributes<HTMLDivElement>)
export function EmptyHeader({ ... })
export function EmptyMedia({ variant, ... })
export function EmptyTitle({ ... })
export function EmptyDescription({ ... })
export function EmptyContent({ ... })
```

From frontend/src/components/ui/tooltip.tsx:
```typescript
export { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger }
```

From frontend/src/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuilderPanel grid item wrapper</name>
  <files>frontend/src/components/builder/builder-panel.tsx</files>
  <read_first>
    - frontend/src/types/builder.ts (BuilderItem type)
    - frontend/src/components/ui/tooltip.tsx (Tooltip components)
    - frontend/src/components/ui/button.tsx (Button component, ghost variant, size="icon")
    - frontend/src/components/dashboard/config-chart-grid.tsx (existing chart card pattern for reference)
    - .planning/phases/08-dashboard-builder/08-UI-SPEC.md (BuilderPanel specs)
  </read_first>
  <action>
Create `frontend/src/components/builder/builder-panel.tsx`:

```typescript
interface BuilderPanelProps {
  item: BuilderItem
  onEdit: (itemId: string) => void
  onRemove: (itemId: string) => void
  children: React.ReactNode
}
```

Layout per UI-SPEC BuilderPanel section:
- Outer div: `className="group relative h-full rounded-lg border bg-card transition-all hover:ring-2 hover:ring-primary/20"`
- Active drag state will be handled by react-grid-layout CSS classes (adding `opacity-80 shadow-xl ring-2 ring-primary/50` via RGL's built-in classes)
- Header div: `className="drag-handle flex cursor-grab items-center justify-between border-b px-3 py-2 active:cursor-grabbing"`
  - Left side: `<div className="flex items-center gap-2">` containing:
    - `<GripVertical className="size-4 text-muted-foreground" />` (no tooltip per UI-SPEC)
    - `<span className="text-sm font-medium truncate flex-1">{title}</span>` where title is derived from `item.chart?.title ?? item.kpi?.title ?? item.grid?.title ?? 'Untitled'`
  - Right side: `<div className="flex items-center gap-1">` containing:
    - Edit button: `<Tooltip><TooltipTrigger asChild><Button variant="ghost" size="icon" className="h-7 w-7" onClick={(e) => { e.stopPropagation(); onEdit(item.id) }}><Pencil className="size-3.5" /></Button></TooltipTrigger><TooltipContent side="bottom" sideOffset={4}>Edit Panel Settings</TooltipContent></Tooltip>`
    - Remove button: `<Tooltip><TooltipTrigger asChild><Button variant="ghost" size="icon" className="h-7 w-7 hover:text-destructive" onClick={(e) => { e.stopPropagation(); onRemove(item.id) }}><X className="size-3.5" /></Button></TooltipTrigger><TooltipContent side="bottom" sideOffset={4}>Remove from Dashboard</TooltipContent></Tooltip>`
- Content area: `<div className="flex-1 overflow-hidden px-3 pb-2">{children}</div>`

Icons from lucide-react: `GripVertical`, `Pencil`, `X`
Wrap the entire component in `<TooltipProvider delayDuration={300}>` for tooltip delay.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - builder-panel.tsx exports `BuilderPanel` as named export
    - builder-panel.tsx contains `className="drag-handle` on the header div
    - builder-panel.tsx imports `GripVertical`, `Pencil`, `X` from 'lucide-react'
    - builder-panel.tsx contains Tooltip with content "Edit Panel Settings"
    - builder-panel.tsx contains Tooltip with content "Remove from Dashboard"
    - builder-panel.tsx contains `hover:text-destructive` on the remove button
    - builder-panel.tsx contains `TooltipProvider` with `delayDuration={300}`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>BuilderPanel component renders a drag handle header with edit/remove buttons and content area, matching UI-SPEC exactly.</done>
</task>

<task type="auto">
  <name>Task 2: BuilderEmptyState component</name>
  <files>frontend/src/components/builder/builder-empty-state.tsx</files>
  <read_first>
    - frontend/src/components/ui/empty.tsx (Empty component pattern)
    - frontend/src/components/ui/button.tsx (Button component)
    - .planning/phases/08-dashboard-builder/08-UI-SPEC.md (Empty Canvas section and Copywriting Contract)
  </read_first>
  <action>
Create `frontend/src/components/builder/builder-empty-state.tsx`:

```typescript
interface BuilderEmptyStateProps {
  onAddContent: () => void
}
```

Layout per UI-SPEC:
- Use the composable `Empty` component from ui/empty.tsx
- `<Empty className="border-none py-12">` (no border since it's on the dashed canvas)
  - `<EmptyHeader>`
    - `<EmptyMedia variant="icon"><LayoutDashboard /></EmptyMedia>`
    - `<EmptyTitle>Start building your dashboard</EmptyTitle>`
    - `<EmptyDescription>Click + Add to place charts, KPIs, and data grids on the canvas.</EmptyDescription>`
  - `<EmptyContent>`
    - `<Button onClick={onAddContent}><Plus className="mr-2 size-4" />Add Content</Button>`

Fade-out animation per UI-SPEC: wrap in `<motion.div initial={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: 0.2, ease: 'easeOut' }}>` from `motion/react` with `<AnimatePresence>` in the parent (BuilderCanvas) to trigger exit animation when first item is added.

Icons from lucide-react: `LayoutDashboard`, `Plus`
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - builder-empty-state.tsx exports `BuilderEmptyState` as named export
    - builder-empty-state.tsx contains text "Start building your dashboard"
    - builder-empty-state.tsx contains text "Click + Add to place charts, KPIs, and data grids on the canvas."
    - builder-empty-state.tsx contains `<Button` with `onAddContent` onClick handler
    - builder-empty-state.tsx contains text "Add Content"
    - builder-empty-state.tsx imports from 'motion/react' (NOT 'framer-motion')
    - builder-empty-state.tsx uses `Empty` component from '@/components/ui/empty'
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>BuilderEmptyState shows empty canvas CTA with correct copy and fade-out animation support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Client-side UI components only. No server communication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Spoofing | BuilderPanel title | accept | Title comes from builder store which is populated from trusted backend data. React auto-escapes JSX. No XSS risk. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. `cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -5` shows no failures
</verification>

<success_criteria>
- BuilderPanel renders drag handle header with GripVertical, title, edit button with tooltip, remove button with tooltip
- BuilderEmptyState shows "Start building your dashboard" with "Add Content" button
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-builder/08-04-SUMMARY.md`
</output>
