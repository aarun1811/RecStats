---
phase: 06-dashboards-page
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - .planning/USAGE-TRACKER.md
autonomous: false
requirements:
  - DASH-02
  - DASH-04
  - DASH-08
  - DASH-10
must_haves:
  truths:
    - "Dashboard CRUD lifecycle verified end-to-end: create, view, edit, delete against Oracle"
    - "Dashboard renderer loads with real data from Oracle via the rewired pipeline"
    - "Embed route renders with filter, lock, hide, and theme URL params"
    - "URL filter round-tripping works correctly in the view route"
    - "USAGE-TRACKER.md updated with Phase 6 file inventory"
    - "Both light and dark mode work across all dashboard pages"
  artifacts:
    - path: ".planning/USAGE-TRACKER.md"
      provides: "Phase 6 file inventory"
      contains: "Phase 6"
  key_links:
    - from: "frontend/src/routes/embed/dashboards/$dashboardId.tsx"
      to: "frontend/src/components/dashboard/dashboard-renderer.tsx"
      via: "DashboardRenderer component"
      pattern: "DashboardRenderer"
    - from: "frontend/src/routes/_app/dashboards/$dashboardId.tsx"
      to: "frontend/src/lib/dashboard-url-state.ts"
      via: "parseFilterParams, serializeFilterParams"
      pattern: "serializeFilterParams"
---

<objective>
Verify the complete dashboard lifecycle via Playwright MCP against live Oracle, verify the embed route with all URL parameters, and update USAGE-TRACKER.md.

Purpose: This is the final verification plan -- confirms the pipeline fix (Plan 01), list page polish (Plan 02), renderer premium treatment (Plan 03), and builder/store cleanup (Plan 04) all work together as a cohesive dashboard experience.

Output: Verified dashboard lifecycle, verified embed route, updated USAGE-TRACKER.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboards-page/06-CONTEXT.md
@.planning/phases/06-dashboards-page/06-UI-SPEC.md
@.planning/phases/06-dashboards-page/06-01-SUMMARY.md
@.planning/phases/06-dashboards-page/06-02-SUMMARY.md
@.planning/phases/06-dashboards-page/06-03-SUMMARY.md
@.planning/phases/06-dashboards-page/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright MCP dashboard lifecycle + embed verification</name>
  <files>N/A (verification only)</files>
  <read_first>
    - frontend/src/routes/_app/dashboards/index.tsx
    - frontend/src/routes/_app/dashboards/$dashboardId.tsx
    - frontend/src/routes/embed/dashboards/$dashboardId.tsx
    - frontend/src/lib/dashboard-url-state.ts
    - frontend/src/components/dashboard/dashboard-renderer.tsx
  </read_first>
  <action>
Use Playwright MCP to verify the complete dashboard lifecycle (D-27, D-28) and embed route (D-24, D-25, D-26):

**Dashboard CRUD lifecycle (D-27, D-28, DASH-02, DASH-04):**
1. Navigate to `/dashboards` -- verify list page loads with motion cards (border-l-primary visible, stagger entrance)
2. Click a seeded dashboard card -- verify detail page loads with:
   - Header showing name, description, metadata row (panel counts + "Updated X ago")
   - Filter bar with SlidersHorizontal header and stagger entrance
   - KPI row with trend-colored border-l accent and spring entrance
   - Chart panels with fade-in animation
   - Real data from Oracle (not empty/error states)
3. Test global filter: select a filter value, click "Apply Filters", verify charts and KPIs update
4. Test cross-filter: click a data point on a chart, verify cross-filter bar appears with chip
5. Test drill-down: double-click a data point, verify breadcrumb appears
6. Navigate back to list, verify grid/list view toggle crossfade works
7. Search for a non-existent dashboard, verify filtered empty state shows "No dashboards matching..."
8. Toggle dark mode, verify all dashboard pages render correctly
9. If possible, create a new dashboard via the builder, add panels, save, view, then delete

**Embed route (D-24, D-25, D-26, DASH-08):**
1. Navigate to `/embed/dashboards/{seeded_dashboard_id}` -- verify renders without sidebar/header
2. Test `?theme=dark` -- verify dark mode applies to embed
3. Test `?filter.{filterId}={value}` -- verify filter is pre-filled
4. Test `?filter.lock={filterId}` -- verify filter shows lock icon and is disabled
5. Test `?hide=filter-bar` -- verify filter bar is hidden
6. Test `?hide=toolbar` -- verify toolbar is hidden
7. Test combination: `?filter.region=EMEA&filter.lock=region&hide=toolbar&theme=dark`

**URL filter round-tripping (D-26, DASH-05):**
1. On the view route `/dashboards/{id}`, apply a filter
2. Verify the URL updates with `?filter.{id}={value}` via replaceState (no history pollution)
3. Refresh the page -- verify the filter is still applied from the URL
4. Reset filters -- verify URL is cleaned up

Any issues found should be fixed immediately. Document any unfixable issues for DASH-09.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard list page loads with motion cards and border-l-primary accent
    - Dashboard detail page loads with real data from Oracle via rewired pipeline
    - Embed route renders correctly with filter, lock, hide, and theme params
    - URL filter round-tripping works (apply -> URL updates -> refresh -> filter persists)
    - Both light and dark mode work across all dashboard pages
  </acceptance_criteria>
  <done>Full dashboard lifecycle verified via Playwright MCP. CRUD operations work against Oracle. Embed route supports all URL parameters. Filter round-tripping works. Both light and dark modes verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 6 dashboard page</name>
  <action>Present the completed Phase 6 dashboard page for human visual verification. All prior plans (pipeline fix, list polish, renderer polish, builder polish, store audit) must be complete before this checkpoint.</action>
  <what-built>Complete Phase 6 dashboard page: pipeline fix (ConfigStore reads recviz_datasets), list page polish (motion cards, crossfade, filtered empty state), renderer premium treatment (filter bar header, KPI trend accents, chart fade-in, accessibility), builder polish (filter chip animation, picker stagger), store audit, embed route verification.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173/dashboards -- verify cards have blue left border, hover lift, stagger entrance
    2. Toggle grid/list view -- verify smooth crossfade transition
    3. Click a dashboard -- verify metadata row shows panel counts and "Updated X ago"
    4. Verify filter bar has SlidersHorizontal icon header
    5. Verify KPI cards have green/red left border based on trend
    6. Verify charts fade in smoothly
    7. Apply a filter, verify charts update
    8. Toggle dark mode, verify everything renders correctly
    9. Open an embed URL with ?theme=dark and hide=toolbar -- verify renders correctly
    10. Search for nonexistent dashboard -- verify Empty component with Search icon appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update USAGE-TRACKER.md</name>
  <files>.planning/USAGE-TRACKER.md</files>
  <read_first>
    - .planning/USAGE-TRACKER.md
  </read_first>
  <action>
Add a Phase 6 section to USAGE-TRACKER.md documenting all files created, modified, and deleted during this phase:

**Files modified (backend):**
- `backend/app/services/config_store.py` [06-01] -- Rewired from recviz_data_sources to recviz_datasets + recviz_connections

**Files modified (frontend):**
- `frontend/src/components/dashboard/dashboard-list.tsx` [06-02] -- AnimatePresence crossfade, filtered empty state
- `frontend/src/components/dashboard/dashboard-list-card.tsx` [06-02] -- motion.div, border-l-primary, stagger
- `frontend/src/components/dashboard/dashboard-list-row.tsx` [06-02] -- motion.div, border-l-primary, stagger
- `frontend/src/routes/_app/dashboards/index.tsx` [06-02] -- Page entrance animation
- `frontend/src/routes/_app/dashboards/$dashboardId.tsx` [06-02] -- Metadata row with panel counts
- `frontend/src/components/dashboard/dashboard-renderer.tsx` [06-03] -- Superset comment removed
- `frontend/src/components/dashboard/config-filter-bar.tsx` [06-03] -- Section header + stagger
- `frontend/src/components/dashboard/config-kpi-row.tsx` [06-03] -- Trend border-l + spring entrance
- `frontend/src/components/dashboard/config-chart-grid.tsx` [06-03] -- Chart fade-in, Superset comment removed
- `frontend/src/components/dashboard/chart-toolbar.tsx` [06-03] -- aria-label accessibility
- `frontend/src/components/dashboard/drill-breadcrumb.tsx` [06-03] -- Entrance animation
- `frontend/src/components/builder/builder-filter-bar.tsx` [06-04] -- AnimatePresence filter chips
- `frontend/src/components/builder/chart-picker-dialog.tsx` [06-04] -- Stagger entrance
- `frontend/src/components/builder/kpi-picker-dialog.tsx` [06-04] -- Stagger entrance
- `frontend/src/components/builder/dataset-picker-dialog.tsx` [06-04] -- Stagger entrance
- `frontend/src/components/builder/builder-canvas.tsx` [06-04] -- Panel hover polish
- `frontend/src/stores/filter-store.ts` [06-04] -- Dead code audit
- `frontend/src/stores/drill-store.ts` [06-04] -- Dead code audit
- `frontend/src/stores/builder-store.ts` [06-04] -- Dead code audit
- `frontend/src/stores/layout-history-store.ts` [06-04] -- Dead code audit

**Files deleted:**
- None (legacy files already deleted prior to Phase 6)

**Dead code candidates for Phase 8:**
- `backend/app/db/models/data_source.py` -- RecvizDataSource ORM model, no longer read by ConfigStore
- `backend/app/services/config_migrator.py` -- No longer imported by ConfigStore
- `recviz_data_sources` table -- Phase 8 Alembic audit drop candidate
- `scripts/seed-oracle.py` -- Still writes recviz_data_sources rows (Phase 8 cleanup)

Use the established tabular format from prior phases.
  </action>
  <verify>
    <automated>grep -c "Phase 6" /Users/aarun/Workspace/Projects/RecViz/.planning/USAGE-TRACKER.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Phase 6" .planning/USAGE-TRACKER.md
    - grep -q "config_store.py" .planning/USAGE-TRACKER.md
    - grep -q "dashboard-list-card" .planning/USAGE-TRACKER.md
    - grep -q "config-kpi-row" .planning/USAGE-TRACKER.md
    - grep -q "Dead code candidates" .planning/USAGE-TRACKER.md (or equivalent section)
  </acceptance_criteria>
  <done>USAGE-TRACKER.md updated with comprehensive Phase 6 file inventory including all modified files, their plan attribution, and dead code candidates for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embed route URL params | filter.*, filter.lock, hide, theme params from untrusted URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | dashboard-url-state.ts | mitigate | parseFilterParams validates key prefix, only processes filter.* keys. parseHideTokens only reads the hide key. parseLockedFilters only reads filter.lock. No raw URL values are injected into DOM or SQL -- they flow through React state and the query pipeline's parameterized SQL. |
| T-06-08 | Information Disclosure | embed route | accept | Embed route renders dashboard data without auth. This matches the existing trust model (no auth this milestone, deferred to v2). Embed URL must be shared intentionally. |
</threat_model>

<verification>
- Full dashboard lifecycle verified via Playwright MCP against Oracle
- Embed route renders with all URL parameter combinations
- URL filter round-tripping works
- USAGE-TRACKER.md has Phase 6 section
- Human verification checkpoint confirms visual quality
</verification>

<success_criteria>
Complete Phase 6 verified end-to-end. Dashboard CRUD works against Oracle. Pipeline fix enables both seeded and user-created dashboards. Embed route fully functional. USAGE-TRACKER updated. Human checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboards-page/06-05-SUMMARY.md`
</output>
