---
phase: 06-dashboards-page
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/components/dashboard/dashboard-renderer.tsx
  - frontend/src/components/dashboard/config-filter-bar.tsx
  - frontend/src/components/dashboard/config-kpi-row.tsx
  - frontend/src/components/dashboard/config-chart-grid.tsx
  - frontend/src/components/dashboard/chart-toolbar.tsx
  - frontend/src/components/dashboard/cross-filter-bar.tsx
  - frontend/src/components/dashboard/drill-breadcrumb.tsx
  - frontend/src/components/dashboard/dashboard-toolbar.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-09
must_haves:
  truths:
    - "Filter bar has section header icon and stagger entrance on controls"
    - "KPI cards have trend-colored border-l-2 accent and spring entrance animation"
    - "Chart grid panels have fade-in entrance animation"
    - "Chart toolbar buttons have aria-label attributes for accessibility"
    - "Dashboard renderer Superset comment is removed"
    - "Cross-filter bar polish verified (already has motion)"
    - "Drill breadcrumb has entrance animation"
  artifacts:
    - path: "frontend/src/components/dashboard/config-filter-bar.tsx"
      provides: "Section header icon + stagger entrance"
      contains: "SlidersHorizontal"
    - path: "frontend/src/components/dashboard/config-kpi-row.tsx"
      provides: "Trend border-l-2 + spring entrance"
      contains: "motion.div"
    - path: "frontend/src/components/dashboard/config-chart-grid.tsx"
      provides: "Chart card fade-in"
      contains: "motion.div"
    - path: "frontend/src/components/dashboard/chart-toolbar.tsx"
      provides: "aria-label on icon buttons"
      contains: "aria-label"
    - path: "frontend/src/components/dashboard/drill-breadcrumb.tsx"
      provides: "Breadcrumb entrance animation"
      contains: "motion"
  key_links:
    - from: "frontend/src/components/dashboard/dashboard-renderer.tsx"
      to: "frontend/src/components/dashboard/config-filter-bar.tsx"
      via: "ConfigFilterBar component"
      pattern: "ConfigFilterBar"
    - from: "frontend/src/components/dashboard/dashboard-renderer.tsx"
      to: "frontend/src/components/dashboard/config-kpi-row.tsx"
      via: "ConfigKpiRow component"
      pattern: "ConfigKpiRow"
---

<objective>
Apply premium polish to the dashboard renderer sub-components: filter bar section header + stagger, KPI trend accent + entrance, chart grid fade-in, toolbar accessibility, cross-filter verification, drill breadcrumb animation, and Superset comment removal.

Purpose: The renderer is where users spend most of their time viewing dashboards. Each section should cascade in with purposeful timing, creating a single orchestrated reveal rather than a collection of independent loads.

Output: All renderer sections have motion entrance animations, KPIs have trend-colored accents, chart toolbar is accessible, Superset references removed.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboards-page/06-CONTEXT.md
@.planning/phases/06-dashboards-page/06-UI-SPEC.md

<interfaces>
<!-- Renderer component structure -->

DashboardRenderer renders in order:
1. DashboardToolbar (refresh, auto-refresh)
2. ConfigFilterBar (global filter dropdowns)
3. CrossFilterBar (cross-filter chips)
4. ConfigKpiRow (KPI animated counter cards)
5. ConfigChartGrid (chart panels with data fetching)
6. ConfigDataGrid (data grid panels)

KPI trend color pattern (already in config-kpi-row.tsx):
```tsx
percentage >= 0
  ? 'bg-green-500/10 text-green-600 dark:text-green-400'
  : 'bg-red-500/10 text-red-600 dark:text-red-400'
```

Cross-filter bar (cross-filter-bar.tsx, ~82 lines):
- Already has motion.div with height/opacity animation on container
- Already has motion.div with scale/opacity + layout prop on chips
- Verify and polish only, do NOT rewrite

Drill breadcrumb (drill-breadcrumb.tsx, ~72 lines):
- Currently renders breadcrumb items without animation
- Add motion entrance

Chart toolbar (chart-toolbar.tsx, ~123 lines):
- Has icon-only buttons (Maximize2, Download, RefreshCw, MoreVertical)
- Needs aria-label on each
- Already has AnimatePresence for hover show/hide

Dashboard toolbar (dashboard-toolbar.tsx, ~61 lines):
- RefreshCw spin already implemented via animate-spin
- AutoRefreshControl already implemented
- ShareLinkButton already implemented
- Verify only, no changes expected
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter bar section header + KPI trend accent + chart grid fade-in</name>
  <files>
    frontend/src/components/dashboard/config-filter-bar.tsx,
    frontend/src/components/dashboard/config-kpi-row.tsx,
    frontend/src/components/dashboard/config-chart-grid.tsx
  </files>
  <read_first>
    - frontend/src/components/dashboard/config-filter-bar.tsx
    - frontend/src/components/dashboard/config-kpi-row.tsx
    - frontend/src/components/dashboard/config-chart-grid.tsx
    - frontend/src/components/dashboard/dashboard-renderer.tsx
  </read_first>
  <action>
**config-filter-bar.tsx (D-11):**
1. Import `motion` from `motion/react` and `SlidersHorizontal` from `lucide-react`
2. Add a section header above the filter Card: `<div className="flex items-center gap-2 mb-2"><SlidersHorizontal size={16} className="text-primary/60" /><span className="text-sm font-semibold">Filters</span></div>`
3. Wrap each individual filter control (the Select/multi-select elements in the grid) with `motion.div` stagger entrance: `initial={{ opacity: 0, y: 4 }}`, `animate={{ opacity: 1, y: 0 }}`, `transition={{ delay: i * 0.03, duration: 0.2 }}`. The `i` comes from the `.map()` index of `filters` array.

**config-kpi-row.tsx (D-12):**
1. Import `motion` from `motion/react`
2. Replace each KPI card's outer `<div>` with `<motion.div>` with spring entrance: `initial={{ opacity: 0, scale: 0.95 }}`, `animate={{ opacity: 1, scale: 1 }}`, `transition={{ type: 'spring', stiffness: 300, damping: 24, delay: i * 0.05 }}` where `i` is the map index
3. Add `border-l-2` accent to each KPI card colored by trend direction:
   - If `hasTrend && percentage >= 0`: `border-l-green-500`
   - If `hasTrend && percentage < 0`: `border-l-red-500`
   - If no trend: `border-l-muted`
   Use `cn()` to conditionally apply the right border class
4. Verify existing trend badge colors use explicit `dark:` variants (they already do: `text-green-600 dark:text-green-400` / `text-red-600 dark:text-red-400`). No changes needed there.

**config-chart-grid.tsx (D-13):**
1. Import `motion` from `motion/react`
2. Wrap each chart card in the grid with `motion.div`: `initial={{ opacity: 0 }}`, `animate={{ opacity: 1 }}`, `transition={{ duration: 0.3, delay: 0.1 }}` (fade in from skeleton state)
3. The chart card wrapper already has a `group` class for hover effects. Apply motion.div as an outer wrapper around the existing card div, or convert the card div itself to motion.div
4. Remove the Superset comment on line 58: change `datasourceId: 0, // config-driven charts don't use Superset datasource IDs` to `datasourceId: 0, // unused -- config-driven charts resolve via datasetId`
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SlidersHorizontal" frontend/src/components/dashboard/config-filter-bar.tsx
    - grep -q "motion.div" frontend/src/components/dashboard/config-filter-bar.tsx
    - grep -q "motion.div" frontend/src/components/dashboard/config-kpi-row.tsx
    - grep -q "border-l-green-500" frontend/src/components/dashboard/config-kpi-row.tsx
    - grep -q "border-l-red-500" frontend/src/components/dashboard/config-kpi-row.tsx
    - grep -q "border-l-muted" frontend/src/components/dashboard/config-kpi-row.tsx
    - grep -q "motion.div" frontend/src/components/dashboard/config-chart-grid.tsx
    - grep -qv "Superset" frontend/src/components/dashboard/config-chart-grid.tsx
  </acceptance_criteria>
  <done>Filter bar has section header with SlidersHorizontal icon and stagger entrance on controls. KPI cards have trend-colored border-l-2 accent and spring entrance animation. Chart grid panels have fade-in entrance. Superset comment removed from config-chart-grid.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Chart toolbar accessibility + drill breadcrumb motion + renderer cleanup</name>
  <files>
    frontend/src/components/dashboard/chart-toolbar.tsx,
    frontend/src/components/dashboard/drill-breadcrumb.tsx,
    frontend/src/components/dashboard/dashboard-renderer.tsx,
    frontend/src/components/dashboard/dashboard-toolbar.tsx,
    frontend/src/components/dashboard/cross-filter-bar.tsx
  </files>
  <read_first>
    - frontend/src/components/dashboard/chart-toolbar.tsx
    - frontend/src/components/dashboard/drill-breadcrumb.tsx
    - frontend/src/components/dashboard/dashboard-renderer.tsx
    - frontend/src/components/dashboard/dashboard-toolbar.tsx
    - frontend/src/components/dashboard/cross-filter-bar.tsx
  </read_first>
  <action>
**chart-toolbar.tsx (D-13 addendum):**
1. Add `aria-label` to each icon-only Button in the toolbar per UI-SPEC accessibility table:
   - Expand chart button (Maximize2): `aria-label="Expand chart"`
   - Download button (Download): `aria-label="Download chart as image"`
   - Refresh button (RefreshCw): `aria-label="Refresh chart data"`
   - Options button (MoreVertical): `aria-label="Chart options"`
2. Verify `bg-background/80 backdrop-blur-sm` styling works in both light and dark mode (it should -- opacity-based)

**drill-breadcrumb.tsx (D-16):**
1. Import `motion` from `motion/react`
2. Wrap the breadcrumb container in `motion.div` with entrance animation: `initial={{ opacity: 0, x: -8 }}`, `animate={{ opacity: 1, x: 0 }}`, `transition={{ duration: 0.2 }}`
3. Add `layout` prop to individual breadcrumb list items (the `<li>` elements) for smooth layout animation when drill levels change: wrap each `<li>` content in `motion.span` with `layout` prop

**dashboard-renderer.tsx (D-23):**
1. Remove the Superset reference in the comment on line 64: change `// No staggering needed -- TQ scheduling + HTTP/2 multiplexing + Superset cache handle concurrency.` to `// No staggering needed -- TQ scheduling + HTTP/2 multiplexing handle concurrency.`

**dashboard-toolbar.tsx (D-14):**
1. Read and verify: refresh spin animation (`animate-spin` on RefreshCw), auto-refresh countdown, share button toast are all already implemented. No changes expected. If any Superset references exist, remove them.

**cross-filter-bar.tsx (D-15):**
1. Read and verify: container has `motion.div` with height/opacity, chips have `motion.div` with scale/opacity + `layout` prop, chip styling uses `Badge variant="secondary"`. Verify consistency with Phase 2-4 badge patterns. No rewrite -- polish only if needed (e.g., if badge classes don't match).
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'aria-label' frontend/src/components/dashboard/chart-toolbar.tsx
    - grep -c 'aria-label' frontend/src/components/dashboard/chart-toolbar.tsx | grep -q '[3-9]' (at least 3 aria-labels)
    - grep -q "motion" frontend/src/components/dashboard/drill-breadcrumb.tsx
    - grep -qv "Superset" frontend/src/components/dashboard/dashboard-renderer.tsx
  </acceptance_criteria>
  <done>Chart toolbar has aria-label on all icon buttons. Drill breadcrumb has entrance animation. Superset comment removed from dashboard-renderer.tsx. Dashboard toolbar and cross-filter bar verified as already polished. All renderer sections have appropriate motion and accessibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure frontend UI polish and accessibility, no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | motion animations | accept | Animations are lightweight (opacity/transform only, GPU-accelerated). No performance concern with dashboard-level item counts (typically <20 KPIs, <30 charts). |
</threat_model>

<verification>
- Filter bar shows SlidersHorizontal icon and "Filters" label
- Filter controls stagger in on dashboard load
- KPI cards have colored left border (green for positive, red for negative, muted for no trend)
- KPI cards spring in with scale animation
- Chart panels fade in smoothly
- Chart toolbar buttons are accessible (check with browser dev tools)
- Drill breadcrumb animates in when activated
- Cross-filter bar still works correctly with existing motion
- Dashboard toolbar refresh spin works
- Both light and dark mode work correctly
</verification>

<success_criteria>
All dashboard renderer sections have premium motion treatment. KPI cards visually indicate trend direction via border color. Chart toolbar is accessible. All Superset references removed from dashboard code.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboards-page/06-03-SUMMARY.md`
</output>
