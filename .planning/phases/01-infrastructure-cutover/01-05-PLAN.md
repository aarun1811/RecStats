---
phase: 01-infrastructure-cutover
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker/init-db.sql
  - scripts/setup-superset-local.sh
  - scripts/generate-seed-db.py
  - scripts/mock-audit.sh
  - scripts/seed-postgres.py
  - scripts/seed-oracle.py
  - docs/
  - deployment/
  - CLAUDE.md
autonomous: true
requirements: [INFRA-17, INFRA-24, INFRA-25]
must_haves:
  truths:
    - "docker-compose.yml, docker/, deployment/, docs/ are deleted"
    - "PG/Superset setup scripts are deleted"
    - "seed-oracle.py exists as Oracle rewrite of seed-postgres.py with rich enough data for Phase 2+"
    - "Repo-wide grep for postgresql/JSONB/asyncpg/psycopg2/superset/redis/celery shows zero hits outside .git/"
    - "CLAUDE.md has zero references to postgresql/asyncpg/psycopg2/superset/docker/redis/celery"
  artifacts:
    - path: "scripts/seed-oracle.py"
      provides: "Oracle seed script with meaningful data for Phase 2+ verification"
      contains: "oracledb"
    - path: "CLAUDE.md"
      provides: "Verified fresh for milestone"
  key_links: []
---

<objective>
Remove all PostgreSQL/Docker-compose/Superset/Redis residue from the repo, delete stale documentation, rewrite the seed script for Oracle with rich data relationships, and verify CLAUDE.md is clean.

Purpose: Zero residue from the old PG/Superset/Docker era. Every grep for old tech returns zero hits. The seed script provides meaningful data for Phase 2+ page phases. Addresses Gemini MEDIUM concern: seed script includes enough data relationships for subsequent phases to have real test data, not a minimal skeleton.

Output: Files deleted, seed-oracle.py created, CLAUDE.md verified clean.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-cutover/01-CONTEXT.md
@.planning/phases/01-infrastructure-cutover/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete residue files/dirs + rewrite seed script for Oracle with rich data</name>
  <files>docker-compose.yml, docker/init-db.sql, deployment/, docs/, scripts/setup-superset-local.sh, scripts/generate-seed-db.py, scripts/mock-audit.sh, scripts/seed-postgres.py, scripts/seed-oracle.py</files>
  <read_first>
    docker-compose.yml
    scripts/seed-postgres.py
    scripts/setup-superset-local.sh
  </read_first>
  <action>
**Delete files per D-23, D-24, D-28:**

Delete these files and directories:
- `docker-compose.yml` (PG container definition, per D-23)
- `docker/init-db.sql` (PG init script)
- `docker/` directory (should be empty after init-db.sql removal)
- `deployment/` directory (empty dir)
- `docs/` directory (entire — all stale Superset-era files per D-28, INFRA-24): `API.md`, `ARCHITECTURE.md`, `CODEBASE_GUIDE.md`, `CONFIGURATION.md`, `DEPLOYMENT.md`, `DEVELOPMENT.md`, `GETTING_STARTED.md`, `SETUP.md`, `TESTING.md`, and all subdirs (`plans/`, `research/`, `testing/`, `superpowers/`)
- `scripts/setup-superset-local.sh` (per D-24)
- `scripts/generate-seed-db.py` (per D-24)
- `scripts/mock-audit.sh` (per D-24)
- `scripts/seed-postgres.py` (replaced by seed-oracle.py per D-25)

Use `git rm` for tracked files so git history records the deletion cleanly. Use `rm -rf` for directories.

**Create seed-oracle.py** per D-25. Addresses Gemini MEDIUM concern about seed script parity — the script must include ENOUGH data relationships for subsequent page phases (Phase 2-7) to have meaningful test data, NOT a minimal skeleton.

Write `scripts/seed-oracle.py` — a self-contained Oracle seed script using `oracledb` directly (not SQLAlchemy ORM). The script must:

1. Connect via `oracledb` using env vars: `RECVIZ_DB_URL` parsed for host/port/service_name, or use `ORACLE_USER`, `ORACLE_PASSWORD`, `ORACLE_DSN` directly. For simplicity, accept a DSN string: `recviz/recviz_dev@localhost:1521/FREEPDB1`.

2. Use named bind variables (`:name` style, not `%s`).

3. Use `MERGE INTO` instead of `ON CONFLICT` for upsert behavior.

4. Use `SYSTIMESTAMP` for timestamp defaults.

5. Seed the following tables with RICH data for Phase 2+ verification:

   **recviz_connections** (2 connections):
   - `conn-oracle-local`: The Docker Oracle itself (localhost:1521/FREEPDB1, backend="oracle", status="active")
   - `conn-oracle-sample`: A second connection for testing multi-source (localhost:1521/FREEPDB1, backend="oracle", status="active")
   Both with `extra_params` as JSON `{"timeout": 30}`

   **recviz_datasets** (3 datasets — enough to exercise the Datasets page in Phase 3):
   - `ds-sample-dual`: Simple SQL `SELECT 1 AS val, SYSDATE AS dt FROM DUAL`, linked to conn-oracle-local
   - `ds-employee-mock`: SQL `SELECT ROWNUM AS emp_id, 'Employee ' || ROWNUM AS name, ROUND(DBMS_RANDOM.VALUE(40000, 120000), 2) AS salary FROM DUAL CONNECT BY ROWNUM <= 50`, linked to conn-oracle-local
   - `ds-department-summary`: SQL `SELECT 'Engineering' AS dept, 42 AS headcount, 4200000 AS budget FROM DUAL UNION ALL SELECT 'Marketing', 18, 1800000 FROM DUAL UNION ALL SELECT 'Sales', 31, 3100000 FROM DUAL UNION ALL SELECT 'Support', 24, 2400000 FROM DUAL`, linked to conn-oracle-local
   Each with `columns` JSON describing the output columns, `schema_version=1`

   **recviz_charts** (3 charts — enough to exercise the Charts page in Phase 4):
   - `chart-bar-dept`: Bar chart linked to ds-department-summary, config with `{"vizType": "bar", "xField": "dept", "yField": "budget"}`
   - `chart-pie-dept`: Pie chart linked to ds-department-summary, config with `{"vizType": "pie", "angleField": "budget", "colorField": "dept"}`
   - `chart-line-salary`: Line chart linked to ds-employee-mock, config with `{"vizType": "line", "xField": "emp_id", "yField": "salary"}`

   **recviz_kpis** (2 KPIs — enough to exercise the KPIs page in Phase 5):
   - `kpi-total-headcount`: Linked to ds-department-summary, metric_column="headcount", aggregation="sum"
   - `kpi-avg-salary`: Linked to ds-employee-mock, metric_column="salary", aggregation="avg"
   Both with minimal config JSON `{"format": "number", "prefix": ""}`

   **recviz_dashboards** (1 dashboard — enough to exercise the Dashboards page in Phase 6):
   - `dash-overview`: Name "Department Overview", config JSON with layout referencing the 3 charts and 2 KPIs above:
     ```json
     {
       "schemaVersion": 2,
       "layout": [
         {"i": "chart-bar-dept", "x": 0, "y": 0, "w": 6, "h": 4, "type": "chart"},
         {"i": "chart-pie-dept", "x": 6, "y": 0, "w": 6, "h": 4, "type": "chart"},
         {"i": "chart-line-salary", "x": 0, "y": 4, "w": 12, "h": 4, "type": "chart"},
         {"i": "kpi-total-headcount", "x": 0, "y": 8, "w": 3, "h": 2, "type": "kpi"},
         {"i": "kpi-avg-salary", "x": 3, "y": 8, "w": 3, "h": 2, "type": "kpi"}
       ],
       "filters": [],
       "globalFilterConfig": {}
     }
     ```

   **recviz_data_sources** (1 data source — Phase 6 handles the architectural fix, but it needs a row):
   - `dsrc-oracle-local`: Name "Local Oracle", config JSON `{"connectionId": "conn-oracle-local", "database": "FREEPDB1"}`, schema_version=1

6. The `encrypted_password` for connections should use a placeholder that the user will need to encrypt with their Fernet key. Add a comment explaining this: `# NOTE: encrypted_password is a placeholder. Run the encryption utility to generate a real value.`

7. JSON values for BLOB columns should be passed as `json.dumps(obj).encode('utf-8')`.

8. The script should be idempotent (DELETE + INSERT pattern for simplicity — MERGE INTO is more complex and not worth it for a dev seed script).

9. Include a `#!/usr/bin/env python3` shebang and `from __future__ import annotations`.

10. Print progress messages for each table seeded.

11. Keep the script under 300 lines. It is a dev convenience that Phase 8 will delete.
  </action>
  <verify>
    <automated>ls docker-compose.yml docker/init-db.sql deployment/ docs/ scripts/setup-superset-local.sh scripts/generate-seed-db.py scripts/mock-audit.sh scripts/seed-postgres.py 2>&1 | grep -c "No such file"; echo "expect 8+ (all deleted)" && ls scripts/seed-oracle.py 2>/dev/null && echo "seed-oracle.py exists" && grep -c "recviz_datasets\|recviz_charts\|recviz_kpis\|recviz_dashboards" scripts/seed-oracle.py; echo "expect 4+ (all tables seeded)"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml does NOT exist
    - docker/ directory does NOT exist
    - deployment/ directory does NOT exist
    - docs/ directory does NOT exist
    - scripts/setup-superset-local.sh does NOT exist
    - scripts/generate-seed-db.py does NOT exist
    - scripts/mock-audit.sh does NOT exist
    - scripts/seed-postgres.py does NOT exist
    - scripts/seed-oracle.py EXISTS and contains `import oracledb`
    - scripts/seed-oracle.py seeds recviz_connections with at least 2 connections
    - scripts/seed-oracle.py seeds recviz_datasets with at least 3 datasets
    - scripts/seed-oracle.py seeds recviz_charts with at least 3 charts
    - scripts/seed-oracle.py seeds recviz_kpis with at least 2 KPIs
    - scripts/seed-oracle.py seeds recviz_dashboards with at least 1 dashboard referencing charts and KPIs
    - scripts/seed-oracle.py seeds recviz_data_sources with at least 1 data source
    - scripts/seed-oracle.py contains `from __future__ import annotations`
    - scripts/seed-oracle.py does NOT contain `psycopg2` or `postgresql`
  </acceptance_criteria>
  <done>All residue files/dirs deleted, seed-oracle.py created with rich data: 2 connections, 3 datasets, 3 charts, 2 KPIs, 1 dashboard with layout referencing all charts/KPIs, 1 data source (addresses Gemini MEDIUM seed parity concern)</done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide grep audit + CLAUDE.md verification</name>
  <files>CLAUDE.md, various backend/frontend files with stale references</files>
  <read_first>
    CLAUDE.md
  </read_first>
  <action>
**Repo-wide grep audit** per D-29, INFRA-25:

Run the following grep to find all remaining references:
```bash
grep -rn "postgresql\|JSONB\|asyncpg\|psycopg2\|superset\|redis\|celery" --include="*.py" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.md" --include="*.yml" --include="*.yaml" --include="*.ini" --include="*.cfg" --include="*.toml" --include="*.txt" --include="*.css" --include="*.html" --include="*.env*" --include="*.sh" . | grep -v ".git/" | grep -v "node_modules/" | grep -v ".planning/" | grep -v "__pycache__/"
```

For EACH hit found:
- If it's in a **code file** (`.py`, `.ts`, `.tsx`): Remove or rewrite the reference. Common patterns:
  - Comment referencing PostgreSQL -> rewrite to Oracle
  - Docstring mentioning Superset -> remove reference
  - Import or dialect check for postgresql -> remove
  - Type annotation mentioning JSONB -> change to OracleJSON/BLOB
- If it's in a **config file** (`.ini`, `.env`, `.toml`): Remove PG URL or reference
- If it's in **CLAUDE.md**: Should already be clean per the 2026-04-11 refresh, but verify

**CLAUDE.md verification** per INFRA-25:

Grep CLAUDE.md specifically:
```bash
grep -in "postgresql\|asyncpg\|psycopg2\|superset\|docker\|redis\|celery\|tableau\|qlik" CLAUDE.md
```

If any hits are found in CLAUDE.md, fix them:
- `postgresql` -> remove or replace with "Oracle 19c"
- `asyncpg` -> remove entirely
- `psycopg2` -> remove entirely
- `superset` -> remove entirely
- `docker` -> only the mention about "No Docker" is acceptable (it's a rule, not a reference to using Docker). The Docker Oracle container is for the DB, not the app — CLAUDE.md says "No Docker" meaning the app itself is not containerized.
- `redis` -> only "No Redis" rule mention is acceptable
- `celery` -> only "No Celery" rule mention is acceptable
- `tableau`/`qlik` -> remove if present (old framing per PROJECT.md)

The acceptable pattern in CLAUDE.md is the "No Docker/Redis/Celery" rules in the Non-Negotiable Rules section. These state "don't use X" — they are NOT references to using X. The key: no references to using these technologies, only references to NOT using them.

After cleanup, run the grep again and confirm zero actionable hits outside `.git/`, `node_modules/`, and `.planning/`.
  </action>
  <verify>
    <automated>grep -rn "postgresql\|JSONB\|asyncpg\|psycopg2\|superset\|redis\|celery" --include="*.py" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.ini" --include="*.txt" --include="*.sh" . 2>/dev/null | grep -v ".git/" | grep -v "node_modules/" | grep -v ".planning/" | grep -v "__pycache__/" | wc -l; echo "expect 0"</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide grep for `postgresql|JSONB|asyncpg|psycopg2|superset|redis|celery` in code/config files (excluding .git/, node_modules/, .planning/, __pycache__/) returns ZERO hits
    - CLAUDE.md grep for `postgresql|asyncpg|psycopg2|superset|docker|redis|celery|tableau|qlik` returns only acceptable "No X" rule mentions (not references to using those technologies)
    - CLAUDE.md contains "Oracle 19c only" in the Non-Negotiable Rules section
    - CLAUDE.md does NOT contain any URL with `postgresql://` or `asyncpg://`
  </acceptance_criteria>
  <done>Repo-wide grep audit passes with zero hits for old tech references in code/config files, CLAUDE.md verified clean for the milestone</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script -> Oracle | Seed script connects to Oracle with dev credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | I (Info Disclosure) | seed-oracle.py | mitigate | Script uses env vars or command-line DSN for credentials, not hardcoded passwords; placeholder encrypted_password value noted in comments |
| T-01-15 | I (Info Disclosure) | docs/ deletion | accept | Stale docs contained no secrets (verified pre-deletion); removal reduces attack surface of outdated information |
</threat_model>

<verification>
- `ls docker-compose.yml docker/ deployment/ docs/` all return "No such file or directory"
- `ls scripts/seed-oracle.py` exists and contains oracledb
- `scripts/seed-oracle.py` seeds 2+ connections, 3+ datasets, 3+ charts, 2+ KPIs, 1+ dashboard, 1+ data source
- Full grep audit returns zero hits for banned terms in code/config
- CLAUDE.md is clean
</verification>

<success_criteria>
- All residue files/dirs deleted (docker-compose.yml, docker/, deployment/, docs/, PG scripts)
- seed-oracle.py created with rich Oracle-native seeding: 2 connections, 3 datasets, 3 charts, 2 KPIs, 1 dashboard, 1 data source (addresses Gemini MEDIUM seed parity concern)
- Repo-wide grep audit passes: zero hits for postgresql/JSONB/asyncpg/psycopg2/superset/redis/celery in code/config files
- CLAUDE.md verified fresh: zero references to old tech (only "No X" rules are acceptable)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-cutover/01-05-SUMMARY.md`
</output>
