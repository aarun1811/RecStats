---
phase: 04-charts-page
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - scripts/seed-oracle.py
  - .planning/USAGE-TRACKER.md
autonomous: true
requirements: [CHRT-06, CHRT-07, CHRT-08]
must_haves:
  truths:
    - "Seed data chart configs contain no stale hardcoded hex color values"
    - "Console errors related to chart components are triaged and chart-related ones fixed"
    - "USAGE-TRACKER.md is updated with all files created, modified, and removed in Phase 4"
  artifacts:
    - path: ".planning/USAGE-TRACKER.md"
      provides: "Phase 4 file inventory"
      contains: "Phase 4"
  key_links:
    - from: "scripts/seed-oracle.py"
      to: "recviz_charts config column"
      via: "chart config JSON in seed data"
      pattern: "config"
---

<objective>
Audit stored chart config JSON in seed data for stale hex, investigate and fix chart-related console errors, and update USAGE-TRACKER.md with Phase 4 file changes.

Purpose: Closes out the stored config hex audit (CHRT-06), addresses discovered fixes (CHRT-07), and tracks file changes (CHRT-08).
Output: Updated seed data (if hex found), console error fixes, and USAGE-TRACKER.md update.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-charts-page/04-CONTEXT.md
@.planning/phases/04-charts-page/04-01-SUMMARY.md
@.planning/phases/04-charts-page/04-02-SUMMARY.md
@.planning/phases/04-charts-page/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stored config hex audit + console error triage + USAGE-TRACKER</name>
  <files>
    scripts/seed-oracle.py
    .planning/USAGE-TRACKER.md
  </files>
  <read_first>
    scripts/seed-oracle.py
    .planning/USAGE-TRACKER.md
    frontend/src/components/charts/chart-factory.tsx
    frontend/src/components/charts/ag-chart-wrapper.tsx
    frontend/src/components/charts/echart-wrapper.tsx
    frontend/src/components/charts/chart-builder.tsx
    frontend/src/components/charts/chart-library-list.tsx
    frontend/src/components/charts/chart-library-card.tsx
    frontend/src/components/charts/chart-library-row.tsx
    frontend/src/components/charts/chart-detail-panel.tsx
    frontend/src/components/charts/builder/step-appearance.tsx
    frontend/src/components/charts/builder/step-mapping.tsx
    frontend/src/components/charts/chart-builder-help-sheet.tsx
    frontend/src/lib/style-constants.ts
    frontend/src/lib/chart-themes.ts
    frontend/src/index.css
    frontend/src/types/managed-chart.ts
    frontend/src/routes/_app/charts/index.tsx
  </read_first>
  <action>
**A. Stored config hex audit (per D-12, CHRT-06):**

1. Read `scripts/seed-oracle.py` and search for any chart config JSON (look for keys like `config`, `appearance`, `colorRange`, or any `#` followed by 6 hex digits in chart seed data).

2. If hex color values are found in chart seed data configs (e.g., `"colorRange": ["#43A047", "#FF5722"]` or `"color": "#ef4444"`):
   - Replace them with CSS variable references: e.g., `"colorRange": ["--chart-positive", "--chart-negative"]`
   - Or remove the stale color overrides entirely so charts pick up palette defaults from `chart-themes.ts`
   - The preferred approach: remove stale color fields from config JSON and let the theme system apply defaults

3. If NO hex is found in seed data chart configs, document this finding (the stored configs are clean).

4. Also grep the entire `frontend/src/` directory for remaining hardcoded hex in chart-related files. If any residual hex is found beyond what Plan 01 already cleaned, fix it.

**B. Console error triage (per D-17, CHRT-07):**

1. Start the frontend dev server: `cd frontend && pnpm dev`
2. Open the Charts page in the browser (or use the available build tools to identify errors)
3. Since we cannot run a browser directly, instead:
   - Run `cd frontend && npx tsc --noEmit` to catch TypeScript errors
   - Run `cd frontend && npx eslint src/components/charts/ src/routes/_app/charts/ --max-warnings 0` to catch lint warnings (use `--max-warnings 100` if 0 is too strict, then report the count)
   - Grep for common error patterns: `console.error`, `console.warn`, TODO/FIXME comments in chart files

4. Fix any TypeScript errors or ESLint errors found in chart-related files.

5. For warnings that are NOT chart-related (React, TanStack Router, etc.), log them in USAGE-TRACKER.md under a "Known warnings (non-chart)" section but do NOT fix them.

**C. USAGE-TRACKER update (per CHRT-08):**

Update `.planning/USAGE-TRACKER.md` with a new "Phase 4: Charts Page" section listing:

Files created:
- `frontend/src/components/charts/chart-builder-help-sheet.tsx` [Plan 03]
- `.planning/phases/04-charts-page/chart-config-audit.md` [Plan 01]

Files modified:
- `frontend/src/lib/style-constants.ts` [Plan 01] -- added CHART_TYPE_BORDER_COLORS, CHART_TYPE_PILL_BG, CHART_TYPE_PILL_TEXT
- `frontend/src/index.css` [Plan 01] -- added --chart-warning CSS variable
- `frontend/src/lib/chart-themes.ts` [Plan 01] -- exported resolveColor, added --chart-warning fallback
- `frontend/src/components/charts/echart-wrapper.tsx` [Plan 01] -- replaced gauge hex with resolveColor()
- `frontend/src/components/charts/ag-chart-wrapper.tsx` [Plan 01] -- replaced treemap hex with resolveColor()
- `frontend/src/components/charts/chart-library-card.tsx` [Plan 02] -- motion.div, border-l, color pills, ECharts thumbnails
- `frontend/src/components/charts/chart-library-row.tsx` [Plan 02] -- motion.div, border-l, color icon
- `frontend/src/components/charts/chart-library-list.tsx` [Plan 02] -- AnimatePresence crossfade, filtered empty state
- `frontend/src/components/charts/chart-detail-panel.tsx` [Plan 02] -- motion entrance, border accent, icon tints
- `frontend/src/routes/_app/charts/index.tsx` [Plan 02] -- page stagger animation
- `frontend/src/components/charts/chart-builder.tsx` [Plan 03] -- accordion animations, preview crossfade, help sheet
- `frontend/src/components/charts/builder/step-mapping.tsx` [Plan 03] -- inline tooltips
- `frontend/src/components/charts/builder/step-appearance.tsx` [Plan 03] -- chart-type-specific fields
- `frontend/src/types/managed-chart.ts` [Plan 03] -- added typeSpecific to ChartAppearance
- `scripts/seed-oracle.py` [Plan 04] -- hex audit (if changes needed)

Files removed: none

Use the same tabular format as previous phases.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz && grep -rn "#[0-9a-fA-F]\{6\}" scripts/seed-oracle.py 2>/dev/null | grep -i "chart\|config\|color" | head -5 ; grep -c "Phase 4" .planning/USAGE-TRACKER.md ; cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for hex colors in seed-oracle.py chart configs returns 0 hits (either no hex existed or they were removed)
    - USAGE-TRACKER.md contains a "Phase 4" section
    - USAGE-TRACKER.md lists at least 15 files (created + modified)
    - npx tsc --noEmit passes with no errors
    - No residual hardcoded hex in chart component files (echart-wrapper gauge block, ag-chart-wrapper treemap block)
  </acceptance_criteria>
  <done>Seed data chart configs audited for hex (stale overrides removed or confirmed clean). Console errors triaged. USAGE-TRACKER.md updated with complete Phase 4 file inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed data | Chart config JSON in seed script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T (Tampering) | seed-oracle.py chart configs | accept | Seed data is developer-authored and only used in dev environments; no production security impact |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- No stale hex in seed chart configs
- USAGE-TRACKER updated
</verification>

<success_criteria>
- Stored chart config JSON audited for hex leakage (CHRT-06)
- Console errors triaged and chart-related ones fixed (CHRT-07)
- USAGE-TRACKER.md updated with Phase 4 file changes (CHRT-08)
</success_criteria>

<output>
After completion, create `.planning/phases/04-charts-page/04-04-SUMMARY.md`
</output>
