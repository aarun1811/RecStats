---
phase: 05-kpis-page
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .planning/USAGE-TRACKER.md
autonomous: false
requirements:
  - KPI-03
  - KPI-05

must_haves:
  truths:
    - "KPI CRUD verified against Oracle — create, edit, delete all work"
    - "Animated counter renders with real Oracle data"
    - "Threshold colors apply correctly to computed values"
    - "All aggregation types compute correctly (SUM, AVG, COUNT, MIN, MAX, COUNT_DISTINCT)"
    - "USAGE-TRACKER updated with Phase 5 file changes"
  artifacts:
    - path: ".planning/USAGE-TRACKER.md"
      provides: "Phase 5 file audit entries"
      contains: "Phase 05"
  key_links:
    - from: "frontend/src/components/kpis/kpi-builder.tsx"
      to: "/api/managed-kpis"
      via: "useCreateKpi / useUpdateKpi hooks"
      pattern: "mutateAsync"
---

<objective>
Verify KPI CRUD lifecycle and animated counter rendering end-to-end against live Oracle via Playwright MCP, validate threshold colors and all aggregation types compute correctly, then update USAGE-TRACKER with Phase 5 file changes.

Purpose: Delivers KPI-03 (CRUD + counter verified) and KPI-05 (USAGE-TRACKER update).
Output: Verified working KPI page against Oracle + updated tracker.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-kpis-page/05-CONTEXT.md
@.planning/phases/05-kpis-page/05-UI-SPEC.md
@.planning/phases/05-kpis-page/05-01-SUMMARY.md
@.planning/phases/05-kpis-page/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Playwright MCP verification of KPI page end-to-end</name>
  <files>frontend/src/components/kpis/kpi-library-list.tsx</files>
  <read_first>
    - .planning/phases/05-kpis-page/05-CONTEXT.md (D-14, D-15 verification requirements)
    - .planning/phases/05-kpis-page/05-UI-SPEC.md (interaction contract + copywriting)
    - frontend/src/components/kpis/kpi-library-list.tsx
    - frontend/src/components/kpis/kpi-builder.tsx
    - frontend/src/components/kpis/kpi-detail-panel.tsx
  </read_first>
  <action>
Use Playwright MCP to perform comprehensive visual and functional verification of the KPI page. Ensure both frontend (pnpm dev on port 5173) and backend (uvicorn on port 8000) are running before verification.

**KPI List Page (light + dark mode):**
1. Navigate to http://localhost:5173/kpis
2. Screenshot the list page — verify cards have colored border-l-2 accents (emerald for SUM, blue for AVG, violet for COUNT, amber for MIN/MAX, teal for COUNT_DISTINCT)
3. Verify cards show colored aggregation type pill (not plain text)
4. Toggle to list view — verify rows have same border-l-2 accent and upgraded icon container (bg-primary/5 border border-primary/10)
5. Toggle back to grid — verify AnimatePresence crossfade occurs (no jarring switch)
6. Type a non-matching search — verify "No KPIs found" Empty component shows with Search icon
7. Toggle dark mode — verify all accents and colors work in dark

**KPI Detail Panel:**
8. Click any KPI card — verify Sheet slides in with threshold-colored border on left edge
9. Verify section header icons have primary tint (text-primary/60)
10. Verify animated counter shows with threshold color (green/amber/red/plain depending on value)
11. Verify delete button has aria-label="Delete KPI" and title tooltip on hover

**KPI Builder (CRUD cycle per D-14):**
12. Click "New KPI" — navigate to builder
13. Select a dataset (step 1) — verify preview shows column metadata
14. Select metric column + aggregation (step 2) — verify live counter appears with entrance animation
15. Configure format (step 3) — verify counter format updates with crossfade
16. Set trend comparison (step 4) — continue
17. Configure thresholds (step 5) — verify threshold color applies to counter value
18. Name the KPI and click "Save KPI" — verify toast "KPI saved" and redirect to list
19. Verify new KPI appears in list with correct accent and value
20. Click it to open detail panel — verify all metadata correct
21. Click "Edit KPI" — verify builder reopens with all steps pre-filled
22. Change aggregation to each type (SUM, AVG, COUNT, MIN, MAX, COUNT_DISTINCT) and verify value updates correctly
23. Save changes — verify toast and redirect
24. Delete the KPI — verify toast "KPI deleted" and removal from list

**Dark mode verification:**
25. Toggle dark mode and repeat key visual checks (card accents, threshold colors, builder preview)

Fix any issues found during verification — common issues may include motion import errors, missing type props, or CSS class conflicts. If fixes are needed, commit them before marking this task complete.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>All 25 verification steps pass visually via Playwright MCP: cards show aggregation accents, detail panel has threshold border, builder has step motion + checkmark spring, CRUD lifecycle works end-to-end against Oracle, both light and dark mode correct. User approves.</done>
</task>

<task type="auto">
  <name>Task 2: Update USAGE-TRACKER with Phase 5 file changes</name>
  <files>.planning/USAGE-TRACKER.md</files>
  <read_first>
    - .planning/USAGE-TRACKER.md
    - .planning/phases/05-kpis-page/05-01-SUMMARY.md
    - .planning/phases/05-kpis-page/05-02-SUMMARY.md
  </read_first>
  <action>
Append a Phase 05 section to `.planning/USAGE-TRACKER.md` in the established tabular format. Include all files touched or created in Phase 5:

```markdown
## Phase 05 — KPIs Page

| File | Action | Plan | Notes |
|------|--------|------|-------|
| frontend/src/lib/style-constants.ts | modified | 05-01 | Added KPI_AGG_BORDER_COLORS, KPI_AGG_PILL_BG, KPI_AGG_PILL_TEXT, THRESHOLD_BORDER_COLORS |
| frontend/src/components/kpis/kpi-library-list.tsx | modified | 05-01 | Added AnimatePresence view crossfade, upgraded filtered empty state |
| frontend/src/components/kpis/kpi-library-card.tsx | modified | 05-01 | Added motion.div wrapper, border-l-2 aggregation accent, colored pill |
| frontend/src/components/kpis/kpi-library-row.tsx | modified | 05-01 | Added motion.div wrapper, border-l-2 accent, upgraded icon container |
| frontend/src/components/kpis/kpi-detail-panel.tsx | modified | 05-02 | Added threshold border accent, entrance animation, section icon tint |
| frontend/src/components/kpis/kpi-builder.tsx | modified | 05-02 | Added accordion step motion, checkmark spring animation |
| frontend/src/components/kpis/kpi-builder-preview.tsx | modified | 05-02 | Added AnimatePresence crossfade, entrance spring animation |
```

If any additional files were touched during execution (bug fixes found during verification), include those as well.
  </action>
  <verify>
    <automated>grep -c "Phase 05" /Users/aarun/Workspace/Projects/RecViz/.planning/USAGE-TRACKER.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Phase 05" .planning/USAGE-TRACKER.md` returns at least 1 match
    - `grep "kpi-library-card.tsx" .planning/USAGE-TRACKER.md` returns at least 1 match
    - `grep "kpi-builder.tsx" .planning/USAGE-TRACKER.md` returns at least 1 match
    - `grep "style-constants.ts" .planning/USAGE-TRACKER.md` returns at least 1 match (Phase 05 entry)
    - The section uses the tabular format with File | Action | Plan | Notes columns
  </acceptance_criteria>
  <done>USAGE-TRACKER.md has Phase 05 section listing all files modified in this phase with plan attribution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | KPI CRUD mutations send user-supplied name/description to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | S (Spoofing) | KPI CRUD API | accept | No auth this milestone (deferred to v2 AUTH-01..04) |
| T-05-04 | I (Info Disclosure) | SQL execute endpoint | accept | Internal tool only, no external users; Oracle data accessible to all internal users by design |
</threat_model>

<verification>
1. Playwright MCP screenshots confirm visual polish in both light and dark mode
2. Full KPI CRUD lifecycle works: create, view, edit, delete
3. All 6 aggregation types compute correctly
4. Threshold colors apply to counter values
5. `.planning/USAGE-TRACKER.md` contains Phase 05 section
</verification>

<success_criteria>
- KPI CRUD works end-to-end against Oracle (create, read, edit, delete)
- Animated counter displays correctly with real Oracle data
- All aggregation types (SUM, AVG, COUNT, MIN, MAX, COUNT_DISTINCT) produce correct values
- Threshold colors (green/amber/red) apply correctly based on configured thresholds
- Both light and dark mode render correctly
- USAGE-TRACKER updated with Phase 5 file changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-kpis-page/05-03-SUMMARY.md`
</output>
