---
phase: 04-data-source-connectivity
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-04

must_haves:
  truths:
    - "Docker image builds successfully with all drivers installed"
    - "Data sources UI shows dynamic form fields per backend type"
    - "Test Connection works for PostgreSQL databases in local dev"
    - "Status dots update correctly after test connection"
    - "Create mode enforces test-before-save"
---

<objective>
Verify the complete data source connectivity flow end-to-end: Docker build with drivers, data sources UI with dynamic forms, test connection, and status tracking.

Purpose: Confirms all Phase 4 deliverables work together before marking the phase complete.

Output: Verified working system, no artifacts created.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-data-source-connectivity/04-01-SUMMARY.md
@.planning/phases/04-data-source-connectivity/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Docker image and run all tests</name>
  <files></files>
  <read_first>
    superset/Dockerfile
    superset/superset_config.py
  </read_first>
  <action>
1. Build the Superset Docker image to verify all driver installations succeed:
```bash
cd /Users/aarun/Workspace/Projects/RecViz && docker build -t recviz-superset-test -f superset/Dockerfile superset/
```
This verifies: oracledb, pyhive, and thrift install without errors alongside existing packages.

2. Verify the cx_Oracle alias is correctly positioned in superset_config.py by checking the file starts with the alias block.

3. Run all backend tests:
```bash
cd /Users/aarun/Workspace/Projects/RecViz/backend && python -m pytest tests/ -x -v
```

4. Run frontend TypeScript compilation:
```bash
cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit
```

5. Run frontend tests if any exist for the settings components:
```bash
cd /Users/aarun/Workspace/Projects/RecViz/frontend && pnpm vitest run --reporter=verbose 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/backend && python -m pytest tests/ -x -q && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Docker build completes without errors (exit code 0)
    - All backend tests pass (pytest exits 0)
    - Frontend TypeScript compilation passes (tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Docker image builds with all drivers, all tests pass, TypeScript compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of data sources UI</name>
  <files></files>
  <action>
Present the verification checklist to the user. All implementation was completed in Plans 01 and 02 -- this task is pure visual verification.

What was built:
- Dynamic backend-specific form fields (Oracle/Hive/PostgreSQL)
- Test-before-save enforcement on create
- Colored status dots on data source cards and rows
- Connection status tracking with last tested timestamp
- Elasticsearch backend disabled (deferred)
  </action>
  <verify>User confirms all verification steps pass by typing "approved"</verify>
  <done>User has visually verified the data sources UI works correctly in both light and dark mode</done>
  <how-to-verify>
    1. Start the dev stack: `docker compose up -d` then start the backend and frontend
    2. Navigate to Settings > Data Sources
    3. **Verify status dots**: Existing data sources should show gray dots (untested since restart)
    4. **Verify create flow**:
       a. Click "Add Data Source"
       b. Select "Oracle" -- verify fields: Host, Port, Service Name, Schema, Username, Password
       c. Select "Hive" -- verify fields change to: Host, Port, Database, Username, Password
       d. Select "PostgreSQL" -- verify fields: Host, Port, Database, Username, Password
       e. Select "Elasticsearch" -- verify it's disabled/grayed out
       f. Fill in a PostgreSQL connection (use existing dev DB credentials)
       g. Verify Save button is DISABLED before testing
       h. Click "Test Connection" -- verify success message
       i. Verify Save button is now ENABLED
       j. Save the connection
    5. **Verify detail view**: Click on a data source, verify status dot + last tested timestamp
    6. **Verify edit mode**: Click Edit, verify password field is empty with placeholder
    7. **Verify dark mode**: Toggle dark mode, verify status dots and form render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this verification plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | This plan only verifies existing mitigations from plans 01 and 02. No new threats. |
</threat_model>

<verification>
1. Docker build succeeds
2. Backend tests pass
3. Frontend compiles
4. Human verifies UI visually
</verification>

<success_criteria>
- Docker image builds with oracledb, pyhive, thrift
- All tests pass
- User approves visual verification of data sources UI
- Status dots, dynamic forms, and test-before-save all working
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-source-connectivity/04-03-SUMMARY.md`
</output>
