---
phase: 15-superset-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/superset_client.py        # DELETE
  - backend/app/services/database_registrar.py      # DELETE
  - backend/app/models/database_config.py           # DELETE
  - backend/app/config/databases.json               # DELETE
  - backend/app/config/databases.prod.json          # DELETE
  - backend/app/api/datasets.py                     # DELETE
  - backend/tests/test_database_registrar.py        # DELETE
  - backend/tests/test_dataset_sync.py              # DELETE
  - superset/                                       # DELETE entire directory
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/core/dependencies.py
  - backend/app/api/router.py
  - backend/app/models/error.py
  - backend/requirements.txt
  - backend/.env.example
  - docker-compose.yml
  - backend/tests/test_managed_kpis.py
  - backend/tests/test_managed_charts.py
  - backend/tests/test_search.py
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04
  - INFR-05

must_haves:
  truths:
    - "No file in the repo imports superset_client, database_registrar, or httpx"
    - "No file in the repo references redis_url or REDIS_URL"
    - "docker-compose.yml contains only the postgres service and pgdata volume"
    - "requirements.txt contains no httpx, redis, or requests lines"
    - "The superset/ directory does not exist"
    - "The application starts successfully with only FastAPI + PostgreSQL"
  artifacts:
    - path: "backend/app/main.py"
      provides: "Clean lifespan with no Superset/httpx/registrar references"
      contains: "QueryExecutor"
    - path: "backend/app/config.py"
      provides: "Settings with only recviz_db_url, recon_db_url, recviz_encryption_key"
    - path: "backend/app/core/dependencies.py"
      provides: "Dependencies with no SupersetDep or SupersetClient"
    - path: "docker-compose.yml"
      provides: "PostgreSQL-only Docker Compose"
    - path: "backend/requirements.txt"
      provides: "Clean dependency list without httpx, redis, requests"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/services/engine_manager.py"
      via: "EngineManager in lifespan"
      pattern: "EngineManager"
    - from: "backend/app/core/dependencies.py"
      to: "backend/app/services/query_engine.py"
      via: "QueryEngine type alias"
      pattern: "QueryEngine"
---

<objective>
Delete all Superset code, remove Redis and httpx dependencies, simplify Docker Compose to PostgreSQL-only, and clean all import references across the codebase.

Purpose: Complete the infrastructure cleanup so RecViz runs with only FastAPI + database (no Superset process, no Redis process, no httpx HTTP proxy).

Output: A codebase with zero Superset/Redis/httpx references, a single-service Docker Compose, and a clean startup path.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-superset-removal/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all Superset files and the datasets proxy router (INFR-01)</name>
  <files>
    backend/app/services/superset_client.py
    backend/app/services/database_registrar.py
    backend/app/models/database_config.py
    backend/app/config/databases.json
    backend/app/config/databases.prod.json
    backend/app/api/datasets.py
    backend/tests/test_database_registrar.py
    backend/tests/test_dataset_sync.py
    superset/
  </files>
  <read_first>
    - backend/app/services/superset_client.py (confirm it is dead code with no live callers outside main.py)
    - backend/app/services/database_registrar.py (confirm no live callers outside main.py)
    - backend/app/api/datasets.py (confirm this is the Superset proxy, not managed_datasets.py)
    - backend/app/api/router.py (confirm datasets_router import line to remove)
  </read_first>
  <action>
    Delete these files entirely (per INFR-01):
    1. `rm backend/app/services/superset_client.py`
    2. `rm backend/app/services/database_registrar.py`
    3. `rm backend/app/models/database_config.py`
    4. `rm backend/app/config/databases.json`
    5. `rm backend/app/config/databases.prod.json`
    6. `rm backend/app/api/datasets.py` -- this is the Superset proxy router. The managed_datasets.py router (RecViz-native CRUD) stays.
    7. `rm backend/tests/test_database_registrar.py`
    8. `rm backend/tests/test_dataset_sync.py`
    9. `rm -rf superset/` -- entire directory (Dockerfile, superset_config.py, superset_config_local.py, entrypoint script, __pycache__)

    Then remove the datasets router from `backend/app/api/router.py`:
    - Delete the line: `from app.api.datasets import router as datasets_router`
    - Delete the line: `api_router.include_router(datasets_router)`

    Do NOT delete:
    - `backend/app/api/managed_datasets.py` (RecViz-native dataset CRUD -- stays)
    - `backend/app/services/query_engine.py` (already rewritten to direct execution -- stays)
    - `backend/app/services/engine_manager.py` (new direct engine -- stays)
  </action>
  <verify>
    <automated>bash -c "test ! -f backend/app/services/superset_client.py && test ! -f backend/app/services/database_registrar.py && test ! -f backend/app/models/database_config.py && test ! -f backend/app/config/databases.json && test ! -f backend/app/config/databases.prod.json && test ! -f backend/app/api/datasets.py && test ! -f backend/tests/test_database_registrar.py && test ! -f backend/tests/test_dataset_sync.py && test ! -d superset && ! grep -r 'datasets_router' backend/app/api/router.py && echo 'PASS: all Superset files deleted' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 file/directory targets deleted
    - datasets_router removed from router.py
    - managed_datasets_router still present in router.py
  </acceptance_criteria>
  <done>Every file listed under INFR-01 is deleted. The datasets Superset proxy router is removed from the API router. Only RecViz-native code remains.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Redis, httpx, and requests dependencies; simplify Docker Compose (INFR-02, INFR-03, INFR-04)</name>
  <files>
    backend/requirements.txt
    docker-compose.yml
    backend/.env.example
  </files>
  <read_first>
    - backend/requirements.txt (confirm current httpx, redis, requests lines)
    - docker-compose.yml (confirm redis and superset service blocks)
    - backend/.env.example (confirm SUPERSET_* and REDIS_* lines)
  </read_first>
  <action>
    1. Edit `backend/requirements.txt`:
       - Remove the line `httpx==0.28.1`
       - Remove the line `redis==4.6.0`
       - Remove the line `requests==2.32.5`
       - Remove the comment block referencing Superset ("# Superset runs in Docker..." through "# Pinned version reference...")
       - Update the section comment from "# Database / Cache" to "# Database" (no more cache)

    2. Edit `docker-compose.yml` to contain ONLY PostgreSQL:
       - Remove the entire `redis:` service block (lines 20-29)
       - Remove the entire `superset:` service block (lines 31-52)
       - Remove `depends_on` from postgres (it has none, but verify)
       - The result should be: services.postgres + volumes.pgdata only

    3. Edit `backend/.env.example`:
       - Remove the SUPERSET_URL, SUPERSET_USERNAME, SUPERSET_PASSWORD lines and their comment
       - Remove the REDIS_URL line and its comment
       - Update comments to reflect the new state (no "will be removed in Phase 15" language)
  </action>
  <verify>
    <automated>bash -c "! grep -E 'httpx|^redis|^requests' backend/requirements.txt && ! grep -E 'redis:|superset:' docker-compose.yml && ! grep -iE 'SUPERSET_|REDIS_' backend/.env.example && echo 'PASS: dependencies and Docker cleaned' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt has no httpx, redis, or requests lines
    - docker-compose.yml has only the postgres service and pgdata volume
    - .env.example has no SUPERSET_* or REDIS_* variables
  </acceptance_criteria>
  <done>All three dependency targets (httpx, redis, requests) removed from requirements. Docker Compose runs PostgreSQL only. Environment example reflects the simplified stack.</done>
</task>

<task type="auto">
  <name>Task 3: Clean main.py lifespan, config.py, dependencies.py, error model, health endpoint, and test fixtures (INFR-05)</name>
  <files>
    backend/app/main.py
    backend/app/config.py
    backend/app/core/dependencies.py
    backend/app/models/error.py
    backend/tests/test_managed_kpis.py
    backend/tests/test_managed_charts.py
    backend/tests/test_search.py
  </files>
  <read_first>
    - backend/app/main.py (full file -- understand lifespan flow to preserve)
    - backend/app/config.py (full file -- identify settings to remove)
    - backend/app/core/dependencies.py (full file -- identify SupersetDep to remove)
    - backend/app/models/error.py (line 16 -- update "superset_unavailable" in docstring)
    - backend/tests/test_managed_kpis.py (lines ~245-252 and ~308-315 -- superset_id fixture lines)
    - backend/tests/test_managed_charts.py (lines ~243-250 and ~303-310 -- superset_id fixture lines)
    - backend/tests/test_search.py (lines ~175-200 -- guard test references)
  </read_first>
  <action>
    1. **backend/app/config.py** -- Remove Superset and Redis settings:
       - Delete `superset_url`, `superset_username`, `superset_password`, `redis_url` fields
       - Delete `databases_config_path` field (databases.json no longer exists)
       - Keep: `recon_db_url`, `recviz_db_url`, `recviz_encryption_key`, `model_config`

    2. **backend/app/main.py** -- Gut the Superset lifespan, keep direct engine path:
       - Remove `import httpx`
       - Remove `from app.services.database_registrar import DatabaseRegistrar`
       - Remove `from app.services.superset_client import SupersetClient`
       - Remove `from app.services.query_engine import ... QueryEngine` (keep only QueryExecutor import)
       - Remove `import json` (only used by _migrate_json_connections)
       - Remove `from urllib.parse import parse_qs, urlparse` (only used by _migrate_json_connections)
       - Delete the entire `_migrate_json_connections()` function and `_extract_database()` helper (databases.json migration is a one-time operation that has already run)
       - In `lifespan()`:
         - Delete steps 1 (Superset auth), 2 (DatabaseRegistrar sync), and 4 (legacy QueryEngine)
         - Delete the `http` client creation (`httpx.AsyncClient`) and `await http.aclose()` in shutdown
         - Delete `app.state.superset`, `app.state.database_registrar`, `app.state.http`
         - Delete step 6 (databases.json migration) -- references `settings.databases_config_path` which is being removed
         - Keep steps 3 (ConnectionStatusTracker), 5 (EncryptionService + EngineManager), 7 (pre-warm engines), 8 (ConnectionResolver), 9 (QueryExecutor)
         - Renumber remaining steps sequentially (1-5)
       - Update module docstring from "proxy + sidecar for headless Superset" to "RecViz analytics backend"
       - Update health endpoint: change `{"status": "ok", "superset": True}` to `{"status": "ok"}`
       - Delete the entire `/api/test-superset` endpoint
       - In shutdown section, remove `await http.aclose()` (no more httpx client)

    3. **backend/app/core/dependencies.py** -- Remove Superset dependency:
       - Remove `from app.services.superset_client import SupersetClient`
       - Delete `get_superset_client()` function
       - Delete `SupersetDep` type alias
       - Keep all other dependencies (DbSessionDep, ConfigStoreDep, QueryEngineDep, EngineManagerDep, ConnectionResolverDep, ResolvedDataSourceDep)
       - Note: `QueryEngine` import is still valid -- it is an alias for `QueryExecutor` in query_engine.py

    4. **backend/app/models/error.py** -- Update docstring example:
       - Line 16: Change `"superset_unavailable"` to `"service_unavailable"` in the comment example
       - This is a docstring/comment update only, not a code change

    5. **backend/tests/test_managed_kpis.py** -- Remove dead `superset_id` and `sync_status` from mock fixtures:
       - Find lines setting `ds_row.superset_id = 42` and `ds_row.sync_status = "synced"` and delete them (these columns were removed in Phase 14 migration 006)

    6. **backend/tests/test_managed_charts.py** -- Same cleanup:
       - Find lines setting `ds_row.superset_id = 42` and `ds_row.sync_status = "synced"` and delete them

    7. **backend/tests/test_search.py** -- Update the guard test:
       - The `test_search_no_superset_calls` test checks that search.py has no Superset references. This test should still pass since search.py never had Superset refs. Verify it still works. If it imports SupersetClient for the check, update to not import it. Read the test first to confirm.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/backend && python -c "from app.config import Settings; s = Settings.__fields__; assert 'superset_url' not in s; assert 'redis_url' not in s; assert 'databases_config_path' not in s; print('config OK')" && grep -c "superset_client\|database_registrar\|httpx" app/main.py | xargs test 0 -eq && grep -c "SupersetDep\|SupersetClient" app/core/dependencies.py | xargs test 0 -eq && grep -c "superset_id.*=.*42" tests/test_managed_kpis.py tests/test_managed_charts.py | xargs test 0 -eq && echo "PASS: all references cleaned" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - config.py has no superset_url, superset_username, superset_password, redis_url, or databases_config_path
    - main.py has no imports of httpx, SupersetClient, DatabaseRegistrar
    - main.py lifespan has no Superset auth, registrar sync, or databases.json migration steps
    - main.py health endpoint returns only {"status": "ok"} with no superset key
    - main.py /api/test-superset endpoint does not exist
    - dependencies.py has no SupersetDep or get_superset_client
    - test fixtures have no superset_id or sync_status assignments
  </acceptance_criteria>
  <done>The application code is fully cleaned of Superset references. Config has only RecViz-native settings. Lifespan boots directly into the engine pool with no legacy steps. Health endpoint is clean. All test fixtures reference only current model fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a deletion/cleanup phase -- no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T (Tampering) | config.py credential fields | accept | Removing superset_password field reduces credential surface area -- this is a positive change. recviz_encryption_key remains the only secret in config. |
| T-15-02 | D (Denial of Service) | Docker Compose simplification | accept | Removing Redis and Superset containers reduces attack surface. PostgreSQL-only setup has fewer exposed ports (5432 only vs 5432+6379+8088). |
</threat_model>

<verification>
Final verification after all 3 tasks:

```bash
# 1. No Superset files remain
test ! -d superset && test ! -f backend/app/services/superset_client.py && test ! -f backend/app/services/database_registrar.py

# 2. No Superset/Redis/httpx imports anywhere in backend/app/
grep -rn "superset_client\|database_registrar\|import httpx\|import redis" backend/app/ && echo "FAIL: stale imports" || echo "PASS: no stale imports"

# 3. No Redis in Docker Compose
grep -c "redis" docker-compose.yml | xargs test 0 -eq

# 4. No httpx/redis in requirements
grep -cE "^httpx|^redis|^requests" backend/requirements.txt | xargs test 0 -eq

# 5. Run existing tests (should all pass with no Superset deps)
cd backend && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -20
```
</verification>

<success_criteria>
1. Zero files import from superset_client, database_registrar, or httpx in backend/app/
2. The superset/ directory does not exist
3. docker-compose.yml has exactly one service (postgres) and one volume (pgdata)
4. requirements.txt lists only: fastapi, uvicorn, pydantic, pydantic-settings, python-dotenv, psycopg2-binary, sqlalchemy[asyncio], asyncpg, alembic, cryptography
5. All existing tests pass (pytest backend/tests/ -x)
6. The application starts with `cd backend && python -c "from app.main import app; print('import OK')"` (no import errors)
</success_criteria>

<output>
After completion, create `.planning/phases/15-superset-removal/15-01-SUMMARY.md`
</output>
