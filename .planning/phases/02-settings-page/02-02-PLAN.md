---
phase: 02-settings-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/animated-status-badge.tsx
  - frontend/src/components/settings/connection-test-area.tsx
  - frontend/src/components/settings/connection-health-header.tsx
  - frontend/src/components/settings/data-source-card.tsx
  - frontend/src/components/settings/data-source-row.tsx
  - frontend/src/components/settings/data-source-sheet.tsx
  - frontend/src/components/settings/data-sources-tab.tsx
autonomous: true
requirements:
  - SETT-01
  - SETT-06
must_haves:
  truths:
    - "Data source cards show animated status badges with pulse effect on connected status instead of tiny 2px dots"
    - "Data source cards have left-border color matching status (emerald/red/amber)"
    - "Data source rows show animated status badges with left-border color matching status"
    - "Connection test in sheet shows animated state machine: Idle -> Testing (pulsing bars) -> Success (scale-in check) or Failure (shake X)"
    - "Detail panel shows connection health header with animated status badge + last tested time + 2-column info grid"
    - "Sheet content children animate in with stagger on open"
    - "All components use CSS variable colors only -- no hardcoded hex/rgb/hsl outside of status utility classes"
  artifacts:
    - path: "frontend/src/components/settings/animated-status-badge.tsx"
      provides: "Animated status badge component with pulse dot for connected status"
      exports: ["AnimatedStatusBadge"]
    - path: "frontend/src/components/settings/connection-test-area.tsx"
      provides: "Animated connection test state machine (Idle/Testing/Success/Failure)"
      exports: ["ConnectionTestArea"]
    - path: "frontend/src/components/settings/connection-health-header.tsx"
      provides: "Connection health summary with info grid for detail panel"
      exports: ["ConnectionHealthHeader"]
  key_links:
    - from: "frontend/src/components/settings/data-source-card.tsx"
      to: "frontend/src/components/settings/animated-status-badge.tsx"
      via: "AnimatedStatusBadge import replacing StatusDot"
      pattern: "AnimatedStatusBadge"
    - from: "frontend/src/components/settings/data-source-sheet.tsx"
      to: "frontend/src/components/settings/connection-test-area.tsx"
      via: "ConnectionTestArea import replacing inline test button"
      pattern: "ConnectionTestArea"
    - from: "frontend/src/components/settings/data-source-sheet.tsx"
      to: "frontend/src/components/settings/connection-health-header.tsx"
      via: "ConnectionHealthHeader import in detail view"
      pattern: "ConnectionHealthHeader"
---

<objective>
Build premium animated components for data source status display and connection testing, then integrate them into the data source card, row, and sheet components.

Purpose: Replaces the subtle 2px status dots with animated badges (D-07, D-18), adds the connection test state machine animation (D-08), the detail panel health header (D-09), sheet content stagger animations (D-17), and card hover lift (D-17). This plan handles everything related to Data Source visual enhancement.

Output: 3 new components (`animated-status-badge.tsx`, `connection-test-area.tsx`, `connection-health-header.tsx`), 4 updated components (`data-source-card.tsx`, `data-source-row.tsx`, `data-source-sheet.tsx`, `data-sources-tab.tsx`).
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-page/02-CONTEXT.md
@.planning/phases/02-settings-page/02-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. -->

From frontend/src/types/database.ts:
```typescript
export type DatabaseBackend = 'oracle'
export type ConnectionStatus = 'connected' | 'unreachable' | 'untested'
export interface DatabaseInfo {
  id: string
  databaseName: string
  backend: DatabaseBackend
  createdOn: string | null
  exposeInSqllab: boolean
  status: ConnectionStatus
  lastTested: string | null
}
export interface TestConnectionRequest { ... }
export interface TestConnectionResponse { success: boolean; message: string }
export interface DatasetSummary { id: string; tableName: string; columnCount: number }
```

From frontend/src/hooks/use-databases.ts:
```typescript
export function useTestConnection(): UseMutationResult<TestConnectionResponse, Error, TestConnectionRequest>
export function useSyncDatasets(): UseMutationResult<...>
export function useDatabase(id: string | null): UseQueryResult<DatabaseInfo>
export function useDatabaseDatasets(dbId: string | null): UseInfiniteQueryResult<DatasetPage>
```

From frontend/src/components/settings/data-source-card.tsx (current exports to preserve):
```typescript
export const BACKEND_LABELS: Record<DatabaseBackend, string>
export const BACKEND_COLORS: Record<DatabaseBackend, string>
export const STATUS_STYLES: Record<ConnectionStatus, string>
export const STATUS_LABELS: Record<ConnectionStatus, string>
// StatusDot will be REPLACED by AnimatedStatusBadge -- remove export
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimatedStatusBadge, ConnectionTestArea, and ConnectionHealthHeader components</name>
  <files>frontend/src/components/settings/animated-status-badge.tsx, frontend/src/components/settings/connection-test-area.tsx, frontend/src/components/settings/connection-health-header.tsx</files>
  <read_first>
    - .planning/phases/02-settings-page/02-UI-SPEC.md (Sections: "Animated Status Badge Component", "Connection Test State Machine", "Detail Panel Connection Health Header", "Animation Timing Reference")
    - frontend/src/types/database.ts (ConnectionStatus, DatabaseInfo types)
    - frontend/src/components/ui/badge.tsx (Badge component API)
    - frontend/src/hooks/use-databases.ts (useTestConnection return type)
  </read_first>
  <action>
**Create `frontend/src/components/settings/animated-status-badge.tsx`** (per D-07, UI-SPEC Section 3):

Props interface:
```typescript
interface AnimatedStatusBadgeProps {
  status: ConnectionStatus
  size?: 'default' | 'large'
}
```

Implementation requirements:
- Uses shadcn `Badge` component as the base
- Badge dimensions (default): height 20px (`h-5`), `text-[10px] font-medium`, `px-2`, `rounded-full`
- Badge dimensions (large): `h-6 text-xs`
- Status color mapping (apply to Badge `className`):
  - Connected: `bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400`
  - Unreachable: `bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400`
  - Untested: `bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-400`
- Connected status renders a `motion.span` pulse dot BEFORE the text:
  - Dot: `size-1.5 rounded-full bg-emerald-500`
  - Animation: `animate={{ scale: [1, 1.4, 1], opacity: [1, 0.6, 1] }}`, `transition={{ duration: 1.5, repeat: Infinity, ease: "easeInOut" }}`
- Unreachable: static red dot (`bg-red-500`)
- Untested: static amber dot (`bg-amber-500`)
- Status text from map: `{ connected: 'Connected', unreachable: 'Unreachable', untested: 'Untested' }`
- Import `motion` from `motion/react` (NOT `framer-motion`)

**Create `frontend/src/components/settings/connection-test-area.tsx`** (per D-08, UI-SPEC Section 5):

Props interface:
```typescript
interface ConnectionTestAreaProps {
  onTest: () => void
  isPending: boolean
  result: { success: boolean; message: string } | null
}
```

Implementation requirements:
- Wrap the test button + result in a container: `rounded-lg border p-3 space-y-2`
- Container border color animates on result:
  - Success: briefly `border-emerald-500/50` for 600ms via `motion.div` `animate`, then back to `border` (use `border` CSS variable)
  - Failure: briefly `border-red-500/50` for 600ms, then back
  - Use `useEffect` + `useState` for a `flashColor` state that auto-clears after 600ms
- **Idle state:** "Test Connection" button with `Plug` icon (from lucide-react), `variant="outline"`, `size="sm"`
- **Testing state (isPending=true):** Button disabled, text "Connecting...", icon replaced with 3 animated vertical bars:
  - 3 `motion.span` bars inside a `flex items-end gap-0.5 h-3.5` container
  - Each bar: `w-0.5 bg-current rounded-full`
  - Animation: `animate={{ scaleY: [0.4, 1, 0.4] }}`, `transition={{ duration: 0.8, repeat: Infinity, ease: "easeInOut", delay: i * 0.1 }}` where `i` is 0, 1, 2
  - Container gets subtle background pulse: `motion.div` with `animate={{ backgroundColor: ['hsl(var(--muted) / 0.3)', 'hsl(var(--muted) / 0.6)', 'hsl(var(--muted) / 0.3)'] }}`, `transition={{ duration: 1.5, repeat: Infinity }}`
  - NOTE: For the background pulse, use Tailwind classes with `motion.div` opacity animation instead of direct hsl manipulation. Apply `bg-muted/30` as base, animate opacity `0.3 -> 0.6 -> 0.3` on overlay.
- **Success state (result.success=true):** `CheckCircle2` icon from lucide-react with:
  - `motion.div` wrapper: `initial={{ scale: 0, rotate: -90 }}`, `animate={{ scale: 1, rotate: 0 }}`, `transition={{ type: "spring", stiffness: 300, damping: 20 }}`
  - Text "Connected in {N}s" (extract from `result.message`): `motion.span` with `initial={{ opacity: 0, x: -8 }}`, `animate={{ opacity: 1, x: 0 }}`, `transition={{ delay: 0.15, duration: 0.2, ease: "easeOut" }}`
  - Icon + text color: `text-emerald-600 dark:text-emerald-400`
- **Failure state (result.success=false):** `XCircle` icon with:
  - `motion.div` wrapper: `animate={{ x: [0, -4, 4, -4, 4, 0] }}`, `transition={{ duration: 0.4 }}`
  - Error text from `result.message`: same `motion.span` fade-in as success
  - Icon + text color: `text-red-600 dark:text-red-400`
- Use `AnimatePresence` with `mode="wait"` for transitions between states
- Import `motion, AnimatePresence` from `motion/react`

**Create `frontend/src/components/settings/connection-health-header.tsx`** (per D-09, D-10, UI-SPEC Section 6):

Props interface:
```typescript
interface ConnectionHealthHeaderProps {
  database: DatabaseInfo
}
```

Implementation requirements:
- Container: `rounded-lg border p-4 space-y-3`
- Row 1: `AnimatedStatusBadge` (large variant, pass `size="large"`) + "Last tested: {relative time}" caption using `text-xs text-muted-foreground`
  - Use `formatDistanceToNow` from `date-fns` for relative time (e.g., "3 minutes ago")
  - If `database.lastTested` is null, show "Never tested"
- Row 2: 2-column info grid using `dl` pattern:
  ```
  <div className="grid grid-cols-2 gap-x-3 gap-y-2">
    <div><dt className="text-xs text-muted-foreground">Host</dt><dd className="text-sm font-mono truncate">{value}</dd></div>
    ...
  </div>
  ```
  - Fields: Host, Port, Service Name, Schema
  - Values sourced from `database` object. The `DatabaseInfo` type does NOT directly expose host/port/service -- these are internal. If unavailable, show "Configured" as the value (per UI-SPEC note: "derive from stored connection, or show 'Configured' if unavailable")
  - NO live queries (D-10). Use only data already in the `DatabaseInfo` response.
- Import `AnimatedStatusBadge` from `./animated-status-badge`
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz && grep -n "AnimatedStatusBadge" frontend/src/components/settings/animated-status-badge.tsx && grep -n "ConnectionTestArea" frontend/src/components/settings/connection-test-area.tsx && grep -n "ConnectionHealthHeader" frontend/src/components/settings/connection-health-header.tsx && grep -n "scale.*1.4" frontend/src/components/settings/animated-status-badge.tsx && grep -n "stiffness.*300" frontend/src/components/settings/connection-test-area.tsx && grep -n "formatDistanceToNow" frontend/src/components/settings/connection-health-header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `animated-status-badge.tsx` exports `AnimatedStatusBadge` with `status` and optional `size` props
    - Badge renders pulse animation on connected status: `scale: [1, 1.4, 1]`, `opacity: [1, 0.6, 1]`, duration 1.5s, infinite loop
    - Badge colors match UI-SPEC exactly (emerald/red/amber with dark variants)
    - `connection-test-area.tsx` exports `ConnectionTestArea` with `onTest`, `isPending`, `result` props
    - Testing state shows 3 animated bars with stagger 0.1s, 0.8s loop
    - Success state shows CheckCircle2 with spring animation (stiffness 300, damping 20, scale 0->1, rotate -90->0)
    - Failure state shows XCircle with shake animation (x: [0, -4, 4, -4, 4, 0], 400ms)
    - Result text fades in with 150ms delay, 200ms duration, x: -8->0
    - Border flash on result: 600ms duration, emerald for success, red for failure
    - `connection-health-header.tsx` exports `ConnectionHealthHeader` with `database` prop
    - Health header shows AnimatedStatusBadge (large), last tested relative time, 2-column info grid
    - No `any` types anywhere
    - All three files use `motion/react` (NOT `framer-motion`)
  </acceptance_criteria>
  <done>Three new animated components created: AnimatedStatusBadge with pulse effect, ConnectionTestArea with 4-state animation machine, ConnectionHealthHeader with info grid.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate new components into data-source-card, data-source-row, data-source-sheet, and data-sources-tab</name>
  <files>frontend/src/components/settings/data-source-card.tsx, frontend/src/components/settings/data-source-row.tsx, frontend/src/components/settings/data-source-sheet.tsx, frontend/src/components/settings/data-sources-tab.tsx</files>
  <read_first>
    - frontend/src/components/settings/data-source-card.tsx (current card with StatusDot)
    - frontend/src/components/settings/data-source-row.tsx (current row with StatusDot)
    - frontend/src/components/settings/data-source-sheet.tsx (current 848-line sheet)
    - frontend/src/components/settings/data-sources-tab.tsx (grid layout)
    - frontend/src/components/settings/animated-status-badge.tsx (just created)
    - frontend/src/components/settings/connection-test-area.tsx (just created)
    - frontend/src/components/settings/connection-health-header.tsx (just created)
    - .planning/phases/02-settings-page/02-UI-SPEC.md (Sections 3, 4, 5, 6, 7, 8, "Animation Timing Reference")
  </read_first>
  <action>
**Update `frontend/src/components/settings/data-source-card.tsx`** (per D-07, D-17, UI-SPEC Sections 3, "Card hover lift"):

1. Replace `StatusDot` usage with `AnimatedStatusBadge`:
   - Remove the `StatusDot` component definition and its internal `STATUS_DOT_COLORS` record
   - Keep exporting `BACKEND_LABELS`, `BACKEND_COLORS`, `STATUS_STYLES`, `STATUS_LABELS` (other files import them)
   - Import `AnimatedStatusBadge` from `./animated-status-badge`
2. Add `border-l-2` with status color to Card:
   - Connected: `border-l-emerald-500`
   - Unreachable: `border-l-red-500`
   - Untested: `border-l-amber-500`
   - Create a `STATUS_BORDER_COLORS` record for this mapping
3. Replace the status indicator section (currently `StatusDot` + text label) with just `<AnimatedStatusBadge status={database.status} />`
4. Add `motion/react` hover lift:
   - Import `motion` from `motion/react`
   - Replace `<Card>` with `<motion.div>` that wraps Card, or use Card's className and add `motion.div`:
   - `whileHover={{ y: -2 }}`, `transition={{ duration: 0.15, ease: "easeOut" }}`
   - Remove the existing `hover:-translate-y-0.5` Tailwind class (replaced by motion)
   - Keep `hover:shadow-md` for shadow on hover
5. Wrap Database icon with subtle container: `bg-muted rounded-lg p-1.5` (UI-SPEC Section 3)

**Update `frontend/src/components/settings/data-source-row.tsx`** (per D-07, UI-SPEC Section 4):

1. Replace `StatusDot` with `AnimatedStatusBadge`:
   - Remove import of `StatusDot` from `./data-source-card`
   - Import `AnimatedStatusBadge` from `./animated-status-badge`
2. Add `border-l-2` with status color (same mapping as card):
   - Import `STATUS_BORDER_COLORS` from `./data-source-card` (or define locally)
3. Replace the `StatusDot` + text label with `<AnimatedStatusBadge status={database.status} />`

**Update `frontend/src/components/settings/data-source-sheet.tsx`** (per D-08, D-09, D-17, UI-SPEC Sections 5, 6, 7):

This is the biggest file (848 lines). Changes are surgical -- do NOT rewrite the entire file. Apply these specific modifications:

1. **Import new components:**
   - `import { AnimatedStatusBadge } from './animated-status-badge'`
   - `import { ConnectionTestArea } from './connection-test-area'`
   - `import { ConnectionHealthHeader } from './connection-health-header'`
   - `import { motion, AnimatePresence } from 'motion/react'`

2. **DetailView changes:**
   a. **Replace StatusDot in header** (line ~414): Replace `<StatusDot status={database.status} />` + text span with `<AnimatedStatusBadge status={database.status} />`
   b. **Add ConnectionHealthHeader** after `SheetHeader`, before Datasets section (line ~420-421 area). Insert:
      ```tsx
      <ConnectionHealthHeader database={database} />
      ```
      Place it inside the `ScrollArea > div.p-6.space-y-6` wrapper, as the first child before the Datasets `<div>`.
   c. **Replace inline test connection** (lines ~498-526): Replace the entire test connection `<div>` block with:
      ```tsx
      <ConnectionTestArea
        onTest={onTestConnection}
        isPending={testMutation.isPending}
        result={testResult}
      />
      ```
   d. **Update button labels per UI-SPEC Copywriting:**
      - "Edit" button -> "Edit Source" with `Pencil` icon
      - "Sync" button -> "Sync Datasets" with `RefreshCw` icon

3. **FormView changes:**
   a. **Replace inline test connection** (lines ~738-766): Same as DetailView -- replace with `<ConnectionTestArea>` component
   b. **Update button labels per UI-SPEC Copywriting:**
      - "Save" button -> "Save Connection" (create mode)
      - "Cancel" button -> "Discard"

4. **Sheet content stagger animation** (D-17, UI-SPEC Section 7):
   In both DetailView and FormView, wrap each major section (header, health, datasets, test, footer) in `motion.div` with stagger:
   ```tsx
   <motion.div
     initial={{ opacity: 0, y: 12 }}
     animate={{ opacity: 1, y: 0 }}
     transition={{ duration: 0.25, delay: index * 0.05 }}
   >
   ```
   Where `index` is 0, 1, 2, 3, 4 for each section. Apply to the content sections INSIDE `ScrollArea`, not the header/footer chrome.

5. **Detail -> Edit cross-fade** (D-17, UI-SPEC Section 7):
   Wrap the `{mode === 'detail' ? <DetailView .../> : <FormView .../>}` conditional in:
   ```tsx
   <AnimatePresence mode="wait">
     <motion.div
       key={mode}
       initial={{ opacity: 0 }}
       animate={{ opacity: 1 }}
       exit={{ opacity: 0 }}
       transition={{ duration: mode === 'detail' ? 0.2 : 0.15 }}
     >
   ```

**Update `frontend/src/components/settings/data-sources-tab.tsx`** (minor):

Update the grid to be responsive within the wider container:
- Change `grid-cols-3` to `grid-cols-2 lg:grid-cols-3` for the card grid
- This is a small change to accommodate the wider max-w-5xl container
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz && grep -n "AnimatedStatusBadge" frontend/src/components/settings/data-source-card.tsx && grep -n "border-l-" frontend/src/components/settings/data-source-card.tsx && grep -n "AnimatedStatusBadge" frontend/src/components/settings/data-source-row.tsx && grep -n "ConnectionTestArea" frontend/src/components/settings/data-source-sheet.tsx && grep -n "ConnectionHealthHeader" frontend/src/components/settings/data-source-sheet.tsx && grep -n "Save Connection" frontend/src/components/settings/data-source-sheet.tsx && grep -rn "StatusDot" frontend/src/components/settings/ && npx tsc --noEmit --project frontend/tsconfig.app.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `data-source-card.tsx` uses `AnimatedStatusBadge` (not `StatusDot`)
    - `data-source-card.tsx` has `border-l-2` with emerald/red/amber status colors
    - `data-source-card.tsx` has `motion.div` whileHover `y: -2` with 150ms duration
    - `data-source-card.tsx` has Database icon wrapped in `bg-muted rounded-lg p-1.5`
    - `data-source-row.tsx` uses `AnimatedStatusBadge` (not `StatusDot`)
    - `data-source-row.tsx` has `border-l-2` with status colors
    - `data-source-sheet.tsx` imports and uses `ConnectionTestArea` in both DetailView and FormView
    - `data-source-sheet.tsx` imports and uses `ConnectionHealthHeader` in DetailView
    - `data-source-sheet.tsx` has stagger animation (`delay: index * 0.05`) on content sections
    - `data-source-sheet.tsx` has `AnimatePresence mode="wait"` for detail/edit cross-fade
    - `data-source-sheet.tsx` button labels match UI-SPEC: "Save Connection", "Discard", "Edit Source", "Sync Datasets"
    - `StatusDot` is no longer exported or used in any settings component (grep returns zero hits for StatusDot usage)
    - `data-sources-tab.tsx` grid uses `grid-cols-2 lg:grid-cols-3`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All data source components upgraded: StatusDot replaced with AnimatedStatusBadge, ConnectionTestArea replaces inline test buttons, ConnectionHealthHeader added to detail panel, sheet has stagger + cross-fade animations, button labels match UI-SPEC copywriting contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend API -> Frontend display | Connection test results displayed in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-03 | I (Info Disclosure) | connection-health-header.tsx | accept | Host/port/service are shown to the user who already configured them. No additional exposure. Backend already sanitizes error messages via `sanitize_detail()`. |
| T-02-04 | S (Spoofing) | connection-test-area.tsx | accept | Test results come from the backend the user trusts. Visual-only component. No auth decisions made based on test results. |
</threat_model>

<verification>
1. Data Source cards show colored left border (emerald for connected, red for unreachable, amber for untested)
2. Cards show AnimatedStatusBadge with pulse on connected status (1.5s loop, subtle)
3. Cards lift on hover (motion/react, 150ms, y: -2)
4. Data Source rows show AnimatedStatusBadge with left border color
5. Click a data source card to open detail sheet:
   a. Sheet content sections stagger in (50ms between each, 250ms total)
   b. ConnectionHealthHeader shows at top with large status badge + last tested time + info grid
   c. "Edit Source" button (not "Edit"), "Sync Datasets" button (not "Sync")
6. Click "Edit Source" to switch to edit mode:
   a. Cross-fade animation (detail fades out 150ms, edit fades in 200ms)
   b. ConnectionTestArea visible with "Test Connection" button
7. Click "Test Connection":
   a. Button shows "Connecting..." with 3 animated bars
   b. On success: green check scales in with spring, "Connected in Ns" fades in
   c. On failure: red X shakes, error message fades in
   d. Container border flashes (emerald/red) for 600ms
8. Create new data source sheet shows "Save Connection" (not "Save"), "Discard" (not "Cancel")
9. All elements work in both light and dark mode
</verification>

<success_criteria>
All data source components upgraded with premium animated status badges, connection test state machine, health header, stagger animations, and cross-fade transitions. StatusDot completely replaced. All button labels match UI-SPEC copywriting contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-page/02-02-SUMMARY.md`
</output>
