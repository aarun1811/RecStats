---
phase: 02-settings-page
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - .planning/USAGE-TRACKER.md
autonomous: false
requirements:
  - SETT-02
  - SETT-03
  - SETT-07
must_haves:
  truths:
    - "User can list existing data sources from the Data Sources tab"
    - "User can create a new data source, test its connection against Oracle, and save it"
    - "User can edit an existing data source and update it"
    - "User can delete a data source"
    - "User can list, load, and delete saved views from the Saved Views tab"
    - "All CRUD operations round-trip against live Oracle 19c"
    - "Connection test goes through build_oracle_engine() thick-mode path"
    - "USAGE-TRACKER.md updated with all Phase 2 file changes"
  artifacts:
    - path: ".planning/USAGE-TRACKER.md"
      provides: "Phase 2 file tracking for dead code sweep"
      contains: "Phase 2"
  key_links:
    - from: "frontend (CRUD UI)"
      to: "backend/app/api/databases.py"
      via: "HTTP API calls"
      pattern: "/api/databases"
    - from: "backend/app/api/databases.py"
      to: "backend/app/services/engine_manager.py"
      via: "build_oracle_engine() for connection testing"
      pattern: "build_oracle_engine"
---

<objective>
Verify the full Settings page end-to-end against live Oracle: Data Sources CRUD (create, test, edit, delete), Saved Views (list, load, delete), and update the USAGE-TRACKER with all Phase 2 file changes.

Purpose: This is the verification plan that confirms the Phase 2 UI work (Plans 01 and 02) actually functions against the real database. Also satisfies SETT-07 by updating the usage tracker for Phase 8's dead code sweep.

Output: Verified working Settings page, updated `USAGE-TRACKER.md`.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-page/02-CONTEXT.md
@.planning/USAGE-TRACKER.md
@.planning/phases/02-settings-page/02-01-SUMMARY.md
@.planning/phases/02-settings-page/02-02-SUMMARY.md

<interfaces>
<!-- Backend API endpoints for CRUD operations -->

From backend/app/api/databases.py:
```
GET    /api/databases              -> list all data sources
GET    /api/databases/{id}         -> get single data source
POST   /api/databases              -> create data source
PUT    /api/databases/{id}         -> update data source
DELETE /api/databases/{id}         -> delete data source
POST   /api/databases/test         -> test connection
POST   /api/databases/{id}/sync    -> sync datasets
GET    /api/databases/{id}/datasets -> paginated datasets
```

From backend/app/api/views.py:
```
GET    /api/views                  -> list saved views
DELETE /api/views/{id}             -> delete saved view
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile check, lint, and fix any TypeScript errors from Plans 01 and 02</name>
  <files>(fixes applied to files from Plans 01 and 02 as needed)</files>
  <read_first>
    - .planning/phases/02-settings-page/02-01-SUMMARY.md (Plan 01 results)
    - .planning/phases/02-settings-page/02-02-SUMMARY.md (Plan 02 results)
  </read_first>
  <action>
Run the TypeScript compiler and linter to catch any issues from Plans 01 and 02:

1. `cd frontend && npx tsc --noEmit --project tsconfig.app.json`
2. `cd frontend && pnpm lint`

Fix any errors found. Common issues to watch for:
- Missing imports (motion/react, date-fns formatDistanceToNow, lucide icons)
- Type mismatches between AnimatedStatusBadge props and StatusDot props
- Unused imports from removed StatusDot
- `verbatimModuleSyntax` violations (must use `import type` for type-only imports)
- `noUnusedLocals` violations from removed code

After fixing, re-run both commands to confirm zero errors.

Also verify the backend starts without errors:
- `cd backend && python -c "from app.main import app; print('Backend imports OK')"`
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with zero errors
    - `pnpm lint` exits with zero errors (or only pre-existing warnings)
    - No `StatusDot` references remain in any settings component (verify via grep)
    - Backend imports without errors
  </acceptance_criteria>
  <done>All TypeScript and lint errors from Plans 01 and 02 resolved. Frontend compiles cleanly. Backend imports cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end manual verification of Settings page against Oracle</name>
  <files>frontend/src/routes/_app/settings/index.tsx</files>
  <action>
Human verifies the complete Settings page end-to-end against live Oracle. All UI was built in Plans 01 and 02, compiled in Task 1. This checkpoint confirms visual quality and functional correctness before proceeding.

**Prerequisites:** Oracle Docker container running (`docker ps | grep oracle`), backend running (`uvicorn app.main:app --reload`), frontend running (`pnpm dev`).

**Verification checklist:**

1. Settings Page Layout (D-04): Open http://localhost:5173/settings -- verify page is wider (~1024px) and centered
2. Tab Transitions (D-05): Click between all 3 tabs -- verify horizontal slide + fade animation (200ms)
3. Theme Preview Cards (D-06): Verify 3 cards with mini-mockups, animated border on selection, actual theme change
4. Display Controls (D-01, D-02, D-03, SETT-05): Toggle density (Comfortable/Compact) and font size (S/M/L) -- verify visual changes and localStorage persistence on refresh
5. Saved Views Tab (D-15, SETT-03): Verify list renders or empty state; if views exist, test Load and Delete
6. Data Sources Tab (SETT-01): Verify card grid with animated status badges, left-border colors, hover lift
7. Data Source CRUD (D-11, D-12, D-13, SETT-02):
   a. Create new source (host: localhost, port: 1521, service: FREEPDB1, user: recviz, password: recviz_dev)
   b. Test connection -- verify animated state machine (bars, success check, border flash)
   c. Save -- verify toast
   d. Open detail -- verify health header, stagger animation
   e. Edit -- verify cross-fade, update, toast
   f. Delete -- verify confirm dialog, deletion, toast
8. Dark Mode (D-16, SETT-04): Toggle dark mode, verify all elements themed correctly
9. Light Mode: Toggle back, same check
  </action>
  <verify>Human visual inspection of all 9 verification areas</verify>
  <acceptance_criteria>
    - All 9 verification areas pass visual and functional inspection
    - CRUD operations round-trip against Oracle without errors
    - All animations render with premium feel (not generic defaults)
    - Both light and dark mode work with no gray-only holdouts
  </acceptance_criteria>
  <done>Settings page verified end-to-end. All tabs functional, CRUD works against Oracle, animations premium quality, both themes correct.</done>
</task>

<task type="auto">
  <name>Task 3: Update USAGE-TRACKER.md with Phase 2 file changes</name>
  <files>.planning/USAGE-TRACKER.md</files>
  <read_first>
    - .planning/USAGE-TRACKER.md (current tracker state)
    - .planning/phases/02-settings-page/02-01-SUMMARY.md (files from Plan 01)
    - .planning/phases/02-settings-page/02-02-SUMMARY.md (files from Plan 02)
  </read_first>
  <action>
Append a "Phase 2: Settings Page" section to `.planning/USAGE-TRACKER.md` following the exact format established by Phase 1. Use the same table structure.

**Files Added** (from Plans 01 and 02):
| File | Plan | Purpose |
|------|------|---------|
| `frontend/src/stores/display-store.ts` | 02-01 | Zustand store for density + fontSize (CSS var writing, localStorage) |
| `frontend/src/components/settings/theme-preview-card.tsx` | 02-01 | Theme preview card with CSS-drawn mini-mockups |
| `frontend/src/components/settings/animated-status-badge.tsx` | 02-02 | Animated status badge with pulse effect |
| `frontend/src/components/settings/connection-test-area.tsx` | 02-02 | Connection test state machine (Idle/Testing/Success/Fail) |
| `frontend/src/components/settings/connection-health-header.tsx` | 02-02 | Connection health summary + info grid |

**Files Modified** (from Plans 01 and 02):
| File | Plan | Change |
|------|------|--------|
| `frontend/src/index.css` | 02-01 | Body transition rule for font-size/line-height |
| `frontend/src/routes/_app/settings/index.tsx` | 02-01 | max-w-5xl layout, tab animations, theme preview cards, display controls |
| `frontend/src/components/settings/data-source-card.tsx` | 02-02 | AnimatedStatusBadge, border-l status color, motion hover |
| `frontend/src/components/settings/data-source-row.tsx` | 02-02 | AnimatedStatusBadge, border-l status color |
| `frontend/src/components/settings/data-source-sheet.tsx` | 02-02 | ConnectionTestArea, ConnectionHealthHeader, stagger animation, cross-fade, updated labels |
| `frontend/src/components/settings/data-sources-tab.tsx` | 02-02 | Responsive grid (grid-cols-2 lg:grid-cols-3) |

**Files Removed:** None.

Verify the file list against the actual git diff to ensure accuracy. Run `git diff --name-only HEAD~N` (where N covers Phase 2 commits) to cross-check.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz && grep -n "Phase 2" .planning/USAGE-TRACKER.md && grep -c "display-store" .planning/USAGE-TRACKER.md && grep -c "animated-status-badge" .planning/USAGE-TRACKER.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/USAGE-TRACKER.md` contains a "Phase 2: Settings Page" section
    - Section lists all 5 new files under "Files Added"
    - Section lists all 6 modified files under "Files Modified"
    - Table format matches Phase 1's format exactly (File | Plan | Purpose/Change)
    - No [audit] flags needed (all files have clear purpose)
  </acceptance_criteria>
  <done>USAGE-TRACKER.md updated with complete Phase 2 file tracking. All added and modified files documented with plan attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend API | CRUD operations send user input to backend |
| Backend -> Oracle | SQL execution against live database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-05 | T (Tampering) | databases.py API | mitigate | Backend already validates inputs via Pydantic models. Connection test uses `build_oracle_engine()` which enforces thick mode. `sanitize_detail()` redacts connection strings from error responses. No changes needed -- verify existing mitigations work. |
| T-02-06 | I (Info Disclosure) | connection test error messages | mitigate | Backend `sanitize_detail()` truncates to 500 chars and regex-redacts SQLAlchemy URIs. Verify during E2E test that a failed connection does not leak full connection string in the error message shown in the UI. |
</threat_model>

<verification>
1. TypeScript compiles with zero errors
2. ESLint passes with zero errors
3. All Data Source CRUD operations work against live Oracle
4. Connection test uses thick-mode path (verify in backend logs)
5. Saved Views tab renders correctly
6. All UI works in both light and dark mode
7. USAGE-TRACKER.md accurately reflects Phase 2 changes
</verification>

<success_criteria>
Settings page fully verified end-to-end against live Oracle. CRUD operations round-trip successfully. All visual enhancements from Plans 01 and 02 render correctly. USAGE-TRACKER updated for Phase 8 dead code sweep.
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-page/02-03-SUMMARY.md`
</output>
