---
phase: 16-parity-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - frontend/e2e/parity-dashboards.spec.ts
  - frontend/e2e/parity-builder.spec.ts
autonomous: false
requirements:
  - PRTY-02
  - PRTY-03
  - PRTY-04
  - PRTY-05

must_haves:
  truths:
    - "All 5 seed dashboards render with charts, KPIs, grids visible and no console errors"
    - "Cross-filter click on a chart segment updates other panels on the same dashboard"
    - "Dashboard builder can create a new dashboard, add a chart, save, and delete it"
    - "Sharing URL sync, embed mode, and Cmd+K command palette all function"
  artifacts:
    - path: "frontend/e2e/parity-dashboards.spec.ts"
      provides: "E2E tests for seed dashboard rendering, cross-filter, sharing, embed, Cmd+K"
      min_lines: 40
    - path: "frontend/e2e/parity-builder.spec.ts"
      provides: "E2E test for dashboard builder create/edit/save/delete cycle"
      min_lines: 30
  key_links:
    - from: "frontend/e2e/parity-dashboards.spec.ts"
      to: "frontend/e2e/_fixtures.ts"
      via: "imports CURATED_DASHBOARDS and waitForDashboardLoad"
      pattern: "CURATED_DASHBOARDS"
    - from: "frontend/e2e/parity-builder.spec.ts"
      to: "/api/dashboards/managed"
      via: "Creates and deletes dashboard via UI interactions"
      pattern: "dashboards"
---

<objective>
Verify that all dashboard-related v1.0 features work identically with the new direct engine: seed dashboard rendering (PRTY-02), cross-filtering and drill-down (PRTY-03), builder CRUD cycle (PRTY-04), and sharing/embed/Cmd+K (PRTY-05). This is done via new E2E tests plus running existing E2E suites that already cover sharing, embed, and Cmd+K.

Purpose: Phases 12-15 replaced the entire query engine and API layer. This plan proves the frontend still works end-to-end against real seeded data with the new engine.

Output: Two new E2E test files and a human-verified walkthrough confirming visual correctness.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-parity-verification/16-01-SUMMARY.md

<interfaces>
<!-- Existing E2E test fixtures and helpers -->

From frontend/e2e/_fixtures.ts:
```typescript
export const CURATED_DASHBOARDS = {
  sla: { id: string, name: string },
  aging: { id: string, name: string },
  matchRate: { id: string, name: string },
  volume: { id: string, name: string },
  breaksSummary: { id: string, name: string },
}

export function waitForDashboardLoad(page: Page, name: string): Promise<void>
// Waits for h1 containing name to appear and skeleton loaders to disappear
```

<!-- Existing E2E specs that already cover PRTY-05 -->
- frontend/e2e/share-link.spec.ts -- bidirectional URL sync, clipboard
- frontend/e2e/embed.spec.ts -- embed mode with theme/filter/hide params (8 tests)
- frontend/e2e/command-palette.spec.ts -- Cmd+K search across entities

<!-- API endpoints used -->
- POST /api/dashboards/managed -- create dashboard
- PUT /api/dashboards/managed/:id -- update dashboard
- DELETE /api/dashboards/managed/:id -- delete dashboard
- POST /api/data-sources/:id/query -- query for chart data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for seed dashboard rendering and cross-filter</name>
  <files>frontend/e2e/parity-dashboards.spec.ts</files>
  <read_first>
    frontend/e2e/_fixtures.ts
    frontend/e2e/chart-showcase.spec.ts
    frontend/e2e/dashboard-view-regression.spec.ts
    frontend/playwright.config.ts
  </read_first>
  <action>
Create `frontend/e2e/parity-dashboards.spec.ts` that verifies PRTY-02 and PRTY-03.

**Test structure:**

```typescript
import { expect, test } from '@playwright/test'
import { CURATED_DASHBOARDS, waitForDashboardLoad } from './_fixtures'
```

**Test 1 -- All 5 seed dashboards render without errors (PRTY-02):**
- Loop through all 5 `CURATED_DASHBOARDS` (sla, aging, matchRate, volume, breaksSummary)
- For each dashboard:
  - Navigate to `/dashboards/${dashboard.id}`
  - Call `waitForDashboardLoad(page, dashboard.name)`
  - Assert h1 with dashboard name is visible
  - Assert "Dashboard not found" text is NOT present
  - Assert no console errors were logged
  - Assert at least one chart container is visible (locate `.ag-charts-wrapper` or `[data-testid*="chart"]` or similar chart container)
  - Use `test.describe` with a loop, one `test()` per dashboard so failures are granular

**Test 2 -- Cross-filter interaction (PRTY-03):**
- Navigate to the "volume" dashboard (it has multiple related charts)
- Wait for dashboard to load
- Find a chart panel (use `.ag-charts-wrapper` or a chart container)
- Click on a data point in the chart canvas area (use coordinates relative to the chart container, e.g., click at the center-ish of the first chart)
- After click, verify the cross-filter bar appears or that another chart panel re-renders (check for visual change or network request)
- If cross-filter bar is implemented: look for a visual indicator like a badge or breadcrumb showing the active filter
- NOTE: Cross-filter is client-side via Zustand, so no network request is expected. Verify via DOM changes -- a cross-filter bar element should appear, or chart containers should update

**Test 3 -- Existing E2E suites pass (PRTY-05 -- sharing/embed/Cmd+K):**
- This is NOT a new test to write. The executor should RUN the existing E2E specs:
  - `npx playwright test share-link.spec.ts --reporter=list`
  - `npx playwright test embed.spec.ts --reporter=list`
  - `npx playwright test command-palette.spec.ts --reporter=list`
- Record pass/fail in the summary. These existing tests already cover PRTY-05.

**Important patterns:**
- Follow the existing E2E pattern from `chart-showcase.spec.ts` for console error capture
- Use `test.describe` grouping
- Use `waitForDashboardLoad` from `_fixtures.ts` (handles skeleton removal)
- Set generous timeouts (dashboards load real data from the database)
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx playwright test parity-dashboards.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/e2e/parity-dashboards.spec.ts exists and has at least 40 lines
    - Playwright test run shows all seed dashboard tests passing
    - grep "CURATED_DASHBOARDS" frontend/e2e/parity-dashboards.spec.ts confirms fixture usage
  </acceptance_criteria>
  <done>All 5 seed dashboards render correctly with charts visible and no console errors. Cross-filter interaction test passes. Existing sharing/embed/Cmd+K E2E suites pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E test for dashboard builder create/edit/save/delete cycle</name>
  <files>frontend/e2e/parity-builder.spec.ts</files>
  <read_first>
    frontend/e2e/dashboard-edit-regression.spec.ts
    frontend/e2e/_fixtures.ts
    frontend/src/routes/_app/dashboards/new.tsx
    frontend/src/components/builder/builder-page.tsx
    frontend/src/components/builder/save-dashboard-dialog.tsx
  </read_first>
  <action>
Create `frontend/e2e/parity-builder.spec.ts` that verifies PRTY-04 (dashboard builder CRUD cycle).

**Test -- Dashboard builder create/edit/save/delete (PRTY-04):**

```typescript
import { expect, test } from '@playwright/test'
```

1. **Create a new dashboard:**
   - Navigate to `/dashboards/new`
   - Wait for the builder page to load (look for the builder toolbar or canvas)
   - The page should render without errors

2. **Add a chart to the canvas:**
   - Find and click the "Add Chart" or "+" button in the builder toolbar
   - A chart picker dialog should appear
   - Select the first available chart from the picker
   - Click "Add" or confirm
   - Verify a chart panel appears on the canvas

3. **Save the dashboard:**
   - Click the "Save" button in the builder toolbar
   - A save dialog should appear
   - Type a unique name (e.g., `"Parity Test Dashboard ${Date.now()}"`)
   - Click the save/confirm button
   - Wait for the success toast or navigation
   - Capture the dashboard ID from the URL (e.g., `/dashboards/:id/edit`)

4. **Verify the saved dashboard loads in view mode:**
   - Navigate to `/dashboards/${savedId}`
   - Verify the dashboard loads with the chart visible

5. **Delete the dashboard (cleanup):**
   - Navigate back to `/dashboards/${savedId}/edit`
   - Find and click the "Delete" or trash button
   - Confirm the deletion dialog
   - Verify redirect to dashboard list
   - Verify the deleted dashboard is not in the list

**Important notes:**
- Use `test.setTimeout(60000)` -- builder operations involve multiple API calls
- Use unique dashboard names to avoid conflicts
- Clean up created dashboards in the test itself (not in afterEach) so the test is self-contained
- If button text is not exact, use `getByRole('button')` with `name` regex patterns
- Read the builder component files first to understand exact button labels and dialog structure
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/RecViz/frontend && npx playwright test parity-builder.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/e2e/parity-builder.spec.ts exists and has at least 30 lines
    - Playwright test run shows the builder CRUD cycle test passing
    - Test creates, saves, views, and deletes a dashboard without manual intervention
  </acceptance_criteria>
  <done>Dashboard builder can create a new dashboard, add a chart, save it, view it, and delete it -- full CRUD cycle verified via E2E test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of dashboard features</name>
  <what-built>
    E2E tests verifying all dashboard features work with the new direct engine:
    - All 5 seed dashboards render with real data (PRTY-02)
    - Cross-filtering works (PRTY-03)
    - Dashboard builder CRUD cycle works (PRTY-04)
    - Sharing, embed, and Cmd+K confirmed via existing E2E suites (PRTY-05)
  </what-built>
  <how-to-verify>
    1. Start the full stack (Docker Compose + backend + frontend)
    2. Open http://localhost:5173/dashboards -- verify all 5 seed dashboards are listed
    3. Click into each dashboard -- verify charts, KPIs, and data grids render with real data (not loading spinners stuck or errors)
    4. On any dashboard with multiple charts: click a data segment in one chart and verify other charts update (cross-filter)
    5. Click Cmd+K -- verify command palette opens and shows search results
    6. Share a dashboard URL with filter params -- open in new tab and verify filters are applied
    7. Navigate to /dashboards/new -- verify builder loads, you can add a chart, save, and delete
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test -> API | Tests hit real API with seeded data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Information Disclosure | E2E test data | accept | Tests use seed data only, no production credentials |
</threat_model>

<verification>
1. `cd frontend && npx playwright test parity-dashboards.spec.ts --reporter=list` -- all dashboard rendering tests pass
2. `cd frontend && npx playwright test parity-builder.spec.ts --reporter=list` -- builder CRUD test passes
3. `cd frontend && npx playwright test share-link.spec.ts embed.spec.ts command-palette.spec.ts --reporter=list` -- existing E2E suites pass
4. Human walkthrough confirms visual correctness
</verification>

<success_criteria>
- All 5 seed dashboards render with charts, KPIs, grids, and filters populated from the direct engine
- Cross-filtering interaction produces visual changes on dashboard panels
- Dashboard builder create/edit/save/delete cycle completes successfully
- Sharing URL sync, embed mode, and Cmd+K command palette all function
- Human approval of visual walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/16-parity-verification/16-02-SUMMARY.md`
</output>
