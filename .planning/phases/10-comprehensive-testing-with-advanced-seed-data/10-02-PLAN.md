---
phase: 10-comprehensive-testing-with-advanced-seed-data
plan: 02
type: execute
wave: 4
depends_on:
  - 10-01c
files_modified:
  - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md
  - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md
  - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state
autonomous: false
requirements:
  - INTR-01
  - INTR-02
  - INTR-03
  - INTR-04
  - INTR-05
  - INTR-06
  - INTR-07
  - INTR-08
  - INTR-09
  - BLDR-01
  - BLDR-02
  - BLDR-03
  - BLDR-04
  - BLDR-05
  - BLDR-06
  - BLDR-07
  - BLDR-08
  - SHAR-02
  - SHAR-03
  - SHAR-04

must_haves:
  truths:
    - "Claude has walked every one of the 57 checkpoints from RESEARCH.md §5 via Playwright MCP"
    - "Every walk result is logged in 10-02-WALK-LOG.md with status (pass/fix-inline/sub-phase-spawned/blocker)"
    - "Every checkpoint corresponding to a UAT runbook item has been marked [Claude-checked] in 10-UAT-RUNBOOK.md"
    - "All trivial fixes (<=10 LOC, no behavior change) were landed inline with atomic commits"
    - "All substantive fixes were split into decimal sub-phases (10.1, 10.2, ...) with their own PLAN.md"
    - "Fewer than 10 decimal sub-phases were spawned OR the user explicitly approved continuing past 5"
    - "Playwright MCP smoke pass for all 5 curated dashboards is green"
    - "Full test suite (vitest + playwright + pytest) is still green after all inline fixes"
    - "scripts/mock-audit.sh still exits 0"
    - "No error panels on any of the 5 curated dashboards at the time of walkthrough termination"
  artifacts:
    - path: ".planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md"
      provides: "Walk-by-walk log of every checkpoint, result, and fix action taken during the autonomous walkthrough"
      contains: "Checkpoint"
    - path: ".planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md"
      provides: "Updated UAT runbook with [Claude-checked] tags on every Claude-walked item and inline **Issue:** notes for anything unresolved"
      contains: "[Claude-checked]"
  key_links:
    - from: "10-UAT-RUNBOOK.md"
      to: "10-02-WALK-LOG.md"
      via: "Cross-reference between runbook items and walk log entries"
      pattern: "\\[Claude-checked\\]"
    - from: "Decimal sub-phases (10.1, 10.2, ...)"
      to: "10-02-WALK-LOG.md"
      via: "Walk log references each spawned sub-phase by number"
      pattern: "10\\.\\d"

user_setup: []
---

<objective>
Walk every page, every flow, and every interaction in the RecViz app via Playwright MCP against the curated seed from Plan 10-01. Exhaustively exercise the 57 checkpoints enumerated in RESEARCH.md §5. For each checkpoint: navigate, take screenshot, assert the per-dashboard acceptance criteria from RESEARCH.md §7 (no error panels, no stuck skeletons, content rendered, interactions work), log the result inline.

When a finding requires substantive fix work (>10 LOC or behavior change), spawn a decimal sub-phase (10.1, 10.2, ...) via the standard GSD insert-phase mechanism and let it run before continuing. Trivial fixes (<10 LOC, no behavior change) land inline with atomic commits.

Purpose: This is the autonomous pre-flight loop per D-12. It is the reason the Phase 10 catalog exists — to catch every regression against realistic data before the user starts manual UAT. Without this loop, the user's UAT becomes a bug-discovery session instead of a signoff.

Output: A fully walked codebase with all Claude-fixable issues resolved, an updated 10-UAT-RUNBOOK.md with [Claude-checked] markings, and a 10-02-WALK-LOG.md documenting every walk result. When this plan completes, Claude posts "ready for UAT" and hands off to Plan 10-03.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-CONTEXT.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-01c-SUMMARY.md

<!-- Plan 10-01c (closeout of the 10-01 split) must be complete and its summary must exist before Plan 10-02 starts -->

<!-- Playwright MCP is required — probe availability in Task 0 -->
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude (Playwright MCP) → localhost:5173 frontend → localhost:8000 backend → localhost:5432 Postgres | Full-stack walk on dev machine; same trust posture as manual dev testing |
| Claude → decimal sub-phase spawning | GSD insert-phase flow creates new PLAN.md directories; low risk but touches .planning/ state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering (T) | Playwright MCP state leaking between checkpoints (stale cookies, stale filter URL) | mitigate | Each checkpoint uses a fresh page navigation; no shared session state across independent checkpoints. URL-based state is exactly what we want to test. |
| T-10-09 | Denial of Service (D) | Decimal sub-phase explosion — Claude keeps spawning sub-phases for minor issues | mitigate | Hard ceiling of 10 per RESEARCH.md §6 Q-4. Soft warning at 5 sub-phases triggers a checkpoint. Triviality rule: <=10 LOC + no behavior change = inline, otherwise sub-phase. |
| T-10-10 | Information disclosure (I) | Walk log publishing screenshots containing user data | accept | Seed data is synthetic (no real PII). Screenshots written to the walk log are safe. |

All high-rated threats have mitigations or explicit rationales.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Confirm dev stack running + Playwright MCP availability + create walk log</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-01c-SUMMARY.md (confirm Plan 10-01c completed cleanly with full-stack verification)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5 (the 57 checkpoints — read end to end)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (the draft runbook from Plan 10-01c)
  </read_first>
  <action>
**M-1 FIX — Task 0 is now a checkpoint because the Bash tool cannot reliably background-spawn the dev stack.** The user must bring the dev stack up before this plan proceeds; Claude only probes + validates.

**Step 1 — Post the prompt to the user:**
```
Plan 10-02 needs the dev stack running before the autonomous walkthrough can start.

Please confirm these services are running (in separate terminals or via tmuxp/overmind):
  1. docker compose up -d           (postgres + redis + superset)
  2. superset run -p 8088            (Superset headless engine)
  3. cd backend && uvicorn app.main:app --reload   (FastAPI on :8000)
  4. cd frontend && pnpm dev         (Vite on :5173)

Once all four are up, type "yes" to proceed. Claude will probe each service and Playwright MCP, then create the walk log.

If any service is down, start it now, wait until it is healthy, then type "yes".
```

**Step 2 — After user types "yes", probe the services from Claude (no background spawning):**
```
curl -sf http://localhost:5173/ >/dev/null && echo "frontend up" || (echo "frontend DOWN" && exit 1)
curl -sf http://localhost:8000/health >/dev/null && echo "backend up" || (echo "backend DOWN" && exit 1)
curl -sf http://localhost:8088/health >/dev/null && echo "superset up" || (echo "superset DOWN" && exit 1)
docker compose ps | grep -E '(postgres|redis|superset)'
```

If any probe fails, STOP and re-prompt the user with the specific failure ("backend health check returned <status>, please verify uvicorn is running and the health endpoint is up").

**Step 3 — Verify seed state is present from Plan 10-01b/c:**
```
docker compose exec -T postgres psql -U postgres -d superset_meta -c "SELECT COUNT(*) FROM recviz_dashboards;"
docker compose exec -T postgres psql -U postgres -d superset_meta -c "SELECT id, name FROM recviz_dashboards ORDER BY id;"
docker compose exec -T postgres psql -U postgres -d recon_data -c "SELECT COUNT(*) FROM recon_transactions;"
```
Expected: 5 dashboards (all names prefixed `Phase 10 ·`), 100000 transactions.

If counts are wrong, STOP and re-prompt: "Seed state is missing — please run `python scripts/seed-postgres.py` before continuing".

**Step 4 — Probe Playwright MCP availability** (m-2 fix: no fallback, hard stop if unavailable):
Attempt to invoke the Playwright MCP tool via its canonical mcp__playwright__* name. Try a trivial navigation:
- Navigate to `http://localhost:5173/` via MCP
- Take a screenshot
- Confirm the navigation returned successfully

Write the result to a state file for downstream plans (Plan 10-03 Task 2 reads this per m-4 fix):
```
mkdir -p .planning/phases/10-comprehensive-testing-with-advanced-seed-data
cat > .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state <<EOF
mcp_available=true
probed_at=<ISO timestamp>
probed_by=plan-10-02-task-0
EOF
```

If Playwright MCP is NOT available, STOP. Per RESEARCH.md §Environment Availability, Phase 10 cannot proceed without Playwright MCP. Post this status and halt:
> "Playwright MCP not available. Phase 10 cannot proceed. Please install/enable Playwright MCP in the session and re-run Plan 10-02 Task 0."

Write `mcp_available=false` to the state file before halting.

**Step 5 — Create 10-02-WALK-LOG.md** with the template:
```markdown
# Plan 10-02 — Autonomous Walkthrough Log

**Started:** <timestamp>
**Plan:** 10-02
**Playwright MCP:** available
**Dev stack:** frontend 5173, backend 8000, superset 8088 — all up
**Seed state:** 100k transactions, 5 curated dashboards — verified

## Legend

- `PASS` — checkpoint passed without intervention
- `FIX-INLINE` — trivial fix (<=10 LOC, no behavior change), committed inline with commit hash
- `SUB-PHASE` — substantive fix spawned as decimal sub-phase (10.N)
- `BLOCKER` — issue that cannot be fixed in Phase 10 scope; logged to UAT runbook as P0/P1

## Walk log

(Checkpoint entries populated during Tasks 1-5. Each entry MUST have both a `### Checkpoint N: <path>` heading AND a `- Status:` line per M-2 fix.)

## Decimal sub-phases spawned

| Sub-phase | Title | Reason | Status |
|-----------|-------|--------|--------|

## Decimal sub-phase soft warning at 5

*If the sub-phase count reaches 5, pause here and post a status update, then continue.*

## Decimal sub-phase hard ceiling at 10

*If the sub-phase count reaches 10, STOP and ask the user whether to continue, rewind, or re-plan.*

## Termination

*Populated by Task 5 when Claude declares the build clean.*
```
  </action>
  <verify>
    <automated>curl -sf http://localhost:5173/ >/dev/null && curl -sf http://localhost:8000/health >/dev/null && test -f .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md && test -f .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state && grep -q "mcp_available=true" .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state</automated>
  </verify>
  <done>
User confirmed dev stack is up. Claude probed frontend/backend/superset and all returned healthy. Seed state verified (5 `Phase 10 ·`-prefixed dashboards, 100k transactions). Playwright MCP probed and confirmed available. MCP probe state file written. 10-02-WALK-LOG.md created with template.
  </done>
  <resume-signal>Type "yes" when docker compose, superset, uvicorn, and pnpm dev are all running.</resume-signal>
</task>

</task>

<task type="auto">
  <name>Task 1: Walk all 5 curated dashboards (checkpoints 1-13 from RESEARCH.md §5.1)</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.1 (checkpoints 1-13 — the dashboards area)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §7 (the 10 per-dashboard acceptance criteria)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (the Phase 2, Phase 02.1, and Phase 8 sections that correspond to dashboard walking)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md (append to this file as the walk proceeds)
  </read_first>
  <action>
**Walk every checkpoint in RESEARCH.md §5.1 (#1 through #13) via Playwright MCP in this exact order.** For each checkpoint:

1. Navigate to the URL
2. Take a full-page screenshot
3. Run the 10 acceptance criteria from RESEARCH.md §7:
   - Load: page loaded, h1 with dashboard name visible, no "Dashboard not found" fallback
   - No error panels: zero instances of `text=Column mapping error`, `text=Unsupported chart type`, `text=Failed to load`, `text=Data source not found`, `text=No data available`
   - No stuck skeletons: `[data-slot="skeleton"]` count = 0 after 15s
   - Chart content: each chart card has a `canvas` or `[_echarts_instance_]` or `.ag-charts-wrapper` element
   - KPI row populated: all KPIs show a numeric value
   - Cross-filter: click a segment on a cross-filter source chart, assert `text=Filtered by` bar appears
   - Drill down: double-click a drill-source chart, assert `text=Overview` breadcrumb appears
   - Filter bar: Shadcn Select `[data-slot="select-trigger"]` visible
   - Palette: Press `Meta+K`, verify palette opens, dashboard is findable by name
   - Screenshot: visually verify no broken rendering (blank areas, overflow, wrong colors)

4. Append to 10-02-WALK-LOG.md under "## Walk log". **M-2 fix: EVERY entry MUST have both a `### Checkpoint` heading and a `- Status:` line — the Task verify asserts both counts.** Format:
```
### Checkpoint 1: /dashboards
- Status: PASS
- Notes: <free text>
- Commit: <hash if FIX-INLINE>
- Sub-phase: <10.N if SUB-PHASE>
```
(Status MUST be one of: PASS, FIX-INLINE, SUB-PHASE, BLOCKER — exact string match.)

5. If PASS: Mark the corresponding UAT runbook item as `[Claude-checked]` by editing 10-UAT-RUNBOOK.md and changing `- [ ]` to `- [x] [Claude-checked]`.

6. If a fix is needed:
   - **Triviality test:** Is the fix <=10 LOC AND no behavior change? → FIX-INLINE.
     - Make the edit, run `cd frontend && pnpm vitest run <relevant test>` to confirm, run mock-audit.sh, commit with message `fix(10-02): <description>`, log the commit hash in walk log, re-walk the checkpoint.
   - **Otherwise:** SUB-PHASE.
     - Spawn a decimal sub-phase via `/gsd-insert-phase 10.N "<title>"` (or equivalent manual path): create `.planning/phases/10.N-<slug>/` directory with a PLAN.md scoped to the single fix, execute it, then return here and re-walk the checkpoint.
     - Log the sub-phase number + title in the walk log's "## Decimal sub-phases spawned" table.

**Checkpoints to walk (copy from RESEARCH.md §5.1):**
1. `/dashboards` — list loads, 5 curated dashboards visible, search filter works
2. `/dashboards/dash-sla` — all 10 acceptance criteria
3. `/dashboards/dash-sla?filter.region_code=NAM` — URL filter hydration
4. `/dashboards/dash-sla/edit` — BuilderPage opens
5. `/dashboards/dash-aging`
6. `/dashboards/dash-aging/edit`
7. `/dashboards/dash-match-rate`
8. `/dashboards/dash-match-rate/edit`
9. `/dashboards/dash-volume`
10. `/dashboards/dash-volume/edit`
11. `/dashboards/dash-breaks-summary`
12. `/dashboards/dash-breaks-summary/edit`
13. `/dashboards/new` — empty BuilderPage in create mode

**Sub-phase counter check:** After finishing this task, count the decimal sub-phases spawned so far. If count >= 5, post a status update to `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-DISCUSSION-LOG.md` summarizing what's been walked, fixed, and still broken. If count >= 10, STOP and raise the hard-ceiling checkpoint described in RESEARCH.md §6 Q-4.
  </action>
  <verify>
    <automated>COUNT=$(grep -c '^### Checkpoint' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); STATUS=$(grep -c '^- Status:' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); echo "checkpoints=$COUNT statuses=$STATUS"; test "$COUNT" -ge 13 && test "$STATUS" -ge 13</automated>
  </verify>
  <done>
All 13 checkpoints from §5.1 walked, each logged with a status in 10-02-WALK-LOG.md. UAT runbook updated with [Claude-checked] for passed items. Any sub-phases spawned are running clean. Sub-phase count below 10 (or user approved continuation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk charts + KPIs + datasets CRUD areas (checkpoints 14-27)</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.2 (checkpoints 14-18 — charts area)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.3 (checkpoints 19-23 — KPIs area)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.4 (checkpoints 24-27 — datasets area)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (the Phase 5, 6, 7 sections)
  </read_first>
  <action>
**Walk checkpoints 14-27 from RESEARCH.md §5.2, §5.3, §5.4 via Playwright MCP.** Same process as Task 1 — navigate, screenshot, assert, log, fix inline or spawn sub-phase.

**§5.2 Charts area (14-18):**
14. `/charts` — chart library list, 22 charts visible (user correction 2026-04-08: bullet, box-plot, sunburst EXCLUDED — all 18 working chart types covered), search + filter by chart type
15. `/charts/new` — chart builder wizard step 1 (dataset picker) loads
16. `/charts/chart-txn-trend-line/edit` — edit mode loads, preview renders
17. `/charts/chart-sla-heatmap/edit` — heatmap edits correctly
18. `/charts/chart-break-flow-sankey/edit` — ECharts sankey edits correctly

**§5.3 KPIs area (19-23):**
19. `/kpis` — KPI library list, 12 KPIs visible, search + filter
20. `/kpis/new` — KPI builder loads
21. `/kpis/kpi-match-rate/edit` — edit mode loads, preview card shows correct value, static_target trend renders
22. `/kpis/kpi-total-transactions/edit` — previous_period trend renders
23. `/kpis/kpi-avg-confidence/edit` — decimal format + no trend renders

**§5.4 Datasets area (24-27):**
24. `/datasets` — dataset library list, 16 datasets visible, search
25. `/datasets/new` — dataset creator loads (Monaco SQL editor)
26. `/datasets/ds-recon-transactions-daily/edit` — edit loads SQL + columns, preview runs against real data
27. `/datasets/ds-recon-transaction-detail/edit` — grid-source dataset edits

For each checkpoint: apply the Task 1 process (navigate → screenshot → assert → log → fix-inline-or-sub-phase → mark UAT runbook).

**Re-check sub-phase counter.** If crosses the 5 or 10 thresholds, follow the Task 1 protocol.
  </action>
  <verify>
    <automated>COUNT=$(grep -c '^### Checkpoint' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); STATUS=$(grep -c '^- Status:' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); echo "checkpoints=$COUNT statuses=$STATUS"; test "$COUNT" -ge 27 && test "$STATUS" -ge 27</automated>
  </verify>
  <done>
Checkpoints 14-27 walked, each logged. UAT runbook updated. Sub-phase count tracked and below ceiling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Walk explorer + settings + embed + command palette + share link (checkpoints 28-42)</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.5 (checkpoint 28 — explorer)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.6 (checkpoint 29 — settings)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.7 (checkpoints 30-34 — embed mode)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.8 (checkpoints 35-40 — command palette)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.9 (checkpoints 41-42 — share link)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (Phase 1, Phase 4, Phase 9 sections)
  </read_first>
  <action>
**Walk checkpoints 28-42 via Playwright MCP.**

**§5.5 Explorer (28):**
28. `/explorer` — Monaco loads, schema browser shows 8 dim + 4 fact tables, run `SELECT COUNT(*) FROM recon_transactions` works, query history shows the run

**§5.6 Settings (29):**
29. `/settings` — theme toggle visible, data-sources section visible with 4 logical databases

**§5.7 Embed mode (30-34):**
30. `/embed/dashboards/dash-sla` — chromeless, topbar shows "Open in RecViz", data renders
31. `/embed/dashboards/dash-sla?theme=dark` — dark mode applied to html tag
32. `/embed/dashboards/dash-sla?filter.region_code=EMEA` — filter pre-applied
33. `/embed/dashboards/dash-sla?filter.lock=region_code` — region filter is disabled
34. `/embed/dashboards/dash-sla?hide=filter-bar,title,toolbar` — all three surfaces hidden

**§5.8 Command palette (35-40):**
35. Press `Meta+K` anywhere — palette opens, placeholder reads "Search dashboards, charts, datasets, KPIs..."
36. Type "SLA" — `SLA Overview` appears under "Dashboards" group
37. Type "Match" — Match Rate chart + Match Rate Tracker dashboard + Match Rate KPI appear in correct group order
38. Press Enter on a chart result — navigates to `/charts/{id}/edit`
39. Press Enter on a dataset result — navigates to `/datasets/{id}/edit`
40. Press Enter on a KPI result — navigates to `/kpis/{id}/edit`

**§5.9 Share link (41-42):**
41. On a dashboard, apply a filter — URL updates with `filter.region_code=NAM`
42. Click "Share" button — toast shows "Link copied", clipboard contains the URL

Apply the Task 1 process (navigate → screenshot → assert → log → fix-inline-or-sub-phase → mark UAT runbook) to each.
  </action>
  <verify>
    <automated>COUNT=$(grep -c '^### Checkpoint' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); STATUS=$(grep -c '^- Status:' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); echo "checkpoints=$COUNT statuses=$STATUS"; test "$COUNT" -ge 42 && test "$STATUS" -ge 42</automated>
  </verify>
  <done>
Checkpoints 28-42 walked, each logged. UAT runbook updated. Sub-phase count tracked.
  </done>
</task>

<task type="auto">
  <name>Task 4: Walk dashboard interactions + reports + SQL execute (checkpoints 43-57)</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.10 (checkpoints 43-54 — dashboard interaction)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §5.11 (checkpoints 55-57 — reports + SQL + data sources)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (Phase 2, Phase 3 sections)
  </read_first>
  <action>
**Walk checkpoints 43-57 via Playwright MCP.**

**§5.10 Dashboard interaction (43-54):**
43. Click a chart segment on `dash-volume` — Cross-filter bar appears, "Filtered by: ..."
44. Click "Clear all" on cross-filter bar — bar disappears, charts reset
45. Double-click a bar chart on `dash-volume` — drill breadcrumb appears, detail level loads
46. Click "Overview" in drill breadcrumb — returns to top of drill
47. Click fullscreen icon on a chart — chart opens in fullscreen modal
48. Click chart export → PNG — file downloads
49. Click chart export → CSV — file downloads
50. Click chart export → Copy to clipboard — toast "Copied to clipboard"
51. Click grid export → CSV — file downloads
52. Click grid export → Excel — file downloads
53. Click manual refresh button on dashboard — skeletons flash, data re-renders
54. Wait for auto-refresh (hard cap 60s) — data re-fetches automatically. **m-5 fix:** do NOT skip to manual UAT. If the 60s cap is hit without an auto-refresh event, log the checkpoint as `PASS` with a note `auto-refresh not observed within 60s cap — flag for manual walk` but still assert no regression (no console errors, no error banners during the wait).

**§5.11 Reports + SQL + data sources (55-57):**
55. `/reports` — shows "Coming soon" empty state (per D-17, exempt from data audit) — verify it's an honest Shadcn Empty component
56. `/explorer` — run `SELECT COUNT(*) FROM recon_transactions` — returns 100000
57. `/settings` → data sources — all 4 logical databases show "Connected" status

Apply the Task 1 process to each.

**Final sub-phase counter check.** If any sub-phase is still running, wait for it to complete and re-walk the affected checkpoint before proceeding to Task 5.
  </action>
  <verify>
    <automated>COUNT=$(grep -c '^### Checkpoint' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); STATUS=$(grep -c '^- Status:' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md); echo "checkpoints=$COUNT statuses=$STATUS"; test "$COUNT" -ge 57 && test "$STATUS" -ge 57</automated>
  </verify>
  <done>
All 57 checkpoints walked, each logged in 10-02-WALK-LOG.md. UAT runbook updated. All sub-phases that were spawned are complete.
  </done>
</task>

<task type="auto">
  <name>Task 5: Loop back — re-walk any failed checkpoints after fixes, declare clean or re-spawn</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md (scan for any SUB-PHASE / BLOCKER entries)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (verify all items have been touched)
  </read_first>
  <action>
**Step 1 — Inspect the walk log.** Scan 10-02-WALK-LOG.md for any checkpoint entries with status `FIX-INLINE`, `SUB-PHASE`, or `BLOCKER`. List each:
- FIX-INLINE: confirm the commit landed and the checkpoint was re-walked with PASS status
- SUB-PHASE: confirm the sub-phase completed (execute-phase the sub-phase if it hasn't run) and re-walk the checkpoint
- BLOCKER: confirm it's logged as a P0 or P1 finding in the UAT runbook with a clear description

**Step 2 — Re-run the full test suite.** After any inline fixes or sub-phase completions, re-verify:
```
cd frontend && pnpm vitest run
cd frontend && pnpm exec playwright test --reporter=list
cd backend && python -m pytest tests/ -v
bash scripts/mock-audit.sh
```
All must be green. If any test fails, treat it as a regression from the walkthrough fixes and diagnose.

**Step 3 — Second-pass walk of the 5 curated dashboards.** Via Playwright MCP, re-walk:
- `/dashboards/dash-sla`
- `/dashboards/dash-aging`
- `/dashboards/dash-match-rate`
- `/dashboards/dash-volume`
- `/dashboards/dash-breaks-summary`

Assert the 10 acceptance criteria from RESEARCH.md §7 PASS without any new issues found. This is the gate: if any dashboard fails here, loop back to a sub-phase. If all 5 pass, Claude is ready to declare the build clean.

**Step 4 — Write the termination section of 10-02-WALK-LOG.md:**
```markdown
## Termination

**Terminated:** <timestamp>
**Total checkpoints walked:** 57
**PASS count:** <n>
**FIX-INLINE count:** <n>
**SUB-PHASE count:** <n>
**BLOCKER count:** <n>

**Sub-phases spawned:**
- 10.1 <title> — status
- 10.2 <title> — status
...

**Final test suite status:**
- vitest: <PASS/FAIL>
- playwright: <PASS/FAIL>
- pytest: <PASS/FAIL>
- mock-audit.sh: <CLEAN/DIRTY>

**Second-pass dashboard walk:**
- dash-sla: PASS/FAIL
- dash-aging: PASS/FAIL
- dash-match-rate: PASS/FAIL
- dash-volume: PASS/FAIL
- dash-breaks-summary: PASS/FAIL

**Claude declares:** <BUILD CLEAN, ready for Plan 10-03 / NEEDS USER INTERVENTION>

**Open BLOCKERs (if any):** <list each with rationale for not fixing in Phase 10 scope>
```
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run && pnpm exec playwright test --reporter=list && cd ../backend && python -m pytest tests/ && cd .. && bash scripts/mock-audit.sh && grep -q "BUILD CLEAN" .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md</automated>
  </verify>
  <done>
All tests green. mock-audit.sh clean. Second-pass dashboard walk PASS on all 5 dashboards. Walk log termination section written with Claude's "BUILD CLEAN" declaration. Any remaining BLOCKERs are documented with clear rationale.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Ready for UAT handoff — user reviews walk log + decides whether to proceed to Plan 10-03</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md (the full walk history from Tasks 1-5)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (the runbook after Claude-check tagging)
  </read_first>
  <action>
This is the Plan 10-02 handoff checkpoint. Post to the user a summary of the autonomous walkthrough with concrete next-step instructions. Exact deliverables to reference:
- All 57 checkpoints from RESEARCH.md §5 walked via Playwright MCP
- Every Claude-fixable issue addressed (either inline or via decimal sub-phases)
- 10-02-WALK-LOG.md contains full walk history + termination declaration
- 10-UAT-RUNBOOK.md updated with [Claude-checked] tags on every walked item
- Any remaining BLOCKERs documented in the walk log with rationale

Ask the user to perform these review steps:

1. Read 10-02-WALK-LOG.md end-to-end, especially the termination section
2. Review the list of decimal sub-phases spawned (should be < 10, ideally < 5)
3. Scan 10-UAT-RUNBOOK.md for items still marked `[ ]` (not Claude-checked) — these are the items the user MUST manually walk
4. Scan for any `**Issue:**` notes inline in the runbook — these are findings that need user attention
5. Confirm the full test suite is still green:
   ```
   cd frontend && pnpm vitest run
   cd frontend && pnpm exec playwright test
   cd backend && python -m pytest tests/
   bash scripts/mock-audit.sh
   ```
6. Browse the 5 curated dashboards manually in the browser to double-check Claude's second-pass walk claim:
   - http://localhost:5173/dashboards/dash-sla
   - http://localhost:5173/dashboards/dash-aging
   - http://localhost:5173/dashboards/dash-match-rate
   - http://localhost:5173/dashboards/dash-volume
   - http://localhost:5173/dashboards/dash-breaks-summary

Decision: accept the walkthrough and proceed to Plan 10-03 (UAT finalization), or request more sub-phases / specific fixes.
  </action>
  <verify>
    <automated>MISSING — this is a human verification checkpoint. User reviews walk log + runbook and posts decision.</automated>
  </verify>
  <done>
User types "approved" → proceed to Plan 10-03. User types "loop N.M" → spawn another decimal sub-phase. User describes specific issues → Claude fixes them and re-walks the affected checkpoints before re-asking for approval.
  </done>
  <resume-signal>Type "approved" to proceed to Plan 10-03, "loop N.M" to spawn another decimal sub-phase for a specific fix, or describe what still needs attention.</resume-signal>
</task>

</tasks>

<verification>
Plan 10-02 is complete when:

1. Task 0 verified dev stack + Playwright MCP availability
2. Tasks 1-4 walked all 57 checkpoints from RESEARCH.md §5 via Playwright MCP
3. Every walk result is logged in 10-02-WALK-LOG.md
4. Every [Claude-checked] item in 10-UAT-RUNBOOK.md corresponds to a logged PASS checkpoint
5. All inline fixes have atomic commits
6. All decimal sub-phases have completed (each has its own PLAN.md and commit history)
7. Sub-phase count < 10 (OR user approved continuation)
8. Task 5 second-pass dashboard walk PASS on all 5 curated dashboards
9. Full test suite green, mock-audit.sh clean
10. Task 6 user approval received
</verification>

<success_criteria>
After Plan 10-02:
- The build is verified clean against the curated seed via autonomous Playwright MCP walkthrough
- Every Claude-fixable issue is resolved (inline or via decimal sub-phases)
- The UAT runbook is mostly pre-checked by Claude, leaving only manual-UAT items for the user
- The walk log provides a complete audit trail of what was tested, found, and fixed
- The build is ready for Plan 10-03 (UAT finalize + handoff) and then user manual regression
</success_criteria>

<output>
After completion, create `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-SUMMARY.md` documenting:

- Number of checkpoints walked (expect 57 if no early termination)
- PASS / FIX-INLINE / SUB-PHASE / BLOCKER breakdown
- List of decimal sub-phases spawned with titles
- Final test suite status (pytest, vitest, playwright counts)
- Second-pass dashboard walk results (5 dashboards)
- Declared "BUILD CLEAN" or list of remaining blockers with rationale
- Handoff signal for Plan 10-03
</output>
