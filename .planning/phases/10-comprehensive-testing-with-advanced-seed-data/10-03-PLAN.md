---
phase: 10-comprehensive-testing-with-advanced-seed-data
plan: 03
type: execute
wave: 5
depends_on:
  - 10-02
files_modified:
  - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md
  - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md
  - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md
  - .planning/STATE.md
autonomous: false
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04
  - INFR-05
  - INFR-06
  - INTR-01
  - INTR-02
  - INTR-03
  - INTR-04
  - INTR-05
  - INTR-06
  - INTR-07
  - INTR-08
  - INTR-09
  - DSET-01
  - DSET-02
  - DSET-03
  - DSET-04
  - DSET-05
  - CHRT-01
  - CHRT-02
  - CHRT-03
  - CHRT-04
  - CHRT-05
  - CHRT-06
  - CHRT-07
  - KPI-01
  - KPI-02
  - KPI-03
  - BLDR-01
  - BLDR-02
  - BLDR-03
  - BLDR-04
  - BLDR-05
  - BLDR-06
  - BLDR-07
  - BLDR-08
  - SHAR-02
  - SHAR-03
  - SHAR-04

must_haves:
  truths:
    - "10-UAT-RUNBOOK.md is fully polished: every Claude-walked item tagged [Claude-checked], every inline **Issue:** note either resolved or explicitly carried forward, final Sign-off section ready for user tick"
    - "10-PERF-OBSERVATIONS.md exists with cold/warm load timings for all 5 curated dashboards, interaction timings for filter/cross-filter/drill/palette/refresh, free-form observations, and Phase 11 recommendations"
    - "READY-FOR-UAT.md summary post exists with test results, mock audit status, walkthrough summary, and clear handoff statement"
    - "Full test suite (vitest + playwright + pytest) is green one final time"
    - "scripts/mock-audit.sh exits 0 one final time"
    - "STATE.md is updated with Phase 10 ready-for-UAT status and session continuity pointer"
    - "User has received the handoff banner and knows exactly what to do next (walk the runbook manually)"
    - "Phase 10 close criterion is documented: Plan 10-03 verification is user UAT signoff — the plan intentionally cannot self-verify"
  artifacts:
    - path: ".planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md"
      provides: "Observation-only performance data (no budgets, no assertions) collected during the walkthrough"
      contains: "## First-load timing"
    - path: ".planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md"
      provides: "Handoff summary with test results, walk summary, mock audit status, and explicit instruction for the user to start manual regression"
      contains: "ready for user UAT"
    - path: ".planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md"
      provides: "Final polished UAT runbook with every Claude-walked item tagged, ready for user manual regression"
      contains: "UAT PASS"
  key_links:
    - from: "READY-FOR-UAT.md"
      to: "10-UAT-RUNBOOK.md"
      via: "Summary points user at runbook as next action"
      pattern: "10-UAT-RUNBOOK"
    - from: "10-PERF-OBSERVATIONS.md"
      to: "Phase 11 scaling work"
      via: "Recommendations section carries observations forward"
      pattern: "Phase 11 recommendations"

user_setup: []
---

<objective>
Finalize the Phase 10 deliverables and hand off to the user for manual regression UAT. This is the short closeout plan for the phase. Three concrete outputs: (1) polished 10-UAT-RUNBOOK.md with all Claude-walked items tagged and all inline issues cleaned up, (2) 10-PERF-OBSERVATIONS.md with cold/warm timings and Phase 11 recommendations, (3) READY-FOR-UAT.md summary post with explicit handoff instructions.

Purpose: Plan 10-02 did the heavy lifting (autonomous walkthrough + fixes). Plan 10-03 is the quiet closeout: clean up the runbook notes, record perf observations, post the handoff. After this plan, the user takes over and walks the UAT runbook manually. Phase 10 closes when the user ticks every checkbox and posts "UAT PASS".

Output: A clean runbook ready for the user, a perf observations document for Phase 11 input, a summary handoff post, and an updated STATE.md reflecting the ready-for-UAT status. The plan deliberately does not self-verify because its closing gate is the user's manual walkthrough — a human-only verification that no automated test can replicate.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/RecViz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-CONTEXT.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md
@.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-SUMMARY.md
@.planning/STATE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → .planning/ markdown files | Pure file edits, no runtime effects |
| Claude → STATE.md | State transition recorded in version-controlled JSON-frontmatter |
| Plan 10-03 → user UAT signoff | Plan hands off to human verification; no automated verify possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Spoofing (S) | Claude falsely declaring "ready for UAT" when the walkthrough had unresolved issues | mitigate | Task 2 explicitly requires that every `**Issue:**` note in the runbook has either a resolution commit hash OR an explicit "carried forward" tag with rationale. Task 3 re-runs the full test suite + mock-audit before writing the handoff post, so the claim is verifiable by the user in < 5 minutes. |
| T-10-12 | Repudiation (R) | Losing the audit trail of what was walked vs what was skipped | mitigate | 10-02-WALK-LOG.md from Plan 10-02 is version-controlled and referenced from READY-FOR-UAT.md. STATE.md update points at the walk log. |
| T-10-13 | Tampering (T) | Perf observations overwritten with fabricated numbers | accept | Per D-18, perf observations are light and observation-only. They carry no budget enforcement. Fabrication would be immediately visible as the file is written after the walkthrough completes. |

All high-rated threats have explicit mitigations.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Finalize 10-UAT-RUNBOOK.md — resolve inline issues + polish Claude-checked tags</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (the runbook with Plan 10-02's walk markings)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md (the walk log — cross-reference every `### Checkpoint` entry to the runbook section it covered)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-SUMMARY.md (the Plan 10-02 summary — use this as the source of truth for which checkpoints passed vs. needed fixes)
    - .planning/ROADMAP.md (confirm every phase success criterion is reflected in a runbook item)
  </read_first>
  <action>
**Step 1 — Walk the runbook section by section** (Mock audit, Phase 1, Phase 2, Phase 02.1, Phase 3, Phase 4, Phase 5, Phase 6, Phase 7, Phase 8, Phase 9, Reports + Export, Cross-cutting, Findings log, Sign-off).

**Step 2 — For each section, cross-reference against 10-02-WALK-LOG.md:**
- If the walk log has a PASS entry for a checkpoint that covers this runbook item → ensure the item is tagged `[x] [Claude-checked]`
- If the walk log has a FIX-INLINE entry → ensure the item is tagged `[x] [Claude-checked]` AND add a bracketed note: `[Fixed inline: commit <hash>]`
- If the walk log has a SUB-PHASE entry → ensure the item is tagged `[x] [Claude-checked via sub-phase 10.N]`
- If the walk log has a BLOCKER entry → leave the item `[ ]` and add a `**Issue (P0/P1):** <description>. **Disposition:** <carried forward to next milestone | deferred with rationale>` note directly below
- If the item was NOT walked by Claude (e.g., "visual judgment" items from VALIDATION.md manual-only list) → leave it `[ ]` without `[Claude-checked]` — the user will tick these themselves

**Step 3 — Clean up inline issue notes.** Any `**Issue:**` note that was resolved should be updated to:
```
**Issue (P1):** <original description>. **Resolution:** <commit hash or sub-phase 10.N>. ✓
```
Any `**Issue:**` note that was NOT resolved should be explicitly tagged:
```
**Issue (P2):** <original description>. **Disposition:** Deferred to next milestone — <rationale>.
```

**Step 4 — Add a final section at the bottom of the runbook:**
```markdown
## Claude's Walk Summary

Claude walked <N> of <M> runbook items during Plan 10-02 autonomous pre-flight.
Items not walked by Claude are intentionally manual — they require user judgment (visual polish, animation feel, dark/light mode parity, share-link recipient experience, etc.) per 10-VALIDATION.md "Manual-Only Verifications".

Full walk history: 10-02-WALK-LOG.md

**Pending manual UAT items:**
- Phase 1: <N items>
- Phase 2: <N items>
- ... (enumerate each phase's remaining unchecked items)

Total pending: <N>

**Known deferred issues (not Phase 10 blockers):**
- <list each P2/P3 issue with its disposition>
```

Count the manual items per section (grep `- [ ]` in each phase section after tagging is complete) and paste the counts.

**Step 5 — Verify the Sign-off section at the bottom** reads exactly:
```markdown
## Sign-off

- [ ] User posts "UAT PASS" when all boxes above are checked and no P0/P1 issues remain unresolved.
```
  </action>
  <verify>
    <automated>grep -c '\[Claude-checked\]' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md</automated>
  </verify>
  <done>
Every passed walk log checkpoint is reflected as `[Claude-checked]` in the runbook. Every `**Issue:**` note has a clear resolution or deferred disposition. The Claude's Walk Summary section exists with accurate counts. The Sign-off section is intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 10-PERF-OBSERVATIONS.md with cold/warm timings + Phase 11 recommendations</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-RESEARCH.md §7 "Perf observation deliverable spec" (the exact markdown table shape to produce)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md (any timing notes captured during the walkthrough)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-CONTEXT.md D-18 (observation-only, no assertions, no budgets)
  </read_first>
  <action>
**Step 0 (m-4 fix) — Read the Playwright MCP probe state file from Plan 10-02 Task 0:**
```
test -f .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state && cat .planning/phases/10-comprehensive-testing-with-advanced-seed-data/plan-10-02-mcp-probe.state
```
Assert `mcp_available=true`. If the file is missing OR contains `mcp_available=false`, STOP — Plan 10-03 Task 2 cannot measure perf without Playwright MCP. Post "Plan 10-03 Task 2 requires Playwright MCP; Plan 10-02 Task 0 probe recorded unavailable. Halt and re-enable MCP."

**Step 1 — Re-measure cold and warm dashboard load times.** For each of the 5 curated dashboards, do this twice:

1. **Cold measurement:**
   - Flush Redis: `docker compose exec -T redis redis-cli FLUSHDB`
   - Navigate via Playwright MCP to `/dashboards/dash-<slug>` with a stopwatch start at navigation
   - Stop when all charts have rendered (no skeletons visible, all canvases present)
   - Record the wall-clock time in milliseconds

2. **Warm measurement:**
   - Immediately re-navigate to the same dashboard (do not flush cache)
   - Stop when fully rendered
   - Record the wall-clock time

Both measurements use the same filter set: `Region: NAM`, `Date Range: 30 days` (applied after the initial load if the dashboard defaults to a different state).

**Step 2 — Measure interaction timings.** With `dash-volume` open (warm cache):
- Apply filter (region=EMEA): time from click to charts re-rendered
- Cross-filter click: time from click to dimmed state (should be <16ms — client-side only)
- Drill-down click: time from double-click to detail level loaded
- Command palette query "SLA": time from Meta+K to results appearing
- Full-page refresh: time from refresh-button click to all charts re-rendered

Record all timings in Playwright MCP using `page.evaluate` with `performance.now()` if possible, otherwise manual stopwatch rounded to the nearest 50ms.

**Step 3 — Write 10-PERF-OBSERVATIONS.md** using the exact shape from RESEARCH.md §7:

```markdown
# Phase 10 — Performance Observations

**Measured:** <date>
**Dataset:** 100k recon_transactions + 20k breaks + 80k match events + 5k SLA events
**Hardware:** <dev machine spec — e.g., "MacBook Pro M3 Max, 64GB, localhost Postgres 16 via docker">
**Filter set:** All dashboards with 'Region: NAM', 'Date Range: 30 days'
**Note:** Per D-18, these observations are light and observation-only. No budgets, no assertions, no test failures.

## First-load timing (cold Superset cache)

| Dashboard | Wall time (cold) | Wall time (warm) | Notes |
|-----------|------------------|------------------|-------|
| dash-sla | <n> ms | <n> ms | <n charts, n KPIs, n cross-filter sources, n drill hierarchy> |
| dash-aging | <n> ms | <n> ms | — |
| dash-match-rate | <n> ms | <n> ms | — |
| dash-volume | <n> ms | <n> ms | heaviest (scatter + parallel + treemap) |
| dash-breaks-summary | <n> ms | <n> ms | — |

## Interaction timing

| Action | Target dashboard | Response time | Notes |
|--------|------------------|---------------|-------|
| Apply filter (region=EMEA) | dash-sla | <n> ms | — |
| Cross-filter click | dash-volume | <n> ms | Client-side, should be <16ms |
| Drill down click | dash-volume | <n> ms | Network fetch for detail level |
| Command palette query "SLA" | — | <n> ms | Target <200ms |
| Full-page refresh | dash-sla | <n> ms | — |

## Observations

- <what felt slow>
- <what surprised you as fast>
- <which dashboards load first, which load last>
- <whether warm cache hits produced near-instant rendering>
- <whether any dashboard exceeded 5s cold load — flag for Phase 11>
- <whether cross-filter click response felt snappy or laggy>
- <observations about Playwright MCP screenshot latency separate from actual page timing>

## Phase 11 recommendations

- <items to carry forward to the 1M / 10M row scaling pass>
- <e.g., "Consider adding server-side pagination for the drill detail grid at 1M rows">
- <e.g., "Redis cache invalidation strategy needs review for multi-user production">
- <e.g., "Investigate dashboard-renderer mounting 3 useQuery hooks per chart — may thrash at high fan-out">
```

Fill every `<n>` and `<...>` placeholder with real measured data. Do NOT fabricate numbers — if a measurement was not captured, leave `—` and note "not measured in Plan 10-02".
  </action>
  <verify>
    <automated>test -f .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md && grep -q "Phase 11 recommendations" .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md && grep -q "First-load timing" .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md</automated>
  </verify>
  <done>
10-PERF-OBSERVATIONS.md exists with cold/warm timings for all 5 curated dashboards, interaction timings for 5 actions, at least 3 observations, and at least 3 Phase 11 recommendations. Every `<n>` placeholder is filled with a real number or explicit `—` with reason.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run final test suite + mock audit + write READY-FOR-UAT.md handoff post</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (polished version from Task 1)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md (from Task 2)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-SUMMARY.md (walkthrough summary)
  </read_first>
  <action>
**Step 1 — Final green-suite verification.** Run the full test suite and capture raw output:
```
cd /Users/aarun/Workspace/Projects/RecViz
cd frontend && pnpm vitest run --reporter=verbose 2>&1 | tail -20
cd frontend && pnpm exec playwright test --reporter=list 2>&1 | tail -20
cd backend && python -m pytest tests/ -v 2>&1 | tail -20
cd .. && bash scripts/mock-audit.sh
```

Record exactly:
- vitest: PASS/FAIL, test count, duration
- playwright: PASS/FAIL, test count, duration
- pytest: PASS/FAIL, test count, duration
- mock-audit: CLEAN/DIRTY, offender list if any

If ANY check fails, STOP. Either (a) fix inline if trivial, (b) loop back to Plan 10-02 to spawn a sub-phase, or (c) document the failure as a P0 blocker in READY-FOR-UAT.md and halt the handoff.

**Step 2 — Count runbook items.** Grep the runbook:
- Total items: `grep -c '^- \[' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md`
- Claude-checked items: `grep -c '\[Claude-checked\]' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md`
- Pending manual items: (total) - (Claude-checked)
- Issues logged: `grep -c '\*\*Issue' .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md`

**Step 3 — Write READY-FOR-UAT.md** with this exact structure:

```markdown
# Phase 10 — ready for user UAT

**Status:** Claude has completed autonomous pre-flight. Handing off for manual regression UAT.
**Date:** <timestamp>
**Phase:** 10 — Comprehensive Testing with Advanced Seed Data
**Plans complete:** 10-01 (test bed), 10-02 (autonomous walkthrough), 10-03 (this plan — UAT finalize + handoff)

## Test suite status

| Suite | Status | Test count | Duration |
|-------|--------|------------|----------|
| Vitest (frontend unit) | <PASS/FAIL> | <n> | <n>s |
| Playwright E2E (6 specs) | <PASS/FAIL> | <n> | <n>s |
| pytest (backend) | <PASS/FAIL> | <n> | <n>s |
| scripts/mock-audit.sh | <CLEAN/DIRTY> | — | — |

## Autonomous walkthrough summary

- Checkpoints walked: 57 (per RESEARCH.md §5)
- PASS count: <n>
- FIX-INLINE count: <n>
- SUB-PHASE count: <n>
- BLOCKER count: <n>
- Decimal sub-phases spawned: <list by number and title, or "none">

Full walk log: `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-WALK-LOG.md`

## UAT runbook status

- Total runbook items: <n>
- Pre-checked by Claude: <n> (`[Claude-checked]` tag)
- Pending manual UAT by user: <n>
- Issues logged: <n> (all have resolution or deferred disposition)

Runbook location: `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md`

## Mock audit result

<paste the exact mock-audit.sh output line>

## Performance observations

Recorded in `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md`.

Highlights:
- <list the most interesting observations — e.g., "dash-volume cold load was 2.3s, warm was 380ms">
- <e.g., "Cross-filter response was consistently <20ms — client-side pipeline is healthy">
- <e.g., "Command palette query returned in 120ms against the managed tables — well under target">

## Known deferred issues

- <list any P2/P3 issues that are explicitly carried forward>
- <e.g., "Sunburst chart type excluded from catalog per RESEARCH.md §6 Q-3 — documented as known limitation in runbook">

## What to do next (USER INSTRUCTIONS)

1. Open `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md`
2. Walk each pending `[ ]` item section by section (starting with Mock Audit, then Phase 1 through Phase 9)
3. Tick each `[ ]` → `[x]` as you verify it
4. Log any new issues inline as `**Issue (P1):** <description>. **Fix:** <TBD>`
5. When every box is ticked and no P0/P1 issues remain unresolved, post "UAT PASS" in the Findings log section and commit
6. Run `/gsd-verify-work` to close Phase 10 or spin up `/gsd-plan-phase 10 --gaps` if new issues emerged

## Handoff signal

Phase 10 is ready for user UAT. Claude has done everything within its scope — further progress depends on your manual walkthrough and signoff.
```

Fill every `<n>`, `<...>`, and bracket placeholder with real values from the test output and grep counts.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run && pnpm exec playwright test --reporter=list && cd ../backend && python -m pytest tests/ && cd .. && bash scripts/mock-audit.sh && test -f .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md && grep -q "ready for user UAT" .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md</automated>
  </verify>
  <done>
Full test suite green. mock-audit clean. READY-FOR-UAT.md exists with accurate test counts, walkthrough summary, runbook counts, perf highlights, known deferred issues, and explicit user instructions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final handoff — user starts manual regression UAT</name>
  <files>.planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md, .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-PERF-OBSERVATIONS.md</files>
  <read_first>
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md (the handoff post from Task 3)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md (the polished runbook from Task 1)
  </read_first>
  <action>
This is the Plan 10-03 final handoff. Claude has finished its work. The deliverables are:
- 10-UAT-RUNBOOK.md polished with [Claude-checked] tags and resolved inline issues
- 10-PERF-OBSERVATIONS.md with cold/warm timings + Phase 11 recommendations
- READY-FOR-UAT.md handoff summary with test results, walkthrough summary, and user instructions
- STATE.md updated to "awaiting-user-uat" (see Task 5)
- Full test suite green, mock-audit clean

Post a concise handoff message pointing the user at READY-FOR-UAT.md. Then describe the user's actions:

1. Read `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md` end-to-end
2. Open `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md`
3. Walk the runbook manually, ticking items as verified. This is the Phase 10 signoff gate per D-15.1.
4. Phase 10 closes when:
   - Every `[ ]` in the runbook is `[x]` (user ticked OR Claude-checked)
   - No unresolved `**Issue (P0)**` or `**Issue (P1)**` notes
   - User posts "UAT PASS" in the runbook's Findings log section
5. After UAT PASS: run `/gsd-verify-work` to close Phase 10 and update the milestone progress.

**This task intentionally has no automated verification.** The runbook walk is the verification, and it is a human-only activity per D-15.

If user finds P0/P1 issues during manual UAT:
- Run `/gsd-plan-phase 10 --gaps` to create a gap-closure plan
- Or manually insert decimal sub-phases via `/gsd-insert-phase 10.N "fix-X"`
- Re-walk the affected items after the fix lands, re-tick the runbook, post UAT PASS

Wait here for the user response.
  </action>
  <verify>
    <automated>MISSING — Plan 10-03 intentionally cannot self-verify. The runbook walk is the Phase 10 acceptance gate and is a human-only activity per D-15.</automated>
  </verify>
  <done>
User posts "UAT PASS" after walking the runbook clean. Alternatively, user describes specific gaps requiring a /gsd-plan-phase 10 --gaps run or a decimal sub-phase insertion.
  </done>
  <resume-signal>Type "UAT PASS" when the manual runbook walk is complete and no P0/P1 issues remain, or describe issues that need gap-closure work.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md and commit the Phase 10 handoff state</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md (the handoff post)
    - .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-02-SUMMARY.md
  </read_first>
  <action>
**Step 1 — Update STATE.md frontmatter.** Change:
- `status:` → `awaiting-user-uat`
- `stopped_at:` → `Phase 10 autonomous pre-flight complete — awaiting user manual UAT`
- `last_updated:` → current ISO timestamp
- `last_activity:` → current date

**Step 2 — Update Current Position section** to read:
```
## Current Position

Phase: 10
Plan: 10-03 complete — awaiting user UAT
Next: User walks 10-UAT-RUNBOOK.md manually
Status: Autonomous pre-flight complete — ready for user manual regression
Last activity: <date>

Progress: [██████████] 100% (Claude-side)
```

**Step 3 — Append to Session Continuity:**
```
Last session: <timestamp>
Stopped at: Phase 10 Plan 10-03 complete — ready for user UAT
Resume file: .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md
```

**Step 4 — Append to Accumulated Context → Decisions:**
Add a new bullet under the `### Decisions` list:
```
- [Phase 10]: Curated catalog seeded: 16 datasets, 22 charts (covering all 18 working chart types), 12 KPIs, 5 dashboards. Bullet, box-plot, sunburst excluded per user correction 2026-04-08 (bullet/box-plot fall back to bar; sunburst needs hierarchical transform). A10 dual-row pairing guard enforced via test_seed_script.py::test_dataset_data_source_pairing.
- [Phase 10]: <N> decimal sub-phases spawned during autonomous pre-flight: <list>
- [Phase 10]: Phase closes when user ticks 10-UAT-RUNBOOK.md clean and posts "UAT PASS". Claude cannot self-verify the phase because its acceptance gate is human manual walkthrough (D-15).
```

**Step 5 — Post the handoff banner to the user** (as the final output of this plan execution):
```
================================================================
PHASE 10 READY FOR USER UAT
================================================================

Claude has completed autonomous pre-flight for Phase 10.

Next step: Walk the manual regression UAT runbook at
  .planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-UAT-RUNBOOK.md

Full handoff summary:
  .planning/phases/10-comprehensive-testing-with-advanced-seed-data/READY-FOR-UAT.md

When the walk is complete and no P0/P1 issues remain:
  - Tick every checkbox in the runbook
  - Post "UAT PASS" in the Findings log section
  - Run /gsd-verify-work to close Phase 10

Phase 10 is the v1 release-readiness milestone. The advanced seed,
curated catalog, and autonomous walkthrough are all behind you.
The final gate is the manual regression — take your time with it.

================================================================
```
  </action>
  <verify>
    <automated>grep -q "awaiting-user-uat" .planning/STATE.md && grep -q "Phase 10.*awaiting user manual UAT" .planning/STATE.md</automated>
  </verify>
  <done>
STATE.md reflects the ready-for-UAT status with a clear resume pointer. The Decisions log mentions Phase 10's catalog shape, decimal sub-phase count, and the human-UAT acceptance gate. The handoff banner is posted.
  </done>
</task>

</tasks>

<verification>
Plan 10-03 is complete (Claude-side) when:

1. 10-UAT-RUNBOOK.md is polished with [Claude-checked] tags and all inline issues resolved or explicitly deferred
2. 10-PERF-OBSERVATIONS.md exists with real measured data and Phase 11 recommendations
3. Full test suite green: vitest, playwright, pytest
4. scripts/mock-audit.sh exits 0
5. READY-FOR-UAT.md exists with accurate test counts, walk summary, and user instructions
6. STATE.md updated to "awaiting-user-uat" with session continuity pointer
7. Handoff banner posted to the user

**The user-side completion criterion** (Phase 10 close): Plan 10-03's Task 4 is not self-verifiable. Phase 10 closes when the user ticks every runbook item, resolves all P0/P1 issues, and posts "UAT PASS" in the runbook's Findings log. This is intentionally a human-only gate per D-15.1 and is documented in must_haves.truths.
</verification>

<success_criteria>
Plan 10-03 is the quiet closeout. It ships when:

- Claude has handed off cleanly to the user
- Every artifact the user needs to complete the manual regression is ready and polished
- The test suite is green one final time
- STATE.md reflects the ready-for-UAT status
- The handoff banner is posted

Phase 10 itself ships when the user walks the runbook, fixes any P0/P1 issues found, and posts "UAT PASS". That user-facing gate is outside the scope of this plan's automated verification.
</success_criteria>

<output>
After completion, create `.planning/phases/10-comprehensive-testing-with-advanced-seed-data/10-03-SUMMARY.md` documenting:

- Final test suite status (counts and durations)
- mock-audit.sh final result
- UAT runbook item counts (total, [Claude-checked], pending manual, issues)
- Path to READY-FOR-UAT.md and 10-PERF-OBSERVATIONS.md
- STATE.md transition (previous status → new status)
- Handoff signal: "ready for user UAT"
- Note that Phase 10 close criterion is user UAT PASS — not automated
</output>
